# Pixel Asset Studio

`tools/Pixel Asset Studio/` is the normalized forward-facing path for pixel-art authoring.

## Transitional compatibility

- Current implementation target: `tools/Sprite Editor/`
- Legacy repo-local variants such as `tools/SpriteEditor/` remain legacy-only and are not the naming standard.
- Future docs and user-facing references should use `Pixel Asset Studio`.
