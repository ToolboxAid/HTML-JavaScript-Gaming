# Rollback Notes

Rollback guidance is documentation only.

- The vector-native template baseline does not package sprite fallback assets.
- If a project needs a temporary rollback during local experimentation, document that outside the active template runtime contract.
- Do not reintroduce sprite fallback as an active packaged dependency for the template baseline.
