# Text to Speech V2

Text to Speech V2 is a first-class tool for previewing browser text-to-speech output from sample narration, game prompts, and menu feedback text.

The tool uses the shared engine audio `TextToSpeechEngine` wrapper and enum-style defaults from `src/engine/audio/TextToSpeechDefaults.js`.
