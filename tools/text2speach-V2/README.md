# text2speach-V2

text2speach-V2 is a first-class tool baseline for previewing browser text-to-speech output from sample narration, game prompts, and menu feedback text.

The tool uses the shared engine audio `TextToSpeechEngine` wrapper and enum-style defaults from `src/engine/audio/TextToSpeechDefaults.js`.
