# Tilemap Studio

`tools/Tilemap Studio/` is the normalized forward-facing path for tilemap authoring.

## Transitional compatibility

- Current implementation target: `tools/Tile Map Editor/`
- Legacy naming remains compatibility-only during the transition window.
- Future docs and user-facing references should use `Tilemap Studio`.
