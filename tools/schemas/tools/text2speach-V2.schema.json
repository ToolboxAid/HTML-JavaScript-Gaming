{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "tools/schemas/tools/text2speach-V2.schema.json",
  "title": "text2speach-V2 Payload",
  "type": "object",
  "required": ["$schema", "schema", "version", "name", "queue"],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "const": "tools/schemas/tools/text2speach-V2.schema.json"
    },
    "schema": {
      "type": "string",
      "const": "html-js-gaming.text2speach-V2"
    },
    "version": {
      "type": "integer",
      "minimum": 1
    },
    "name": {
      "type": "string",
      "minLength": 1
    },
    "queue": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/speechQueueItem"
      }
    }
  },
  "$defs": {
    "speechQueueItem": {
      "type": "object",
      "required": [
        "id",
        "name",
        "text",
        "language",
        "voice",
        "volume",
        "rate",
        "pitch",
        "queueMode",
        "autoSpeak",
        "repeatCount",
        "delayBetweenRepeatsMs",
        "characterPreset",
        "ssmlLikePreset"
      ],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string",
          "minLength": 1
        },
        "text": {
          "type": "string",
          "minLength": 1
        },
        "language": {
          "type": "string",
          "enum": ["en-US", "en-GB", "es-ES", "fr-FR", "ja-JP"]
        },
        "voice": {
          "type": "string"
        },
        "volume": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "rate": {
          "type": "number",
          "minimum": 0.1,
          "maximum": 10
        },
        "pitch": {
          "type": "number",
          "minimum": 0,
          "maximum": 2
        },
        "queueMode": {
          "type": "string",
          "enum": ["replace", "append"]
        },
        "autoSpeak": {
          "type": "boolean"
        },
        "repeatCount": {
          "enum": [1, 2, 3, "loop"]
        },
        "delayBetweenRepeatsMs": {
          "type": "number",
          "minimum": 0,
          "maximum": 5000
        },
        "characterPreset": {
          "type": "string",
          "enum": ["narrator", "hero", "villain", "alert"]
        },
        "ssmlLikePreset": {
          "type": "string",
          "enum": ["normal", "whisper-ish", "announcement", "slow"]
        }
      }
    }
  },
  "description": "Payload-only text2speach-V2 schema for workspace-embeddable speech queue data."
}
