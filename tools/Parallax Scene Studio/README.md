# Parallax Scene Studio

`tools/Parallax Scene Studio/` is the normalized forward-facing path for parallax composition.

## Transitional compatibility

- Current implementation target: `tools/Parallax Editor/`
- Legacy naming remains compatibility-only during the transition window.
- Future docs and user-facing references should use `Parallax Scene Studio`.
