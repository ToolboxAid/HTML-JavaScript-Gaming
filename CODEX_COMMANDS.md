MODEL: GPT-5.4
REASONING: medium
COMMAND: BUILD_PR: add InputMap to V2 input system, extend InputService to support action-based queries without breaking existing APIs, create sample06-input-mapping demonstrating multiple keys mapped to the same action, add minimal tests and docs, and keep strict concern separation
