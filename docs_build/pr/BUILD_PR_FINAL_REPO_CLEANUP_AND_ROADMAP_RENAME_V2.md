# FINAL REPO CLEANUP + ROADMAP RENAME (V2)

## Purpose
Final safe cleanup with explicit exclusion for template structure.

## Scope
- remove empty directories EXCEPT:
  - games/_template/
- remove orphaned `.keep` files (except where needed for template)
- clean docs residue
- rename roadmap file
- update all references

## Hard Guard (UPDATED)
- DO NOT delete:
  games/_template/
  OR any directories inside it, even if empty

## Acceptance
- no empty dirs remain outside template
- template structure preserved intact
- roadmap renamed and references updated
- validation report confirms no breakage
