Theme V2 public display-mode asset path fix - PR_26154_005-theme-v2-public-asset-path-fix

- Update `tool-display-mode.js` so badge and character images resolve from `/assets/theme/v2/assets/images/` instead of deriving image URLs from `src/engine/theme/v2`.
- Seed public Theme V2 badge and character PNGs under `assets/theme/v2/assets/images/`, including required `badges/index.png` and `characters/index.png` fallbacks.
- Keep `src/engine/theme` in place and document remaining references as engine/runtime shell styling, current public Theme V2 styling/scripts, or obsolete public image-source references needing follow-up.
- Validate changed JS, targeted asset paths/references, browser image request statuses, diff whitespace, review artifacts, and repo-structured delta ZIP packaging.
