Theme V1 assets, games images, and favicon cleanup - PR_26154_009-theme-v1-assets-games-favicon-cleanup

- Canonicalize active HTML favicon links to root `/favicon.ico` and confirm no duplicate `.ico` files exist.
- Move Theme V2 games art into `games/assets/images/` and update active game image references.
- Move confirmed static V1 companions into `assets/theme/v1/` while keeping active `src/engine/theme` runtime shell styling/code in place.
- Validate targeted references, static paths, root HTTP asset resolution, diff whitespace, review artifacts, and repo-structured delta ZIP packaging.
