Rename active tools surface to toolbox and align footer/template wiring - PR_26154_022-toolbox-footer-template-consistency-bundle

- Rename active `tools/` surface to `toolbox/` and update active routes, docs, scripts, tests, and Workspace/tool launch references.
- Rename the active tool template source to `toolbox/_tool_template-v2/` and update project instructions.
- Align shared footer IA to the requested Product, Community, Docs & Help, Account, Legal, and Company groups.
- Add Theme V2 CSS wiring to marketplace and verify Toolbox, Objects, and Worlds menu behavior there.
- Produce template consistency audit and bundle reports with remaining mismatches documented.
- Validate with targeted path/static checks, `git diff --check`, and `npm run test:workspace-v2`.
