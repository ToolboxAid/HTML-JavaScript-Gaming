Theme V2 public asset teardown - PR_26154_008-theme-v2-public-asset-teardown

- Move remaining public/root Theme V2 CSS, JS helpers, partials, and `image-missing.svg` into `assets/theme/v2/`.
- Update public/root pages, templates, tool pages, old tool reference, and docs to consume `assets/theme/v2/`.
- Remove the empty former Theme V2 source folder after confirming no references remain outside generated artifacts.
- Keep unrelated `src/engine` runtime files in place.
- Validate targeted references, changed static files, browser loading of moved assets, diff whitespace, review artifacts, and repo-structured delta ZIP packaging.
