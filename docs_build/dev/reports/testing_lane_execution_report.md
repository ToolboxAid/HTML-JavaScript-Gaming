# Testing Lane Execution Report

PR: PR_26157_016-login-page-session-mode-lockdown

## Impacted Lanes

- Theme V2 header/session state
- Login page local/dev session mode runtime
- Admin protected page direct URL access
- Account protected page direct URL access
- Shared Mock DB session persistence gating
- Admin DB Viewer regression
- Project Journey session/search/runtime regression
- changed-file/static validation

## Commands Run

| Command | Result |
| --- | --- |
| `node --check assets/theme-v2/js/gamefoundry-partials.js` | PASS |
| `node --check assets/theme-v2/js/login-session.js` | PASS |
| `node --check src/engine/persistence/mock-db-store.js` | PASS |
| `node --check admin/db-viewer.js` | PASS |
| `node --check admin/notes.js` | PASS |
| `node --check admin/tools-progress.js` | PASS |
| `node --check tests/playwright/tools/LoginSessionMode.spec.mjs` | PASS |
| `node --check tests/playwright/tools/AdminDbViewer.spec.mjs` | PASS |
| `node --check tests/playwright/tools/ProjectJourneyTool.spec.mjs` | PASS |
| `npx playwright test tests/playwright/tools/LoginSessionMode.spec.mjs --reporter=list --workers=1` | PASS, 4/4 |
| `npx playwright test tests/playwright/tools/AdminDbViewer.spec.mjs --reporter=list --workers=1` | PASS, 2/2 |
| `npx playwright test tests/playwright/tools/ProjectJourneyTool.spec.mjs --reporter=list --workers=1` | PASS, 13/13 |
| `npx playwright test tests/playwright/tools/AdminDbViewer.spec.mjs tests/playwright/tools/ProjectJourneyTool.spec.mjs tests/playwright/tools/LoginSessionMode.spec.mjs --reporter=list --workers=1` | PASS, 19/19 |
| `npm run test:playwright:static` | PASS |
| `git diff --check` | PASS, line-ending warnings only |

## Behavior Covered

- Login page renders with Theme V2 header/footer and no inline style/script usage.
- Local mode shows Guest, User 1, User 2, User 3, and Admin.
- DEV mode shows no users, forces Guest, and disables mock DB persistence.
- Guest is not stored in the `users` table.
- Header shows Login for Guest/unauthenticated sessions.
- Header shows User 1/Admin display names for logged-in Local users.
- Account submenu appears only for logged-in users.
- Admin navigation appears only for Admin role.
- Direct Admin URLs block Guest and User 1.
- Direct Account URLs block Guest.
- Admin direct URL opens for Admin.
- Account direct URL opens for User 1.
- Guest can open Project Journey as an allowed Toolbox demo surface but cannot persist data.
- Admin DB Viewer still renders live shared Memory DB data and preserves clear/seed behavior on the authorized page.
- Project Journey session, Guest, filter, search, and persistence behavior remains green after session mode changes.

## Skipped Lanes

- Full samples smoke: SKIP per request.
- Full broad Playwright suite: SKIP because the changed shared surfaces are covered by targeted Login Session Mode, Admin DB Viewer, and Project Journey lanes.

## Notes

- `npm run test:playwright:static` regenerated the shared static validation reports under `docs_build/dev/reports/`.
- The final V8 coverage report was generated by the combined targeted Playwright run so it includes login/session, Admin DB Viewer, and Project Journey browser coverage.
