# PR_26158_031 Testing Lane Execution Report

## Lanes Run

| Lane | Command | Result |
| --- | --- | --- |
| Changed-file syntax | `node --check assets/theme-v2/js/gamefoundry-partials.js`; `node --check src/engine/api/server-api-client.js`; `node --check tests/playwright/tools/StaticOnlyLoginFallback.spec.mjs` | PASS |
| Static-only login Playwright | `npx playwright test tests/playwright/tools/StaticOnlyLoginFallback.spec.mjs` | PASS, 1/1 |
| LoginSessionMode Playwright | `npx playwright test tests/playwright/tools/LoginSessionMode.spec.mjs` | PASS, 5/5 on serialized rerun |
| ToolboxRoutePages Playwright | `npx playwright test tests/playwright/tools/ToolboxRoutePages.spec.mjs` | PASS, 1/1 |
| Changed-file/static validation | `git diff --check` | PASS, with Git line-ending warnings only |
| ShowOneChild/ActionableCoachmark reference audit | `rg -n "ShowOneChild\|showOneChild\|ActionableCoachmark" .`; `Get-ChildItem -Path . -Recurse -Filter ShowOneChild.js -File`; scoped active HTML/JS `rg` | PASS |
| Runtime JS V8 coverage | Playwright coverage generated by targeted runs | PASS/WARN; `docs_build/dev/reports/playwright_v8_coverage_report.txt` and `coverage_changed_js_guardrail.txt` generated. |

## Validation Notes

| Check | Evidence | Result |
| --- | --- | --- |
| Static-only login keeps Local Mem enabled and clickable when `/api/session/current` 404s. | `StaticOnlyLoginFallback.spec.mjs` serves `login.html` from a static-only `127.0.0.1` server and verifies Local Mem remains enabled, selected, and clickable. | PASS |
| Static-only Local DB shows visible actionable diagnostic. | `StaticOnlyLoginFallback.spec.mjs` clicks Local DB and verifies the status says Local DB requires the API-backed local server for SQLite-backed Local DB. | PASS |
| Static-only serving does not repeatedly spam `/api/session/current`. | `StaticOnlyLoginFallback.spec.mjs` now asserts exactly one failed `/api/session/current` request and no `/api/session/mode`, `/api/session/users`, or `/api/session/modes` follow-up failures after clicks. | PASS |
| Repo-owned ShowOneChild/ActionableCoachmark errors are absent. | Static-only Playwright now asserts no non-extension `ShowOneChild`, `showOneChild`, or `ActionableCoachmark` error text in page or console errors. | PASS |
| API-backed Local Mem and Local DB behavior still works. | `LoginSessionMode.spec.mjs` passed 5/5 on serialized rerun. | PASS |
| Toolbox route pages still render. | `ToolboxRoutePages.spec.mjs` passed 1/1 for Project Journey, Colors, and Assets `/tools/...` aliases. | PASS |

## Transient Validation Note

An initial parallel run of `LoginSessionMode.spec.mjs` and `ToolboxRoutePages.spec.mjs` produced a Playwright artifact-file `ENOENT` while both lanes wrote trace artifacts concurrently. `ToolboxRoutePages.spec.mjs` passed during that run, and `LoginSessionMode.spec.mjs` passed 5/5 when rerun by itself. No page assertion failed in the clean rerun.

## Skipped Lanes

| Lane | Decision | Reason |
| --- | --- | --- |
| AdminDbViewer Playwright | SKIP | DB Viewer routes, DB adapters, and admin DB rendering were not changed; LoginSessionMode covers API-backed Local DB session behavior touched by the shared session client. |
| Full samples smoke | SKIP | No samples, shared sample loader, or sample framework files changed. |
| Full Playwright suite | SKIP | Static-only login, API-backed login, toolbox route, syntax, static, and V8 coverage checks cover the changed surfaces. |
