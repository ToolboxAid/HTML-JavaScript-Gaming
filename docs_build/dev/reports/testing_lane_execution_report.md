# PR_26158_025 Testing Lane Execution Report

## Lanes Run

| Lane | Command | Result |
| --- | --- | --- |
| Changed-file syntax | `node --check src/dev-runtime/server/mock-api-router.mjs`; `node --check src/dev-runtime/persistence/mock-db-store.js`; `node --check admin/db-viewer.js`; `node --check tests/playwright/tools/LoginSessionMode.spec.mjs` | PASS |
| Local DB/API contract validation | Custom Node probe using `tests/helpers/playwrightRepoServer.mjs` with `GAMEFOUNDRY_LOCAL_DB_PATH=tmp/local-db/pr_025_contract.json` | PASS |
| LoginSessionMode Playwright | `npx playwright test tests/playwright/tools/LoginSessionMode.spec.mjs` | PASS, 5/5 |
| AdminDbViewer Playwright | `npx playwright test tests/playwright/tools/AdminDbViewer.spec.mjs` | PASS, 5/5 |
| ProjectJourneyTool Playwright | `npx playwright test tests/playwright/tools/ProjectJourneyTool.spec.mjs` | PASS, 13/13 |
| Static import boundary | Focused `rg` checks for forbidden browser imports of `src/dev-runtime`, mock repositories, and static `toolRegistry.js` | PASS |
| Changed-file/static validation | `git diff --check` | PASS, with Git line-ending warnings only |

## Local DB/API Contract Probe

| Assertion | Result |
| --- | --- |
| `/api/session/modes` returns only `Local Mem` and `Local DB`. | PASS |
| DEV/UAT/Prod are not local login choices. | PASS |
| `/api/data-source/adapter-contract` reports `local-db` as configured. | PASS |
| `POST /api/session/mode` with `local-db` succeeds through the server API. | PASS |
| Local DB exposes Guest/User 1/User 2/User 3/Admin as selector choices and does not store Guest in `users`. | PASS |
| Local DB resolves Admin through persisted users/roles/user_roles rows. | PASS |
| `/api/mock-db/snapshot` reads Local DB state through the server API. | PASS |
| Local DB clear persists after a new server instance reloads the same local DB JSON file. | PASS |
| Disabled Local DB storage fails visibly with `Local DB adapter not configured` and `GAMEFOUNDRY_LOCAL_DB_DISABLE` diagnostic text. | PASS |

## V8 Coverage

`docs_build/dev/reports/playwright_v8_coverage_report.txt` was regenerated by the targeted Playwright runs. Advisory warnings remain for server-side or non-browser-collected runtime modules.

## Skipped Lanes

| Lane | Decision | Reason |
| --- | --- | --- |
| Full samples smoke | SKIP | No shared sample loader/framework or sample JSON changed. |
| Full Playwright suite | SKIP | Targeted login, DB Viewer, Project Journey, server API, and static import lanes cover the changed surfaces. |
