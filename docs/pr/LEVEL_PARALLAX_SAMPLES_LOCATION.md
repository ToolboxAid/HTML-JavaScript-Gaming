
# LEVEL_PARALLAX_SAMPLES_LOCATION

Parallax sample files must reside in:

tools/Parallax Editor/samples/

These are editor-local learning and test files.

Rules:
- Must be loadable directly from the Parallax Editor
- Must demonstrate background and foreground layers
- Must not depend on external sample directories
