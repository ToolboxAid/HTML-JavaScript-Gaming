# PR_11_162_SVG_SHARED_BADGE_CONTEXT_TRACE_AND_DEAD_WIRING_REPORT

## Purpose
Fix the remaining SVG Asset Studio badge failure by constraining Codex to the shared shell display path only.

## Current confirmed state
- Workspace Manager launches.
- Sample 1902 loads the manifest.
- SVG Asset Studio receives valid SVG data.
- `payloadJson.vectorAssetDocument`, `svgText`, and `sourceName` exist.
- The visible shared shell badge still renders `Asset: none`.

## Scope
Codex must inspect and fix only the shared shell asset badge derivation path.

Allowed files:
- `tools/shared/platformShell.js`
- directly adjacent shared shell test/evidence files only if they already exist
- `docs/dev/reports/*` for evidence
- `docs/dev/roadmaps/MASTER_ROADMAP_ENGINE.md` status marker only, if execution-backed

Do not modify:
- schemas
- sample JSON
- SVG Asset Studio payload shape
- Workspace Manager manifest payloads
- start_of_day folders

## Required implementation intent for Codex
1. Find the exact code path that renders the header badge text for `Asset:`.
2. Confirm what context object the shared shell uses for asset label derivation.
3. Wire SVG payload-derived asset label into that existing derivation path without fallback data.
4. Use the real SVG payload fields already proven present; expected label is `sample-0901-ship.svg`.
5. Remove or document dead/obsolete badge branches found in the touched shared-shell path.
6. Do not introduce alias/pass-through variables.

## Dead code tracking requirement
Codex must add a report at:

`docs/dev/reports/pr_11_162_dead_wiring_report.txt`

The report must list:
- dead/unused branches discovered in shared shell badge rendering
- whether each branch was removed, left in place, or needs a follow-up PR
- exact reason for any retained branch

## Validation
Run targeted validation only:
- `node --check tools/shared/platformShell.js`
- launch sample 1902 / Workspace Manager / SVG Asset Studio path and verify badge text
- browser console check of `window.__TOOLS_HOST_SHARED_CONTEXT__` only if badge still fails

Full samples smoke test: skip unless Codex changes shared loader behavior beyond badge derivation.

## Acceptance
- SVG Asset Studio header badge shows `Asset: sample-0901-ship.svg`.
- No schemas changed.
- No sample JSON changed.
- No SVG tool payload contract changed.
- Dead/obsolete shared-shell badge wiring is tracked in the report.
