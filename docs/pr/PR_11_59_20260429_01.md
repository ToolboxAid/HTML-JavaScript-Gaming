# PR 11.59 — Bulk Metadata-Aware JSON Cleanup

## Purpose
Remove 32 audit-confirmed unused sample JSON files and remove matching stale references from `samples/metadata/samples.index.metadata.json` so the audit NO/missing-reference count drops.

## Scope
- Run `scripts/PS/audit-sample-json-js-references.ps1` before changes.
- Select 32 `NO` JSON items.
- Delete selected unused JSON files.
- Remove metadata/index references for deleted files.
- Re-run audit and prove the NO count drops.

## Guardrails
- Do not touch `palette.json`.
- Do not touch `tile-map-editor-document.json`.
- Do not touch sample 1902.
- Do not modify roadmap text during cleanup work.
- Only update roadmap status markers if execution-backed and already present.
- No shared loader/framework changes.
- No full sample suite.

## Metadata Rule
If the only broad repo reference is `samples/metadata/samples.index.metadata.json`, treat that as stale metadata. Delete the JSON file and remove the stale metadata entry/reference in the same PR.

## Acceptance
- 32 unused JSON files removed, unless fewer safe NO items remain.
- Matching stale metadata references removed.
- Before/after audit report captured.
- NO count decreases by the number of files removed.
- Targeted validation only; full samples smoke test skipped with reason.
