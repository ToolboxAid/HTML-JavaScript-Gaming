# Archive

Use this folder for old planning notes, review snapshots, and deprecated docs that are not part of the active working set.

Move documents here instead of deleting them when:
- they may still be useful for historical context
- they describe an older rewrite phase
- they would otherwise clutter the active docs
