# Legacy PR History Archive

This directory stores historical PR markdown records moved out of the active `docs/pr/` folder.

## Policy
- Preserve filenames exactly.
- Use archive moves instead of deletion.
- Keep active working PR docs in `docs/pr/`.
- Do not place start-of-day files here.
