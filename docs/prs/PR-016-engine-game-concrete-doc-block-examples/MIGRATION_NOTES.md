PR-016 — migration notes

### Current State

This PR is docs-only and compatibility-preserving.

### Recommended Next Direction

The next surgical step should turn these example blocks into concrete per-export documentation drafts, such as:
- example blocks for `GameCollision`, `GameObjectSystem`, and `GameUtils`
- example blocks for `GameObjectManager`, `GameObjectRegistry`, and `GameObjectUtils`
- standard reusable wording inserts for API/reference docs

### Guardrails

- no runtime behavior changes without approval
- no destructive changes
- no export removal in this PR
- keep example blocks consistent with approved wording and compatibility promises
