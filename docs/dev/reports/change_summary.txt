Change Summary

1) Feature gate
- Added score gate constant: `authoritativeScore`.
- Gate defaults OFF unless explicitly enabled through options.featureGates.

2) Authoritative score transition
- Promoted exactly one transition to authoritative behavior: `applyScoreDelta`.
- Implemented score authoritative apply function behind gate and passive-mode checks.

3) Transition-only write enforcement
- When score authoritative mode is active (`passiveMode=false` + gate ON), external snapshot patch writes to `worldState.scores` are rejected.
- Score writes in authoritative mode are routed through `requestTransition('applyScoreDelta', ...)`.

4) Read + consumer boundaries
- Selectors remain read-only (readonly clone behavior unchanged).
- Existing single consumer path is retained; no additional consumer introduced.

5) Contract tests
- Added score-specific contract test file and test runner registration.
- Full test suite passes.

6) Engine boundary
- No files under `engine/` were modified.
