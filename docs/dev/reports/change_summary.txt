Build: BUILD_PR_TOOL_THEME_AND_SHARED_SWATCH_WORKFLOW
Date: 2026-03-30

Implementation Summary
1) Shared theme adoption across tools
- Added shared theme include to Sprite Editor entry page:
  - tools/SpriteEditor/index.html now includes ../../engine/ui/hubCommon.css
- Verified all tool index pages now include the shared theme stylesheet:
  - tools/SpriteEditor/index.html
  - tools/SVG Background Editor/index.html
  - tools/Tile Map Editor/index.html
  - tools/Parallax Editor/index.html
  - tools/VectorMapEditor/index.html

2) SVG Background Editor explicit palette target workflow
- Updated palette target button labels to explicit workflow language:
  - Active Paint
  - Active Stroke
- Updated palette state/readout messaging to make behavior explicit:
  - User selects active target first, then selects palette/used swatch color.
  - Paint/Stroke values remain stored until intentionally changed.

3) Sprite Editor used-color swatch strip above palette
- Added used-color strip section above the main palette grid in the right panel.
- Added used-color discovery from active frame layer pixel data.
- Added swatch normalization for stable dedupe and matching.
- Used-color swatches are clickable and set current color directly.
- Main palette and used-color swatches now report source in status messaging.

Boundaries respected
- No files under engine/ were modified.
- Changes are scoped to tools and required docs/dev/reports outputs.
