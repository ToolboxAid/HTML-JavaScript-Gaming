Validation Checklist

[PASS] Feature gate added and default OFF
- `authoritativeScore` added in `src/advanced/state/constants.js` and defaulted to false in `createWorldGameStateSystem`.

[PASS] Exactly one authoritative score transition
- `applyScoreDelta` is the score transition with authoritative apply logic.

[PASS] All score writes via transition in authoritative mode
- External score patch writes are rejected when authoritative score mode is active.

[PASS] Selectors remain read-only
- Read-only selector contract preserved and validated by tests.

[PASS] One consumer only
- No additional consumers introduced; existing read-only consumer path retained.

[PASS] Contract tests added
- Added `tests/world/WorldGameStateAuthoritativeScore.test.mjs` and wired into `tests/run-tests.mjs`.

[PASS] No engine changes
- No modified `engine/` paths in working tree.

[PASS] Validation run
- `npm test` passed: 84/84 explicit tests.
