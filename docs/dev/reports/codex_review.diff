diff --git a/docs/dev/codex_commands.md b/docs/dev/codex_commands.md
index 41fcf58b..98884179 100644
--- a/docs/dev/codex_commands.md
+++ b/docs/dev/codex_commands.md
@@ -1,24 +1,34 @@
-# Codex Commands - PR_26126_026-locate-missing-samples
+# Codex Commands - PR_26126_028-sample-local-readme-and-fullscreen-capture-fix

 ```bash
-codex run "Create PR_26126_026-locate-missing-samples. Do not modify code. Perform a repo search to determine where sample IDs 0125, 0126, and 0127 are defined or referenced. Search for these IDs across samples, metadata, tool hints, and any index/registry files. Report exact file paths where they exist, or explicitly confirm they do not exist as folders under samples/phase-* directories. If they are referenced but missing physically, identify the source (metadata, index, or generator logic) that is producing them. Output findings only in docs/dev/reports/missing_samples_0125_0126_0127.txt. Produce review artifacts."
+codex run "Create PR_26126_028-sample-local-readme-and-fullscreen-capture-fix. Fix Preview Generator V2 documentation and capture behavior. Add a README.md template under existing sample folders using the pattern samples/phase-xx/XXXX/README.md. The README must explain where the sample implementation actually lives, how the sample folder/index.html launches or bypasses into that code, and how Preview Generator V2 should discover/launch the sample without assuming numeric folders. Do not add a root samples README. Do not modify sample JSON. Also fix Full Screen (1600x900 HTML Page) capture so it does not silently fall back to non-fullscreen capture when html-to-image/html2canvas fails on unsupported CSS color functions such as color(...). For fullscreen mode, capture must either use the intended 1600x900 viewport/full-page path or fail clearly with an actionable error; do not mark OK when fallback was used for fullscreen. Preserve Canvas Only behavior. Produce review artifacts."
 ```

 ## Validation Commands

 ```bash
-Get-ChildItem -Path samples -Directory -Recurse -ErrorAction SilentlyContinue | Where-Object { $_.Name -in @('0125','0126','0127') } | Select-Object -ExpandProperty FullName
-rg -n --hidden --no-ignore --glob '!node_modules/**' --glob '!.git/**' --glob '!tmp/**' --glob '!**/*.svg' --glob '!**/*.png' --glob '!**/*.jpg' --glob '!**/*.jpeg' --glob '!**/*.gif' --glob '!**/*.zip' "\b(0125|0126|0127)\b" samples docs tools src scripts tests games package.json *.json *.md
-Select-String -Path tools/preview-generator-v2/index.html -Pattern 'resolveSamplePhase|phaseDir.entries|hasIndexHtml|sampleEntries.push|sampleEntries.sort|No sample index.html' -Context 2,3
-Select-String -Path @('scripts/generate-sample-manifest.mjs','scripts/generate-samples-index.mjs','scripts/generate-runtime-sample-previews.mjs') -Pattern 'readdir|phase-|index.html|samplesDir|sampleId|id' -Context 2,3
-Select-String -Path samples/metadata/metadataReference.js,samples/metadata/samples.index.metadata.json -Pattern '0125|0126|0127|0124|0101|phase-01' -Context 1,2
-rg -n "0225|0226|0227" samples/metadata/samples.index.metadata.json samples/index.html samples/index.render.js --glob '!**/*.svg'
-rg -n -i "executeSample|execute sample|sample-card|samples-phase-list|samples-pinned-list|buildSampleRows|createSampleCard" samples/index.html samples/index.render.js samples/metadata/samples.index.metadata.json
-git diff --check -- docs/dev/reports/missing_samples_0125_0126_0127.txt docs/dev/codex_commands.md docs/dev/commit_comment.txt
-git diff --cached --name-only -- samples games start_of_day tools src scripts tests
+$sampleDirs = Get-ChildItem samples -Directory -Filter 'phase-*' | Sort-Object Name | ForEach-Object { Get-ChildItem $_.FullName -Directory | Where-Object { $_.Name -match '^\d{4}$' } | Sort-Object FullName }
+$missingReadmes = $sampleDirs | Where-Object { !(Test-Path (Join-Path $_.FullName 'README.md')) }
+if ($missingReadmes) { $missingReadmes.FullName; throw "Missing sample README files." }
+if (Test-Path samples/README.md) { throw "Unexpected root samples/README.md" }
+$sampleReadmes = $sampleDirs | ForEach-Object { Join-Path $_.FullName 'README.md' }
+rg -n "SAMPLE_LOCAL_CONTRACT_README|Preview Generator V2 discovers samples|SKIP|FAIL|games/<gamename>|index.html is the only discovery" $sampleReadmes
+rg -n "Full Screen capture failed|html2canvas may not support CSS color functions|extractBestToolFallbackSvg|using fallback" tools/preview-generator-v2/index.html
+git diff --check
+git diff --cached --name-only -- '*.json' start_of_day tools/shared tools/schemas
+npm run test:workspace-v2
+npm test
 npm run codex:review-artifacts
 ```

-## Notes
+## Playwright

-No Playwright impact. This PR is search/report only and does not modify runtime code, samples, schemas, or tests.
+No Playwright test was added. This PR changes documentation and Preview Generator V2 fullscreen capture error handling only. `npm run test:workspace-v2` was attempted but is not defined in the current `package.json`.
+
+## Test Notes
+
+`npm test` was attempted as the available default test gate and failed in the existing shared extraction guard baseline before this PR's changed Preview Generator V2 code or README files were involved. The reported violations are outside this PR scope.
+
+## Manual Test
+
+Use Preview Generator V2 with Canvas Only to confirm existing canvas capture still works. Use Full Screen (1600x900 HTML Page) on a target with unsupported CSS color functions and confirm it logs a clear `FAIL` instead of silently falling back to a non-fullscreen capture.
diff --git a/docs/dev/commit_comment.txt b/docs/dev/commit_comment.txt
index 0cffa076..8c9a22a7 100644
--- a/docs/dev/commit_comment.txt
+++ b/docs/dev/commit_comment.txt
@@ -1 +1 @@
-Locate missing sample IDs 0125-0127 - PR_26126_026-locate-missing-samples
+Document sample-local contracts and harden fullscreen preview capture - PR_26126_028-sample-local-readme-and-fullscreen-capture-fix
diff --git a/samples/phase-01/0101/README.md b/samples/phase-01/0101/README.md
new file mode 100644
index 00000000..398aa519
--- /dev/null
+++ b/samples/phase-01/0101/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0101 - Basic Loop
+
+Sample-local contract for `samples/phase-01/0101`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-01/0101/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `BouncerScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-01/0101` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-01` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-01/0101/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-01/0101/index.html
+samples/phase-01/0101
+0101
+```
+
+Phase folder:
+
+```text
+samples/phase-01
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-01/0102/README.md b/samples/phase-01/0102/README.md
new file mode 100644
index 00000000..b5176313
--- /dev/null
+++ b/samples/phase-01/0102/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0102 - Keyboard Move
+
+Sample-local contract for `samples/phase-01/0102`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-01/0102/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `KeyboardMoveScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-01/0102` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-01` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-01/0102/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-01/0102/index.html
+samples/phase-01/0102
+0102
+```
+
+Phase folder:
+
+```text
+samples/phase-01
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-01/0103/README.md b/samples/phase-01/0103/README.md
new file mode 100644
index 00000000..93b8c8b6
--- /dev/null
+++ b/samples/phase-01/0103/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0103 - Mouse Input
+
+Sample-local contract for `samples/phase-01/0103`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-01/0103/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `MouseInputScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-01/0103` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-01` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-01/0103/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-01/0103/index.html
+samples/phase-01/0103
+0103
+```
+
+Phase folder:
+
+```text
+samples/phase-01
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-01/0104/README.md b/samples/phase-01/0104/README.md
new file mode 100644
index 00000000..201ee6a4
--- /dev/null
+++ b/samples/phase-01/0104/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0104 - Gamepad Input
+
+Sample-local contract for `samples/phase-01/0104`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-01/0104/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `GamepadScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-01/0104` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-01` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-01/0104/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-01/0104/index.html
+samples/phase-01/0104
+0104
+```
+
+Phase folder:
+
+```text
+samples/phase-01
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-01/0105/README.md b/samples/phase-01/0105/README.md
new file mode 100644
index 00000000..296c510e
--- /dev/null
+++ b/samples/phase-01/0105/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0105 - Scene Switch
+
+Sample-local contract for `samples/phase-01/0105`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-01/0105/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `IntroScene.js`
+- `main.js`
+- `PlayScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-01/0105` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-01` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-01/0105/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-01/0105/index.html
+samples/phase-01/0105
+0105
+```
+
+Phase folder:
+
+```text
+samples/phase-01
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-01/0106/README.md b/samples/phase-01/0106/README.md
new file mode 100644
index 00000000..e74e7625
--- /dev/null
+++ b/samples/phase-01/0106/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0106 - Input Mapping
+
+Sample-local contract for `samples/phase-01/0106`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-01/0106/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `InputMappingScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-01/0106` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-01` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-01/0106/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-01/0106/index.html
+samples/phase-01/0106
+0106
+```
+
+Phase folder:
+
+```text
+samples/phase-01
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-01/0107/README.md b/samples/phase-01/0107/README.md
new file mode 100644
index 00000000..a973fe27
--- /dev/null
+++ b/samples/phase-01/0107/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0107 - Entity Movement
+
+Sample-local contract for `samples/phase-01/0107`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-01/0107/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `EntityMovementScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-01/0107` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-01` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-01/0107/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-01/0107/index.html
+samples/phase-01/0107
+0107
+```
+
+Phase folder:
+
+```text
+samples/phase-01
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-01/0108/README.md b/samples/phase-01/0108/README.md
new file mode 100644
index 00000000..fa47fe93
--- /dev/null
+++ b/samples/phase-01/0108/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0108 - Render Adapter
+
+Sample-local contract for `samples/phase-01/0108`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-01/0108/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `RenderAdapterScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-01/0108` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-01` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-01/0108/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-01/0108/index.html
+samples/phase-01/0108
+0108
+```
+
+Phase folder:
+
+```text
+samples/phase-01
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-01/0109/README.md b/samples/phase-01/0109/README.md
index 52f161db..f6ef644d 100644
--- a/samples/phase-01/0109/README.md
+++ b/samples/phase-01/0109/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0109 - Collision (AABB)
+
+Sample-local contract for `samples/phase-01/0109`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-01/0109/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `CollisionScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-01/0109` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-01` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-01/0109/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-01/0109/index.html
+samples/phase-01/0109
+0109
+```
+
+Phase folder:
+
+```text
+samples/phase-01
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0109 - Collision (AABB)

 ## Purpose
diff --git a/samples/phase-01/0110/README.md b/samples/phase-01/0110/README.md
index d68d67f0..a1d59c88 100644
--- a/samples/phase-01/0110/README.md
+++ b/samples/phase-01/0110/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0110 - Collision Response
+
+Sample-local contract for `samples/phase-01/0110`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-01/0110/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `CollisionResponseScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-01/0110` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-01` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-01/0110/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-01/0110/index.html
+samples/phase-01/0110
+0110
+```
+
+Phase folder:
+
+```text
+samples/phase-01
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0110 - Collision Response

 ## Purpose
diff --git a/samples/phase-01/0111/README.md b/samples/phase-01/0111/README.md
index 1531136e..923e66ef 100644
--- a/samples/phase-01/0111/README.md
+++ b/samples/phase-01/0111/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0111 - Multiple Solids
+
+Sample-local contract for `samples/phase-01/0111`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-01/0111/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `MultipleSolidsScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-01/0111` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-01` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-01/0111/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-01/0111/index.html
+samples/phase-01/0111
+0111
+```
+
+Phase folder:
+
+```text
+samples/phase-01
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0111 - Multiple Solids

 ## Purpose
diff --git a/samples/phase-01/0112/README.md b/samples/phase-01/0112/README.md
index c14e91fb..6ce03559 100644
--- a/samples/phase-01/0112/README.md
+++ b/samples/phase-01/0112/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0112 - Axis-Separated Collision
+
+Sample-local contract for `samples/phase-01/0112`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-01/0112/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `AxisSeparatedCollisionScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-01/0112` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-01` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-01/0112/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-01/0112/index.html
+samples/phase-01/0112
+0112
+```
+
+Phase folder:
+
+```text
+samples/phase-01
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0112 - Axis-Separated Collision

 ## Purpose
diff --git a/samples/phase-01/0113/README.md b/samples/phase-01/0113/README.md
index ddaa7540..c2d95ea8 100644
--- a/samples/phase-01/0113/README.md
+++ b/samples/phase-01/0113/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0113 - Tile Collision
+
+Sample-local contract for `samples/phase-01/0113`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-01/0113/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `TileCollisionScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-01/0113` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-01` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-01/0113/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-01/0113/index.html
+samples/phase-01/0113
+0113
+```
+
+Phase folder:
+
+```text
+samples/phase-01
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0113 - Tile Collision

 ## Purpose
diff --git a/samples/phase-01/0114/README.md b/samples/phase-01/0114/README.md
index daac073d..b83ce94d 100644
--- a/samples/phase-01/0114/README.md
+++ b/samples/phase-01/0114/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0114 - Collision Debug Tools
+
+Sample-local contract for `samples/phase-01/0114`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-01/0114/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `CollisionDebugToolsScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-01/0114` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-01` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-01/0114/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-01/0114/index.html
+samples/phase-01/0114
+0114
+```
+
+Phase folder:
+
+```text
+samples/phase-01
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0114 - Collision Debug Tools

 ## Purpose
diff --git a/samples/phase-01/0115/README.md b/samples/phase-01/0115/README.md
index b9c9c80c..5d9f18be 100644
--- a/samples/phase-01/0115/README.md
+++ b/samples/phase-01/0115/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0115 - ECS Foundation
+
+Sample-local contract for `samples/phase-01/0115`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-01/0115/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `ECSFoundationScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-01/0115` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-01` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-01/0115/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-01/0115/index.html
+samples/phase-01/0115
+0115
+```
+
+Phase folder:
+
+```text
+samples/phase-01
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0115 - ECS Foundation

 ## Purpose
diff --git a/samples/phase-01/0116/README.md b/samples/phase-01/0116/README.md
index ef53afbf..06076937 100644
--- a/samples/phase-01/0116/README.md
+++ b/samples/phase-01/0116/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0116 - ECS Movement System
+
+Sample-local contract for `samples/phase-01/0116`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-01/0116/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `ECSMovementSystemScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-01/0116` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-01` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-01/0116/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-01/0116/index.html
+samples/phase-01/0116
+0116
+```
+
+Phase folder:
+
+```text
+samples/phase-01
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0116 - ECS Movement System

 ## Purpose
diff --git a/samples/phase-01/0117/README.md b/samples/phase-01/0117/README.md
index c22e51cd..b7cac583 100644
--- a/samples/phase-01/0117/README.md
+++ b/samples/phase-01/0117/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0117 - ECS Input System
+
+Sample-local contract for `samples/phase-01/0117`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-01/0117/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `ECSInputSystemScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-01/0117` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-01` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-01/0117/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-01/0117/index.html
+samples/phase-01/0117
+0117
+```
+
+Phase folder:
+
+```text
+samples/phase-01
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0117 - ECS Input System

 ## Purpose
diff --git a/samples/phase-01/0118/README.md b/samples/phase-01/0118/README.md
index 6eaf1257..ccf1a45b 100644
--- a/samples/phase-01/0118/README.md
+++ b/samples/phase-01/0118/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0118 - ECS Collision System
+
+Sample-local contract for `samples/phase-01/0118`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-01/0118/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `ECSCollisionSystemScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-01/0118` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-01` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-01/0118/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-01/0118/index.html
+samples/phase-01/0118
+0118
+```
+
+Phase folder:
+
+```text
+samples/phase-01
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0118 - ECS Collision System

 ## Purpose
diff --git a/samples/phase-01/0119/README.md b/samples/phase-01/0119/README.md
index 50e1194c..7f7ec1d8 100644
--- a/samples/phase-01/0119/README.md
+++ b/samples/phase-01/0119/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0119 - ECS Render System
+
+Sample-local contract for `samples/phase-01/0119`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-01/0119/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `ECSRenderSystemScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-01/0119` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-01` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-01/0119/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-01/0119/index.html
+samples/phase-01/0119
+0119
+```
+
+Phase folder:
+
+```text
+samples/phase-01
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0119 - ECS Render System

 ## Purpose
diff --git a/samples/phase-01/0120/README.md b/samples/phase-01/0120/README.md
index d970367d..37e6ecd3 100644
--- a/samples/phase-01/0120/README.md
+++ b/samples/phase-01/0120/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0120 - ECS Scene World
+
+Sample-local contract for `samples/phase-01/0120`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-01/0120/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `ECSSceneWorldScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-01/0120` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-01` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-01/0120/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-01/0120/index.html
+samples/phase-01/0120
+0120
+```
+
+Phase folder:
+
+```text
+samples/phase-01
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0120 - ECS Scene World

 ## Purpose
diff --git a/samples/phase-01/0121/README.md b/samples/phase-01/0121/README.md
index 7559067d..487e93ea 100644
--- a/samples/phase-01/0121/README.md
+++ b/samples/phase-01/0121/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0121 - UI Overlay
+
+Sample-local contract for `samples/phase-01/0121`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-01/0121/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `UIOverlayScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-01/0121` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-01` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-01/0121/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-01/0121/index.html
+samples/phase-01/0121
+0121
+```
+
+Phase folder:
+
+```text
+samples/phase-01
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0121 - UI Overlay

 ## Purpose
diff --git a/samples/phase-01/0122/README.md b/samples/phase-01/0122/README.md
index 9fb41ad0..8d25acea 100644
--- a/samples/phase-01/0122/README.md
+++ b/samples/phase-01/0122/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0122 - Entity Lifecycle
+
+Sample-local contract for `samples/phase-01/0122`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-01/0122/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `EntityLifecycleScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-01/0122` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-01` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-01/0122/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-01/0122/index.html
+samples/phase-01/0122
+0122
+```
+
+Phase folder:
+
+```text
+samples/phase-01
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0122 - Entity Lifecycle

 ## Purpose
diff --git a/samples/phase-01/0123/README.md b/samples/phase-01/0123/README.md
index 45c021e8..58958c08 100644
--- a/samples/phase-01/0123/README.md
+++ b/samples/phase-01/0123/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0123 - Debug Stats
+
+Sample-local contract for `samples/phase-01/0123`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-01/0123/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `DebugStatsScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-01/0123` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-01` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-01/0123/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-01/0123/index.html
+samples/phase-01/0123
+0123
+```
+
+Phase folder:
+
+```text
+samples/phase-01
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0123 - Debug Stats

 ## Purpose
diff --git a/samples/phase-01/0124/README.md b/samples/phase-01/0124/README.md
index 57a236c9..cbfc5167 100644
--- a/samples/phase-01/0124/README.md
+++ b/samples/phase-01/0124/README.md
@@ -1,3 +1,66 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0124 - Data Driven World
+
+Sample-local contract for `samples/phase-01/0124`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-01/0124/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `DataDrivenWorldScene.js`
+- `index.html`
+- `main.js`
+- `worldData.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-01/0124` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-01` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-01/0124/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-01/0124/index.html
+samples/phase-01/0124
+0124
+```
+
+Phase folder:
+
+```text
+samples/phase-01
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0124 - Data-Driven World

 ## Purpose
diff --git a/samples/phase-02/0201/README.md b/samples/phase-02/0201/README.md
index 682655d8..d74f9f4b 100644
--- a/samples/phase-02/0201/README.md
+++ b/samples/phase-02/0201/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0201 - Camera Follow
+
+Sample-local contract for `samples/phase-02/0201`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0201/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `CameraFollowScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0201` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0201/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0201/index.html
+samples/phase-02/0201
+0201
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0201 - Camera Follow

 ## Purpose
diff --git a/samples/phase-02/0202/README.md b/samples/phase-02/0202/README.md
index 7b836652..8142b029 100644
--- a/samples/phase-02/0202/README.md
+++ b/samples/phase-02/0202/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0202 - Camera Bounds
+
+Sample-local contract for `samples/phase-02/0202`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0202/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `CameraBoundsScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0202` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0202/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0202/index.html
+samples/phase-02/0202
+0202
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0202 - Camera Bounds

 ## Purpose
diff --git a/samples/phase-02/0203/README.md b/samples/phase-02/0203/README.md
index c2d4b9e9..f41dc71d 100644
--- a/samples/phase-02/0203/README.md
+++ b/samples/phase-02/0203/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0203 - Minimap
+
+Sample-local contract for `samples/phase-02/0203`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0203/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `MinimapScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0203` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0203/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0203/index.html
+samples/phase-02/0203
+0203
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0203 - Minimap

 ## Purpose
diff --git a/samples/phase-02/0204/README.md b/samples/phase-02/0204/README.md
index de5ec4e8..fca064c6 100644
--- a/samples/phase-02/0204/README.md
+++ b/samples/phase-02/0204/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0204 - Asset Registry
+
+Sample-local contract for `samples/phase-02/0204`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0204/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `AssetRegistryScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0204` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0204/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0204/index.html
+samples/phase-02/0204
+0204
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0204 - Asset Registry

 ## Purpose
diff --git a/samples/phase-02/0205/README.md b/samples/phase-02/0205/README.md
index dbadea6e..9c11bfa6 100644
--- a/samples/phase-02/0205/README.md
+++ b/samples/phase-02/0205/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0205 - Save Load State
+
+Sample-local contract for `samples/phase-02/0205`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0205/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `SaveLoadStateScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0205` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0205/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0205/index.html
+samples/phase-02/0205
+0205
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0205 - Save Load State

 ## Purpose
diff --git a/samples/phase-02/0206/README.md b/samples/phase-02/0206/README.md
index 6c791356..3d1c4e60 100644
--- a/samples/phase-02/0206/README.md
+++ b/samples/phase-02/0206/README.md
@@ -1,3 +1,66 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0206- Level Loader
+
+Sample-local contract for `samples/phase-02/0206`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0206/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `levelData.js`
+- `LevelLoaderScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0206` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0206/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0206/index.html
+samples/phase-02/0206
+0206
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0206- Level Loader

 ## Purpose
diff --git a/samples/phase-02/0207/README.md b/samples/phase-02/0207/README.md
index a3974ab8..73e40dfa 100644
--- a/samples/phase-02/0207/README.md
+++ b/samples/phase-02/0207/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0207 - Animation System
+
+Sample-local contract for `samples/phase-02/0207`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0207/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `AnimationSystemScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0207` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0207/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0207/index.html
+samples/phase-02/0207
+0207
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0207 - Animation System

 ## Purpose
diff --git a/samples/phase-02/0208/README.md b/samples/phase-02/0208/README.md
index 5ea63e15..236399b3 100644
--- a/samples/phase-02/0208/README.md
+++ b/samples/phase-02/0208/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0208 - State Machine
+
+Sample-local contract for `samples/phase-02/0208`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0208/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `StateMachineScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0208` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0208/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0208/index.html
+samples/phase-02/0208
+0208
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0208 - State Machine

 ## Purpose
diff --git a/samples/phase-02/0209/README.md b/samples/phase-02/0209/README.md
index b5e161fe..36bea392 100644
--- a/samples/phase-02/0209/README.md
+++ b/samples/phase-02/0209/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0209 - Interaction System
+
+Sample-local contract for `samples/phase-02/0209`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0209/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `InteractionSystemScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0209` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0209/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0209/index.html
+samples/phase-02/0209
+0209
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0209 - Interaction System

 ## Purpose
diff --git a/samples/phase-02/0210/README.md b/samples/phase-02/0210/README.md
index 9fefffd2..dc2c1aa3 100644
--- a/samples/phase-02/0210/README.md
+++ b/samples/phase-02/0210/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0210 - Projectile Spawn System
+
+Sample-local contract for `samples/phase-02/0210`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0210/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `ProjectileSystemScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0210` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0210/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0210/index.html
+samples/phase-02/0210
+0210
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0210 - Projectile Spawn System

 ## Purpose
diff --git a/samples/phase-02/0211/README.md b/samples/phase-02/0211/README.md
index 46d858f6..4ca652e5 100644
--- a/samples/phase-02/0211/README.md
+++ b/samples/phase-02/0211/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0211 - Enemy Patrol AI
+
+Sample-local contract for `samples/phase-02/0211`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0211/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `EnemyPatrolAIScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0211` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0211/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0211/index.html
+samples/phase-02/0211
+0211
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0211 - Enemy Patrol AI

 ## Purpose
diff --git a/samples/phase-02/0212/README.md b/samples/phase-02/0212/README.md
index 0028f30b..30e7bd4a 100644
--- a/samples/phase-02/0212/README.md
+++ b/samples/phase-02/0212/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0212 - Playable Micro-Level
+
+Sample-local contract for `samples/phase-02/0212`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0212/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `PlayableMicroLevelScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0212` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0212/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0212/index.html
+samples/phase-02/0212
+0212
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0216 - Playable Micro-Level

 ## Purpose
diff --git a/samples/phase-02/0213/README.md b/samples/phase-02/0213/README.md
index 11fd10f6..558ce3db 100644
--- a/samples/phase-02/0213/README.md
+++ b/samples/phase-02/0213/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0213 - Sprite Render Layer
+
+Sample-local contract for `samples/phase-02/0213`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0213/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `SpriteRenderLayerScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0213` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0213/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0213/index.html
+samples/phase-02/0213
+0213
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0217 - Sprite Render Layer

 ## Purpose
diff --git a/samples/phase-02/0214/README.md b/samples/phase-02/0214/README.md
index a6132ff3..3dc05b2b 100644
--- a/samples/phase-02/0214/README.md
+++ b/samples/phase-02/0214/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0214 - Animation Sprite Binding
+
+Sample-local contract for `samples/phase-02/0214`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0214/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `AnimationSpriteBindingScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0214` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0214/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0214/index.html
+samples/phase-02/0214
+0214
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0218 - Animation Sprite Binding

 ## Purpose
diff --git a/samples/phase-02/0215/README.md b/samples/phase-02/0215/README.md
index 630fcef8..e6470968 100644
--- a/samples/phase-02/0215/README.md
+++ b/samples/phase-02/0215/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0215 - Z Layer Ordering
+
+Sample-local contract for `samples/phase-02/0215`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0215/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `ZLayerOrderingScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0215` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0215/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0215/index.html
+samples/phase-02/0215
+0215
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0219 - Z Layer Ordering

 ## Purpose
diff --git a/samples/phase-02/0216/README.md b/samples/phase-02/0216/README.md
index db23906b..f803a9cc 100644
--- a/samples/phase-02/0216/README.md
+++ b/samples/phase-02/0216/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0216 - Prefab System
+
+Sample-local contract for `samples/phase-02/0216`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0216/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `PrefabSystemScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0216` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0216/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0216/index.html
+samples/phase-02/0216
+0216
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0216 - Prefab System

 ## Purpose
diff --git a/samples/phase-02/0217/README.md b/samples/phase-02/0217/README.md
index c929c926..1934fb3d 100644
--- a/samples/phase-02/0217/README.md
+++ b/samples/phase-02/0217/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0217 - Game Mode State
+
+Sample-local contract for `samples/phase-02/0217`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0217/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `GameModeStateScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0217` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0217/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0217/index.html
+samples/phase-02/0217
+0217
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0217 - Game Mode State

 ## Purpose
diff --git a/samples/phase-02/0218/README.md b/samples/phase-02/0218/README.md
index 45cb7c43..35b2021b 100644
--- a/samples/phase-02/0218/README.md
+++ b/samples/phase-02/0218/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0218 - Polished Playable Slice
+
+Sample-local contract for `samples/phase-02/0218`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0218/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `PolishedPlayableSliceScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0218` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0218/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0218/index.html
+samples/phase-02/0218
+0218
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0218 - Polished Playable Slice

 ## Purpose
diff --git a/samples/phase-02/0219/README.md b/samples/phase-02/0219/README.md
index d47e5dcf..48f8d710 100644
--- a/samples/phase-02/0219/README.md
+++ b/samples/phase-02/0219/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0219 - Sprite Atlas + Image Rendering
+
+Sample-local contract for `samples/phase-02/0219`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0219/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `SpriteAtlasImageRenderingScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0219` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0219/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0219/index.html
+samples/phase-02/0219
+0219
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0219 - Sprite Atlas + Image Rendering

 ## Purpose
diff --git a/samples/phase-02/0220/README.md b/samples/phase-02/0220/README.md
index ffbd9cae..0a92ca1f 100644
--- a/samples/phase-02/0220/README.md
+++ b/samples/phase-02/0220/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0220 - Camera System
+
+Sample-local contract for `samples/phase-02/0220`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0220/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `CameraSystemScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0220` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0220/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0220/index.html
+samples/phase-02/0220
+0220
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 01044 - Camera System

 ## Purpose
diff --git a/samples/phase-02/0221/README.md b/samples/phase-02/0221/README.md
index 0392f12c..b06e201e 100644
--- a/samples/phase-02/0221/README.md
+++ b/samples/phase-02/0221/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0221 - Tilemap System
+
+Sample-local contract for `samples/phase-02/0221`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0221/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `TilemapSystemScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0221` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0221/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0221/index.html
+samples/phase-02/0221
+0221
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0221 - Tilemap System

 ## Purpose
diff --git a/samples/phase-02/0222/README.md b/samples/phase-02/0222/README.md
index 990b4940..9af695f6 100644
--- a/samples/phase-02/0222/README.md
+++ b/samples/phase-02/0222/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0222 - Input Action Mapping
+
+Sample-local contract for `samples/phase-02/0222`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0222/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `InputActionMappingScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0222` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0222/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0222/index.html
+samples/phase-02/0222
+0222
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0222 - Input Action Mapping

 ## Purpose
diff --git a/samples/phase-02/0223/README.md b/samples/phase-02/0223/README.md
index db77958e..d82c43b6 100644
--- a/samples/phase-02/0223/README.md
+++ b/samples/phase-02/0223/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0223 - World Serialization
+
+Sample-local contract for `samples/phase-02/0223`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0223/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `WorldSerializationScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0223` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0223/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0223/index.html
+samples/phase-02/0223
+0223
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0223 - World Serialization

 ## Purpose
diff --git a/samples/phase-02/0224/README.md b/samples/phase-02/0224/README.md
index f096bde6..a9aa291a 100644
--- a/samples/phase-02/0224/README.md
+++ b/samples/phase-02/0224/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0224 - Tile + Camera + Sprite Slice
+
+Sample-local contract for `samples/phase-02/0224`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0224/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `TileCameraSpriteSliceScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0224` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0224/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0224/index.html
+samples/phase-02/0224
+0224
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0224 - Tile + Camera + Sprite Slice

 ## Purpose
diff --git a/samples/phase-02/0225/README.md b/samples/phase-02/0225/README.md
new file mode 100644
index 00000000..ed600d9d
--- /dev/null
+++ b/samples/phase-02/0225/README.md
@@ -0,0 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Projectile Lab
+
+Sample-local contract for `samples/phase-02/0225`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0225/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `flow/attract.js`
+- `flow/highscore.js`
+- `flow/intro.js`
+- `game/ProjectileLabModel.js`
+- `game/ProjectileLabScene.js`
+- `index.html`
+- `main.js`
+- `rules/flowContent.js`
+- `rules/flowRules.js`
+- `rules/gameFlowRules.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0225` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0225/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0225/index.html
+samples/phase-02/0225
+0225
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-02/0226/README.md b/samples/phase-02/0226/README.md
index f24e2b18..ac48252e 100644
--- a/samples/phase-02/0226/README.md
+++ b/samples/phase-02/0226/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0226 - Primitive Skin Editor Breakout Readiness
+
+Sample-local contract for `samples/phase-02/0226`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0226/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `PrimitiveSkinEditorBreakoutReadinessScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0226` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0226/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0226/index.html
+samples/phase-02/0226
+0226
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0226 - Primitive Skin Editor Breakout Readiness

 ## Purpose
diff --git a/samples/phase-02/0227/README.md b/samples/phase-02/0227/README.md
index 13e56f0a..6ef4c111 100644
--- a/samples/phase-02/0227/README.md
+++ b/samples/phase-02/0227/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 0227 - Primitive Skin Editor Pong Readiness
+
+Sample-local contract for `samples/phase-02/0227`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-02/0227/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `PrimitiveSkinEditorPongReadinessScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-02/0227` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-02` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-02/0227/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-02/0227/index.html
+samples/phase-02/0227
+0227
+```
+
+Phase folder:
+
+```text
+samples/phase-02
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 0227 - Primitive Skin Editor Pong Readiness

 ## Purpose
diff --git a/samples/phase-03/0301/README.md b/samples/phase-03/0301/README.md
index 97190876..3b8c58da 100644
--- a/samples/phase-03/0301/README.md
+++ b/samples/phase-03/0301/README.md
@@ -1,3 +1,66 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0301 - Real Sprite Rendering
+
+Sample-local contract for `samples/phase-03/0301`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0301/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `demoSpriteFactory.js`
+- `index.html`
+- `main.js`
+- `RealSpriteRenderingScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0301` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0301/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0301/index.html
+samples/phase-03/0301
+0301
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # sample 0301 - Real Sprite Rendering

 ## Purpose
diff --git a/samples/phase-03/0302/README.md b/samples/phase-03/0302/README.md
index d32be0ca..9a16b05b 100644
--- a/samples/phase-03/0302/README.md
+++ b/samples/phase-03/0302/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0302 - Animation System
+
+Sample-local contract for `samples/phase-03/0302`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0302/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `AnimationSystemScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0302` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0302/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0302/index.html
+samples/phase-03/0302
+0302
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # sample 0302 - Animation System

 ## Purpose
diff --git a/samples/phase-03/0303/README.md b/samples/phase-03/0303/README.md
index 7e086d7a..48ce050d 100644
--- a/samples/phase-03/0303/README.md
+++ b/samples/phase-03/0303/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0303 - Physics System
+
+Sample-local contract for `samples/phase-03/0303`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0303/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `PhysicsSystemScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0303` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0303/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0303/index.html
+samples/phase-03/0303
+0303
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # sample 0303 - Physics System

 ## Purpose
diff --git a/samples/phase-03/0304/README.md b/samples/phase-03/0304/README.md
index 4d02dbd2..6f7ace59 100644
--- a/samples/phase-03/0304/README.md
+++ b/samples/phase-03/0304/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0304 - Collision Resolution
+
+Sample-local contract for `samples/phase-03/0304`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0304/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `CollisionResolutionScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0304` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0304/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0304/index.html
+samples/phase-03/0304
+0304
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # sample 0304 - Collision Resolution

 ## Purpose
diff --git a/samples/phase-03/0305/README.md b/samples/phase-03/0305/README.md
index fbef9b3b..de865a5b 100644
--- a/samples/phase-03/0305/README.md
+++ b/samples/phase-03/0305/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0305 - Tile Metadata
+
+Sample-local contract for `samples/phase-03/0305`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0305/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `TileMetadataScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0305` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0305/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0305/index.html
+samples/phase-03/0305
+0305
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # sample 0305 - Tile Metadata

 ## Purpose
diff --git a/samples/phase-03/0306/README.md b/samples/phase-03/0306/README.md
index a510fba7..c10e2a96 100644
--- a/samples/phase-03/0306/README.md
+++ b/samples/phase-03/0306/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0306 - NES-Style Zones + Parallax
+
+Sample-local contract for `samples/phase-03/0306`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0306/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `NESStyleZonesParallaxScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0306` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0306/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0306/index.html
+samples/phase-03/0306
+0306
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # sample 0306 - NES-Style Zones + Parallax

 ## Purpose
diff --git a/samples/phase-03/0307/README.md b/samples/phase-03/0307/README.md
index 1cf8e242..635fd814 100644
--- a/samples/phase-03/0307/README.md
+++ b/samples/phase-03/0307/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0307 - Slopes / Ramps
+
+Sample-local contract for `samples/phase-03/0307`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0307/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `SlopesRampsScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0307` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0307/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0307/index.html
+samples/phase-03/0307
+0307
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/21/2026
diff --git a/samples/phase-03/0308/README.md b/samples/phase-03/0308/README.md
index d2ecba5e..38220e5c 100644
--- a/samples/phase-03/0308/README.md
+++ b/samples/phase-03/0308/README.md
@@ -1,3 +1,66 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0308 - Gravity Zones
+
+Sample-local contract for `samples/phase-03/0308`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0308/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `GravityZonesScene.js`
+- `index.html`
+- `main.js`
+- `minimalJump.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0308` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0308/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0308/index.html
+samples/phase-03/0308
+0308
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/21/2026
diff --git a/samples/phase-03/0309/README.md b/samples/phase-03/0309/README.md
index 7e976df6..5b5e045d 100644
--- a/samples/phase-03/0309/README.md
+++ b/samples/phase-03/0309/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0309 - Ladders / Climb Zones
+
+Sample-local contract for `samples/phase-03/0309`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0309/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `LaddersClimbZonesScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0309` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0309/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0309/index.html
+samples/phase-03/0309
+0309
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/21/2026
diff --git a/samples/phase-03/0310/README.md b/samples/phase-03/0310/README.md
index a20c6c9b..af65ed94 100644
--- a/samples/phase-03/0310/README.md
+++ b/samples/phase-03/0310/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0310 - Moving Platforms
+
+Sample-local contract for `samples/phase-03/0310`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0310/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `MovingPlatformsScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0310` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0310/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0310/index.html
+samples/phase-03/0310
+0310
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/21/2026
diff --git a/samples/phase-03/0311/README.md b/samples/phase-03/0311/README.md
index 5a30475f..0de67cdb 100644
--- a/samples/phase-03/0311/README.md
+++ b/samples/phase-03/0311/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0311 - One-Way Platforms
+
+Sample-local contract for `samples/phase-03/0311`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0311/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `OneWayPlatformsScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0311` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0311/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0311/index.html
+samples/phase-03/0311
+0311
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/21/2026
diff --git a/samples/phase-03/0312/README.md b/samples/phase-03/0312/README.md
index 14654eb0..0eba93b6 100644
--- a/samples/phase-03/0312/README.md
+++ b/samples/phase-03/0312/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0312 - Friction Surfaces
+
+Sample-local contract for `samples/phase-03/0312`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0312/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `FrictionSurfacesScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0312` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0312/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0312/index.html
+samples/phase-03/0312
+0312
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/21/2026
diff --git a/samples/phase-03/0313/README.md b/samples/phase-03/0313/README.md
index aecf1f3b..44751a12 100644
--- a/samples/phase-03/0313/README.md
+++ b/samples/phase-03/0313/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0313 - Trigger Zones
+
+Sample-local contract for `samples/phase-03/0313`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0313/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `TriggerZonesScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0313` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0313/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0313/index.html
+samples/phase-03/0313
+0313
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/21/2026
diff --git a/samples/phase-03/0314/README.md b/samples/phase-03/0314/README.md
index 4ea0ef06..ab496525 100644
--- a/samples/phase-03/0314/README.md
+++ b/samples/phase-03/0314/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0314 - Pickups Collectibles
+
+Sample-local contract for `samples/phase-03/0314`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0314/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `PickupsCollectiblesScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0314` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0314/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0314/index.html
+samples/phase-03/0314
+0314
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/21/2026
diff --git a/samples/phase-03/0315/README.md b/samples/phase-03/0315/README.md
index 7879fe6c..7f6be6ad 100644
--- a/samples/phase-03/0315/README.md
+++ b/samples/phase-03/0315/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0315 - Switches Buttons
+
+Sample-local contract for `samples/phase-03/0315`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0315/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `SwitchesButtonsScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0315` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0315/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0315/index.html
+samples/phase-03/0315
+0315
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/21/2026
diff --git a/samples/phase-03/0316/README.md b/samples/phase-03/0316/README.md
index 227ea85a..452f4dfa 100644
--- a/samples/phase-03/0316/README.md
+++ b/samples/phase-03/0316/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0316 - Doors Gates
+
+Sample-local contract for `samples/phase-03/0316`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0316/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `DoorsGatesScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0316` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0316/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0316/index.html
+samples/phase-03/0316
+0316
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/21/2026
diff --git a/samples/phase-03/0317/README.md b/samples/phase-03/0317/README.md
index ff274885..e126f894 100644
--- a/samples/phase-03/0317/README.md
+++ b/samples/phase-03/0317/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0317 - Basic NPC Entity
+
+Sample-local contract for `samples/phase-03/0317`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0317/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `BasicNpcEntityScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0317` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0317/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0317/index.html
+samples/phase-03/0317
+0317
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/21/2026
diff --git a/samples/phase-03/0318/README.md b/samples/phase-03/0318/README.md
index d6edcdf5..e5b96033 100644
--- a/samples/phase-03/0318/README.md
+++ b/samples/phase-03/0318/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0318 - Interaction Press Key
+
+Sample-local contract for `samples/phase-03/0318`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0318/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `InteractionPressKeyScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0318` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0318/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0318/index.html
+samples/phase-03/0318
+0318
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/21/2026
diff --git a/samples/phase-03/0319/README.md b/samples/phase-03/0319/README.md
index 531812ab..ce2a5754 100644
--- a/samples/phase-03/0319/README.md
+++ b/samples/phase-03/0319/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0319 - Hitboxes vs Hurtboxes
+
+Sample-local contract for `samples/phase-03/0319`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0319/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `HitboxesHurtboxesScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0319` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0319/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0319/index.html
+samples/phase-03/0319
+0319
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/21/2026
diff --git a/samples/phase-03/0320/README.md b/samples/phase-03/0320/README.md
index 93447531..fbc87993 100644
--- a/samples/phase-03/0320/README.md
+++ b/samples/phase-03/0320/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0320 - Projectile System
+
+Sample-local contract for `samples/phase-03/0320`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0320/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `ProjectileSystemScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0320` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0320/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0320/index.html
+samples/phase-03/0320
+0320
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/21/2026
diff --git a/samples/phase-03/0321/README.md b/samples/phase-03/0321/README.md
index e76298c0..dcc95f13 100644
--- a/samples/phase-03/0321/README.md
+++ b/samples/phase-03/0321/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0321 - Health System
+
+Sample-local contract for `samples/phase-03/0321`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0321/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `HealthSystemScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0321` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0321/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0321/index.html
+samples/phase-03/0321
+0321
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/21/2026
diff --git a/samples/phase-03/0322/README.md b/samples/phase-03/0322/README.md
index f87c561e..5bb19a41 100644
--- a/samples/phase-03/0322/README.md
+++ b/samples/phase-03/0322/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0322 - Damage + Knockback
+
+Sample-local contract for `samples/phase-03/0322`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0322/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `DamageKnockbackScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0322` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0322/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0322/index.html
+samples/phase-03/0322
+0322
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/21/2026
diff --git a/samples/phase-03/0323/README.md b/samples/phase-03/0323/README.md
index 8428b9b5..ecaed5ef 100644
--- a/samples/phase-03/0323/README.md
+++ b/samples/phase-03/0323/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0323 - Invulnerability Frames
+
+Sample-local contract for `samples/phase-03/0323`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0323/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `InvulnerabilityFramesScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0323` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0323/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0323/index.html
+samples/phase-03/0323
+0323
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/21/2026
diff --git a/samples/phase-03/0324/README.md b/samples/phase-03/0324/README.md
index 235c25d3..f2cd6f6b 100644
--- a/samples/phase-03/0324/README.md
+++ b/samples/phase-03/0324/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0324 - Simple Enemy AI
+
+Sample-local contract for `samples/phase-03/0324`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0324/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `SimpleEnemyAIScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0324` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0324/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0324/index.html
+samples/phase-03/0324
+0324
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/21/2026
diff --git a/samples/phase-03/0325/README.md b/samples/phase-03/0325/README.md
new file mode 100644
index 00000000..93e54b99
--- /dev/null
+++ b/samples/phase-03/0325/README.md
@@ -0,0 +1,67 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Gravity
+
+Sample-local contract for `samples/phase-03/0325`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0325/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `flow/attract.js`
+- `flow/highscore.js`
+- `flow/intro.js`
+- `game/GravityAudio.js`
+- `game/GravityInputController.js`
+- `game/GravityScene.js`
+- `game/GravityWorld.js`
+- `index.html`
+- `main.js`
+- `rules/flowContent.js`
+- `rules/flowRules.js`
+- `rules/gameFlowRules.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0325` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0325/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0325/index.html
+samples/phase-03/0325
+0325
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-03/0326/README.md b/samples/phase-03/0326/README.md
new file mode 100644
index 00000000..ac7a48db
--- /dev/null
+++ b/samples/phase-03/0326/README.md
@@ -0,0 +1,67 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Paddle Intercept
+
+Sample-local contract for `samples/phase-03/0326`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0326/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `flow/attract.js`
+- `flow/highscore.js`
+- `flow/intro.js`
+- `game/PaddleInterceptAudio.js`
+- `game/PaddleInterceptInputController.js`
+- `game/PaddleInterceptScene.js`
+- `game/PaddleInterceptWorld.js`
+- `index.html`
+- `main.js`
+- `rules/flowContent.js`
+- `rules/flowRules.js`
+- `rules/gameFlowRules.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0326` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0326/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0326/index.html
+samples/phase-03/0326
+0326
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-03/0327/README.md b/samples/phase-03/0327/README.md
new file mode 100644
index 00000000..a6d9f04b
--- /dev/null
+++ b/samples/phase-03/0327/README.md
@@ -0,0 +1,67 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Multi-Ball Chaos
+
+Sample-local contract for `samples/phase-03/0327`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-03/0327/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `flow/attract.js`
+- `flow/highscore.js`
+- `flow/intro.js`
+- `game/MultiBallChaosAudio.js`
+- `game/MultiBallChaosInputController.js`
+- `game/MultiBallChaosScene.js`
+- `game/MultiBallChaosWorld.js`
+- `index.html`
+- `main.js`
+- `rules/flowContent.js`
+- `rules/flowRules.js`
+- `rules/gameFlowRules.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-03/0327` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-03` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-03/0327/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-03/0327/index.html
+samples/phase-03/0327
+0327
+```
+
+Phase folder:
+
+```text
+samples/phase-03
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-04/0401/README.md b/samples/phase-04/0401/README.md
index d7c9c61d..0335235d 100644
--- a/samples/phase-04/0401/README.md
+++ b/samples/phase-04/0401/README.md
@@ -1,3 +1,66 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0401- Camera Dead Zone
+
+Sample-local contract for `samples/phase-04/0401`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-04/0401/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `CameraDeadZoneScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-04/0401` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-04` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-04/0401/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-04/0401/index.html
+samples/phase-04/0401
+0401
+```
+
+Phase folder:
+
+```text
+samples/phase-04
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/21/2026
diff --git a/samples/phase-04/0402/README.md b/samples/phase-04/0402/README.md
index bdb4f21d..72e2d430 100644
--- a/samples/phase-04/0402/README.md
+++ b/samples/phase-04/0402/README.md
@@ -1,3 +1,66 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0402- Camera Look Ahead
+
+Sample-local contract for `samples/phase-04/0402`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-04/0402/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `CameraLookAheadScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-04/0402` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-04` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-04/0402/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-04/0402/index.html
+samples/phase-04/0402
+0402
+```
+
+Phase folder:
+
+```text
+samples/phase-04
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/21/2026
diff --git a/samples/phase-04/0403/README.md b/samples/phase-04/0403/README.md
index 5cd2468a..d6bad85c 100644
--- a/samples/phase-04/0403/README.md
+++ b/samples/phase-04/0403/README.md
@@ -1,3 +1,66 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0403- Camera Smoothing
+
+Sample-local contract for `samples/phase-04/0403`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-04/0403/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `CameraSmoothingScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-04/0403` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-04` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-04/0403/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-04/0403/index.html
+samples/phase-04/0403
+0403
+```
+
+Phase folder:
+
+```text
+samples/phase-04
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/21/2026
diff --git a/samples/phase-04/0404/README.md b/samples/phase-04/0404/README.md
index bc5d1370..47e9b639 100644
--- a/samples/phase-04/0404/README.md
+++ b/samples/phase-04/0404/README.md
@@ -1,3 +1,66 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0404- Camera Bounds
+
+Sample-local contract for `samples/phase-04/0404`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-04/0404/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `CameraBoundsScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-04/0404` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-04` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-04/0404/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-04/0404/index.html
+samples/phase-04/0404
+0404
+```
+
+Phase folder:
+
+```text
+samples/phase-04
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/21/2026
diff --git a/samples/phase-04/0405/README.md b/samples/phase-04/0405/README.md
index 27079bfb..188a4fa1 100644
--- a/samples/phase-04/0405/README.md
+++ b/samples/phase-04/0405/README.md
@@ -1,3 +1,66 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0405- Camera Shake
+
+Sample-local contract for `samples/phase-04/0405`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-04/0405/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `CameraShakeScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-04/0405` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-04` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-04/0405/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-04/0405/index.html
+samples/phase-04/0405
+0405
+```
+
+Phase folder:
+
+```text
+samples/phase-04
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/21/2026
diff --git a/samples/phase-04/0406/README.md b/samples/phase-04/0406/README.md
index 3e9c103a..e93fbf2d 100644
--- a/samples/phase-04/0406/README.md
+++ b/samples/phase-04/0406/README.md
@@ -1,3 +1,66 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0406 - Camera Zoom
+
+Sample-local contract for `samples/phase-04/0406`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-04/0406/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `CameraZoomScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-04/0406` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-04` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-04/0406/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-04/0406/index.html
+samples/phase-04/0406
+0406
+```
+
+Phase folder:
+
+```text
+samples/phase-04
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/21/2026
diff --git a/samples/phase-04/0407/README.md b/samples/phase-04/0407/README.md
index 92b6167c..bc2aa044 100644
--- a/samples/phase-04/0407/README.md
+++ b/samples/phase-04/0407/README.md
@@ -1,3 +1,66 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0407 - Input Buffering
+
+Sample-local contract for `samples/phase-04/0407`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-04/0407/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `InputBufferingScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-04/0407` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-04` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-04/0407/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-04/0407/index.html
+samples/phase-04/0407
+0407
+```
+
+Phase folder:
+
+```text
+samples/phase-04
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/22/2026
diff --git a/samples/phase-04/0408/README.md b/samples/phase-04/0408/README.md
index ec854d4b..095f257f 100644
--- a/samples/phase-04/0408/README.md
+++ b/samples/phase-04/0408/README.md
@@ -1,3 +1,66 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0408 - Input Queue + Priority
+
+Sample-local contract for `samples/phase-04/0408`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-04/0408/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `InputQueuePriorityScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-04/0408` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-04` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-04/0408/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-04/0408/index.html
+samples/phase-04/0408
+0408
+```
+
+Phase folder:
+
+```text
+samples/phase-04
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/22/2026
diff --git a/samples/phase-04/0409/README.md b/samples/phase-04/0409/README.md
index b3ad02b6..5de29972 100644
--- a/samples/phase-04/0409/README.md
+++ b/samples/phase-04/0409/README.md
@@ -1,3 +1,66 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0409 - Input Timing Windows
+
+Sample-local contract for `samples/phase-04/0409`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-04/0409/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `InputTimingWindowsScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-04/0409` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-04` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-04/0409/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-04/0409/index.html
+samples/phase-04/0409
+0409
+```
+
+Phase folder:
+
+```text
+samples/phase-04
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/22/2026
diff --git a/samples/phase-04/0410/README.md b/samples/phase-04/0410/README.md
index a50fe99e..483341cb 100644
--- a/samples/phase-04/0410/README.md
+++ b/samples/phase-04/0410/README.md
@@ -1,3 +1,66 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0410 - Action Cooldowns
+
+Sample-local contract for `samples/phase-04/0410`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-04/0410/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `ActionCooldownsScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-04/0410` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-04` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-04/0410/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-04/0410/index.html
+samples/phase-04/0410
+0410
+```
+
+Phase folder:
+
+```text
+samples/phase-04
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/22/2026
diff --git a/samples/phase-04/0411/README.md b/samples/phase-04/0411/README.md
index 120a9392..3a83b678 100644
--- a/samples/phase-04/0411/README.md
+++ b/samples/phase-04/0411/README.md
@@ -1,3 +1,66 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0411 - Input Chaining
+
+Sample-local contract for `samples/phase-04/0411`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-04/0411/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `InputChainingScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-04/0411` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-04` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-04/0411/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-04/0411/index.html
+samples/phase-04/0411
+0411
+```
+
+Phase folder:
+
+```text
+samples/phase-04
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/22/2026
diff --git a/samples/phase-04/0412/README.md b/samples/phase-04/0412/README.md
index 724eb1d5..6014a7fd 100644
--- a/samples/phase-04/0412/README.md
+++ b/samples/phase-04/0412/README.md
@@ -1,3 +1,66 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0412 - Input State Debug Overlay
+
+Sample-local contract for `samples/phase-04/0412`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-04/0412/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `InputStateDebugOverlayScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-04/0412` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-04` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-04/0412/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-04/0412/index.html
+samples/phase-04/0412
+0412
+```
+
+Phase folder:
+
+```text
+samples/phase-04
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 Toolbox Aid
 David Quesenberry
 03/22/2026
diff --git a/samples/phase-04/0413/README.md b/samples/phase-04/0413/README.md
new file mode 100644
index 00000000..1602ce17
--- /dev/null
+++ b/samples/phase-04/0413/README.md
@@ -0,0 +1,67 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Thruster
+
+Sample-local contract for `samples/phase-04/0413`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-04/0413/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `flow/attract.js`
+- `flow/highscore.js`
+- `flow/intro.js`
+- `game/ThrusterAudio.js`
+- `game/ThrusterInputController.js`
+- `game/ThrusterScene.js`
+- `game/ThrusterWorld.js`
+- `index.html`
+- `main.js`
+- `rules/flowContent.js`
+- `rules/flowRules.js`
+- `rules/gameFlowRules.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-04/0413` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-04` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-04/0413/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-04/0413/index.html
+samples/phase-04/0413
+0413
+```
+
+Phase folder:
+
+```text
+samples/phase-04
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-05/0501/README.md b/samples/phase-05/0501/README.md
new file mode 100644
index 00000000..b4964fff
--- /dev/null
+++ b/samples/phase-05/0501/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0501 - Hitboxes + Hurtboxes
+
+Sample-local contract for `samples/phase-05/0501`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-05/0501/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `CombatHitboxesScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-05/0501` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-05` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-05/0501/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-05/0501/index.html
+samples/phase-05/0501
+0501
+```
+
+Phase folder:
+
+```text
+samples/phase-05
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-05/0502/README.md b/samples/phase-05/0502/README.md
new file mode 100644
index 00000000..129291ce
--- /dev/null
+++ b/samples/phase-05/0502/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0502 - Attack Timing Windows
+
+Sample-local contract for `samples/phase-05/0502`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-05/0502/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `AttackTimingWindowsScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-05/0502` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-05` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-05/0502/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-05/0502/index.html
+samples/phase-05/0502
+0502
+```
+
+Phase folder:
+
+```text
+samples/phase-05
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-05/0503/README.md b/samples/phase-05/0503/README.md
new file mode 100644
index 00000000..6bcec265
--- /dev/null
+++ b/samples/phase-05/0503/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0503 - Damage + Invulnerability Frames
+
+Sample-local contract for `samples/phase-05/0503`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-05/0503/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `DamageInvulnerabilityScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-05/0503` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-05` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-05/0503/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-05/0503/index.html
+samples/phase-05/0503
+0503
+```
+
+Phase folder:
+
+```text
+samples/phase-05
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-05/0504/README.md b/samples/phase-05/0504/README.md
new file mode 100644
index 00000000..9b0d245e
--- /dev/null
+++ b/samples/phase-05/0504/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0504 - Knockback Response
+
+Sample-local contract for `samples/phase-05/0504`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-05/0504/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `KnockbackResponseScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-05/0504` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-05` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-05/0504/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-05/0504/index.html
+samples/phase-05/0504
+0504
+```
+
+Phase folder:
+
+```text
+samples/phase-05
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-05/0505/README.md b/samples/phase-05/0505/README.md
new file mode 100644
index 00000000..e09d7eba
--- /dev/null
+++ b/samples/phase-05/0505/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0505 - Health + Death State
+
+Sample-local contract for `samples/phase-05/0505`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-05/0505/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `HealthDeathStateScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-05/0505` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-05` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-05/0505/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-05/0505/index.html
+samples/phase-05/0505
+0505
+```
+
+Phase folder:
+
+```text
+samples/phase-05
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-05/0506/README.md b/samples/phase-05/0506/README.md
new file mode 100644
index 00000000..1cb67377
--- /dev/null
+++ b/samples/phase-05/0506/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0506 - Combat Debug Overlay
+
+Sample-local contract for `samples/phase-05/0506`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-05/0506/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `CombatDebugOverlayScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-05/0506` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-05` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-05/0506/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-05/0506/index.html
+samples/phase-05/0506
+0506
+```
+
+Phase folder:
+
+```text
+samples/phase-05
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-05/0507/README.md b/samples/phase-05/0507/README.md
new file mode 100644
index 00000000..b77cce10
--- /dev/null
+++ b/samples/phase-05/0507/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0507 - Event Bus
+
+Sample-local contract for `samples/phase-05/0507`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-05/0507/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `EventBusScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-05/0507` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-05` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-05/0507/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-05/0507/index.html
+samples/phase-05/0507
+0507
+```
+
+Phase folder:
+
+```text
+samples/phase-05
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-05/0508/README.md b/samples/phase-05/0508/README.md
new file mode 100644
index 00000000..3ad62bd2
--- /dev/null
+++ b/samples/phase-05/0508/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0508 - State Machine Framework
+
+Sample-local contract for `samples/phase-05/0508`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-05/0508/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `StateMachineFrameworkScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-05/0508` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-05` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-05/0508/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-05/0508/index.html
+samples/phase-05/0508
+0508
+```
+
+Phase folder:
+
+```text
+samples/phase-05
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-05/0509/README.md b/samples/phase-05/0509/README.md
new file mode 100644
index 00000000..032f482c
--- /dev/null
+++ b/samples/phase-05/0509/README.md
@@ -0,0 +1,61 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0509 - Config System
+
+Sample-local contract for `samples/phase-05/0509`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-05/0509/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `ConfigSystemScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+- `sampleConfig.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-05/0509` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-05` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-05/0509/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-05/0509/index.html
+samples/phase-05/0509
+0509
+```
+
+Phase folder:
+
+```text
+samples/phase-05
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-05/0510/README.md b/samples/phase-05/0510/README.md
new file mode 100644
index 00000000..98234333
--- /dev/null
+++ b/samples/phase-05/0510/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0510 - Asset Loader System
+
+Sample-local contract for `samples/phase-05/0510`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-05/0510/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `AssetLoaderSystemScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-05/0510` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-05` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-05/0510/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-05/0510/index.html
+samples/phase-05/0510
+0510
+```
+
+Phase folder:
+
+```text
+samples/phase-05
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-05/0511/README.md b/samples/phase-05/0511/README.md
new file mode 100644
index 00000000..e6006373
--- /dev/null
+++ b/samples/phase-05/0511/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0511 - Debug Tools
+
+Sample-local contract for `samples/phase-05/0511`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-05/0511/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `DebugToolsScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-05/0511` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-05` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-05/0511/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-05/0511/index.html
+samples/phase-05/0511
+0511
+```
+
+Phase folder:
+
+```text
+samples/phase-05
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-05/0512/README.md b/samples/phase-05/0512/README.md
new file mode 100644
index 00000000..01570545
--- /dev/null
+++ b/samples/phase-05/0512/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0512 - Performance Metrics
+
+Sample-local contract for `samples/phase-05/0512`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-05/0512/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `PerformanceMetricsScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-05/0512` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-05` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-05/0512/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-05/0512/index.html
+samples/phase-05/0512
+0512
+```
+
+Phase folder:
+
+```text
+samples/phase-05
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-06/0601/README.md b/samples/phase-06/0601/README.md
new file mode 100644
index 00000000..58369cbb
--- /dev/null
+++ b/samples/phase-06/0601/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0601 - Grid Pathfinding
+
+Sample-local contract for `samples/phase-06/0601`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-06/0601/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `GridPathfindingScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-06/0601` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-06` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-06/0601/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-06/0601/index.html
+samples/phase-06/0601
+0601
+```
+
+Phase folder:
+
+```text
+samples/phase-06
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-06/0602/README.md b/samples/phase-06/0602/README.md
new file mode 100644
index 00000000..939d2a80
--- /dev/null
+++ b/samples/phase-06/0602/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0602 - Patrol AI
+
+Sample-local contract for `samples/phase-06/0602`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-06/0602/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `PatrolAIScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-06/0602` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-06` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-06/0602/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-06/0602/index.html
+samples/phase-06/0602
+0602
+```
+
+Phase folder:
+
+```text
+samples/phase-06
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-06/0603/README.md b/samples/phase-06/0603/README.md
new file mode 100644
index 00000000..b39acd0e
--- /dev/null
+++ b/samples/phase-06/0603/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0603 - Chase / Evade AI
+
+Sample-local contract for `samples/phase-06/0603`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-06/0603/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `ChaseEvadeAIScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-06/0603` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-06` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-06/0603/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-06/0603/index.html
+samples/phase-06/0603
+0603
+```
+
+Phase folder:
+
+```text
+samples/phase-06
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-06/0604/README.md b/samples/phase-06/0604/README.md
new file mode 100644
index 00000000..8293b416
--- /dev/null
+++ b/samples/phase-06/0604/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0604 - State-Driven AI
+
+Sample-local contract for `samples/phase-06/0604`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-06/0604/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `StateDrivenAIScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-06/0604` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-06` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-06/0604/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-06/0604/index.html
+samples/phase-06/0604
+0604
+```
+
+Phase folder:
+
+```text
+samples/phase-06
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-06/0605/README.md b/samples/phase-06/0605/README.md
new file mode 100644
index 00000000..1b504061
--- /dev/null
+++ b/samples/phase-06/0605/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0605 - Group Behaviors
+
+Sample-local contract for `samples/phase-06/0605`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-06/0605/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `GroupBehaviorsScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-06/0605` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-06` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-06/0605/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-06/0605/index.html
+samples/phase-06/0605
+0605
+```
+
+Phase folder:
+
+```text
+samples/phase-06
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-06/0606/README.md b/samples/phase-06/0606/README.md
new file mode 100644
index 00000000..ddf1fa7e
--- /dev/null
+++ b/samples/phase-06/0606/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0606 - Quest System
+
+Sample-local contract for `samples/phase-06/0606`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-06/0606/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `QuestSystemScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-06/0606` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-06` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-06/0606/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-06/0606/index.html
+samples/phase-06/0606
+0606
+```
+
+Phase folder:
+
+```text
+samples/phase-06
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-06/0607/README.md b/samples/phase-06/0607/README.md
new file mode 100644
index 00000000..ec3eae15
--- /dev/null
+++ b/samples/phase-06/0607/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0607 - Scripting System
+
+Sample-local contract for `samples/phase-06/0607`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-06/0607/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `ScriptingSystemScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-06/0607` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-06` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-06/0607/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-06/0607/index.html
+samples/phase-06/0607
+0607
+```
+
+Phase folder:
+
+```text
+samples/phase-06
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-06/0608/README.md b/samples/phase-06/0608/README.md
new file mode 100644
index 00000000..74f42009
--- /dev/null
+++ b/samples/phase-06/0608/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0608 - Cutscene System
+
+Sample-local contract for `samples/phase-06/0608`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-06/0608/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `CutsceneSystemScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-06/0608` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-06` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-06/0608/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-06/0608/index.html
+samples/phase-06/0608
+0608
+```
+
+Phase folder:
+
+```text
+samples/phase-06
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-06/0609/README.md b/samples/phase-06/0609/README.md
new file mode 100644
index 00000000..d03c18fc
--- /dev/null
+++ b/samples/phase-06/0609/README.md
@@ -0,0 +1,64 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0609 - Spawn System
+
+Sample-local contract for `samples/phase-06/0609`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-06/0609/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `SampleLifecycleSystem.js`
+- `SampleSpawnSystem.js`
+- `SampleWorldEventsSystem.js`
+- `SampleWorldStateSystem.js`
+- `SpawnSystemScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-06/0609` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-06` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-06/0609/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-06/0609/index.html
+samples/phase-06/0609
+0609
+```
+
+Phase folder:
+
+```text
+samples/phase-06
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-06/0610/README.md b/samples/phase-06/0610/README.md
new file mode 100644
index 00000000..93b53d37
--- /dev/null
+++ b/samples/phase-06/0610/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0610 - Day / Night Cycle
+
+Sample-local contract for `samples/phase-06/0610`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-06/0610/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `DayNightCycleScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-06/0610` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-06` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-06/0610/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-06/0610/index.html
+samples/phase-06/0610
+0610
+```
+
+Phase folder:
+
+```text
+samples/phase-06
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-06/0611/README.md b/samples/phase-06/0611/README.md
new file mode 100644
index 00000000..0f3181d2
--- /dev/null
+++ b/samples/phase-06/0611/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0612 - Animated Weather
+
+Sample-local contract for `samples/phase-06/0611`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-06/0611/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `AnimatedWeatherScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-06/0611` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-06` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-06/0611/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-06/0611/index.html
+samples/phase-06/0611
+0611
+```
+
+Phase folder:
+
+```text
+samples/phase-06
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-06/0612/README.md b/samples/phase-06/0612/README.md
new file mode 100644
index 00000000..7d3624ff
--- /dev/null
+++ b/samples/phase-06/0612/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0611 - Weather System
+
+Sample-local contract for `samples/phase-06/0612`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-06/0612/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `WeatherSystemScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-06/0612` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-06` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-06/0612/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-06/0612/index.html
+samples/phase-06/0612
+0612
+```
+
+Phase folder:
+
+```text
+samples/phase-06
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-06/0613/README.md b/samples/phase-06/0613/README.md
new file mode 100644
index 00000000..736ead53
--- /dev/null
+++ b/samples/phase-06/0613/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0613 - World Streaming
+
+Sample-local contract for `samples/phase-06/0613`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-06/0613/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `WorldStreamingScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-06/0613` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-06` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-06/0613/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-06/0613/index.html
+samples/phase-06/0613
+0613
+```
+
+Phase folder:
+
+```text
+samples/phase-06
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-06/0614/README.md b/samples/phase-06/0614/README.md
new file mode 100644
index 00000000..d3afe37e
--- /dev/null
+++ b/samples/phase-06/0614/README.md
@@ -0,0 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Orbit
+
+Sample-local contract for `samples/phase-06/0614`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-06/0614/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `flow/attract.js`
+- `flow/highscore.js`
+- `flow/intro.js`
+- `game/OrbitLabModel.js`
+- `game/OrbitLabScene.js`
+- `index.html`
+- `main.js`
+- `rules/flowContent.js`
+- `rules/flowRules.js`
+- `rules/gameFlowRules.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-06/0614` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-06` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-06/0614/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-06/0614/index.html
+samples/phase-06/0614
+0614
+```
+
+Phase folder:
+
+```text
+samples/phase-06
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-07/0701/README.md b/samples/phase-07/0701/README.md
new file mode 100644
index 00000000..ec7a6128
--- /dev/null
+++ b/samples/phase-07/0701/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0701 - Input Remapping
+
+Sample-local contract for `samples/phase-07/0701`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-07/0701/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `InputRemappingScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-07/0701` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-07` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-07/0701/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-07/0701/index.html
+samples/phase-07/0701
+0701
+```
+
+Phase folder:
+
+```text
+samples/phase-07
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-07/0702/README.md b/samples/phase-07/0702/README.md
new file mode 100644
index 00000000..4f271fba
--- /dev/null
+++ b/samples/phase-07/0702/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0702 - Controller Support
+
+Sample-local contract for `samples/phase-07/0702`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-07/0702/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `ControllerSupportScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-07/0702` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-07` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-07/0702/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-07/0702/index.html
+samples/phase-07/0702
+0702
+```
+
+Phase folder:
+
+```text
+samples/phase-07
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-07/0703/README.md b/samples/phase-07/0703/README.md
new file mode 100644
index 00000000..49332b53
--- /dev/null
+++ b/samples/phase-07/0703/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0703 - Resolution Scaling
+
+Sample-local contract for `samples/phase-07/0703`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-07/0703/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `ResolutionScalingScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-07/0703` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-07` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-07/0703/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-07/0703/index.html
+samples/phase-07/0703
+0703
+```
+
+Phase folder:
+
+```text
+samples/phase-07
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-07/0704/README.md b/samples/phase-07/0704/README.md
new file mode 100644
index 00000000..a517f080
--- /dev/null
+++ b/samples/phase-07/0704/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0704 - Mobile Support Tweaks
+
+Sample-local contract for `samples/phase-07/0704`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-07/0704/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `MobileSupportScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-07/0704` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-07` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-07/0704/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-07/0704/index.html
+samples/phase-07/0704
+0704
+```
+
+Phase folder:
+
+```text
+samples/phase-07
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-07/0705/README.md b/samples/phase-07/0705/README.md
new file mode 100644
index 00000000..517c087a
--- /dev/null
+++ b/samples/phase-07/0705/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0705 - Asset Optimization
+
+Sample-local contract for `samples/phase-07/0705`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-07/0705/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `AssetOptimizationScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-07/0705` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-07` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-07/0705/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-07/0705/index.html
+samples/phase-07/0705
+0705
+```
+
+Phase folder:
+
+```text
+samples/phase-07
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-07/0706/README.md b/samples/phase-07/0706/README.md
new file mode 100644
index 00000000..25fcd5a6
--- /dev/null
+++ b/samples/phase-07/0706/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0706 - Memory Management
+
+Sample-local contract for `samples/phase-07/0706`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-07/0706/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `MemoryManagementScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-07/0706` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-07` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-07/0706/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-07/0706/index.html
+samples/phase-07/0706
+0706
+```
+
+Phase folder:
+
+```text
+samples/phase-07
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-07/0707/README.md b/samples/phase-07/0707/README.md
new file mode 100644
index 00000000..b77a159f
--- /dev/null
+++ b/samples/phase-07/0707/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0707 - Save Compression
+
+Sample-local contract for `samples/phase-07/0707`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-07/0707/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `SaveCompressionScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-07/0707` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-07` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-07/0707/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-07/0707/index.html
+samples/phase-07/0707
+0707
+```
+
+Phase folder:
+
+```text
+samples/phase-07
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-07/0708/README.md b/samples/phase-07/0708/README.md
new file mode 100644
index 00000000..87a96ef1
--- /dev/null
+++ b/samples/phase-07/0708/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0708 - Replay System
+
+Sample-local contract for `samples/phase-07/0708`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-07/0708/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `ReplaySystemScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-07/0708` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-07` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-07/0708/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-07/0708/index.html
+samples/phase-07/0708
+0708
+```
+
+Phase folder:
+
+```text
+samples/phase-07
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-07/0709/README.md b/samples/phase-07/0709/README.md
new file mode 100644
index 00000000..c01755b7
--- /dev/null
+++ b/samples/phase-07/0709/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0709 - Mini-Map System
+
+Sample-local contract for `samples/phase-07/0709`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-07/0709/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `MiniMapSystemScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-07/0709` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-07` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-07/0709/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-07/0709/index.html
+samples/phase-07/0709
+0709
+```
+
+Phase folder:
+
+```text
+samples/phase-07
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-07/0710/README.md b/samples/phase-07/0710/README.md
new file mode 100644
index 00000000..7cf375ec
--- /dev/null
+++ b/samples/phase-07/0710/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0710 - Achievements System
+
+Sample-local contract for `samples/phase-07/0710`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-07/0710/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `AchievementsSystemScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-07/0710` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-07` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-07/0710/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-07/0710/index.html
+samples/phase-07/0710
+0710
+```
+
+Phase folder:
+
+```text
+samples/phase-07
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-07/0711/README.md b/samples/phase-07/0711/README.md
new file mode 100644
index 00000000..60f0dc4b
--- /dev/null
+++ b/samples/phase-07/0711/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0711 - Localization System
+
+Sample-local contract for `samples/phase-07/0711`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-07/0711/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `LocalizationSystemScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-07/0711` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-07` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-07/0711/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-07/0711/index.html
+samples/phase-07/0711
+0711
+```
+
+Phase folder:
+
+```text
+samples/phase-07
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-07/0712/README.md b/samples/phase-07/0712/README.md
new file mode 100644
index 00000000..e5b64d52
--- /dev/null
+++ b/samples/phase-07/0712/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0712 - Packaging / Build System
+
+Sample-local contract for `samples/phase-07/0712`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-07/0712/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `PackagingBuildSystemScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-07/0712` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-07` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-07/0712/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-07/0712/index.html
+samples/phase-07/0712
+0712
+```
+
+Phase folder:
+
+```text
+samples/phase-07
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-07/0713/README.md b/samples/phase-07/0713/README.md
new file mode 100644
index 00000000..7b1d88fa
--- /dev/null
+++ b/samples/phase-07/0713/README.md
@@ -0,0 +1,61 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0713 - Fullscreen Ability
+
+Sample-local contract for `samples/phase-07/0713`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-07/0713/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `FullscreenAbilityScene.js`
+- `fullscreenViewportFit.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-07/0713` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-07` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-07/0713/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-07/0713/index.html
+samples/phase-07/0713
+0713
+```
+
+Phase folder:
+
+```text
+samples/phase-07
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-08/0801/README.md b/samples/phase-08/0801/README.md
new file mode 100644
index 00000000..f65dee91
--- /dev/null
+++ b/samples/phase-08/0801/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0801 - Audio System
+
+Sample-local contract for `samples/phase-08/0801`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-08/0801/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `AudioSystemScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-08/0801` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-08` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-08/0801/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-08/0801/index.html
+samples/phase-08/0801
+0801
+```
+
+Phase folder:
+
+```text
+samples/phase-08
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-08/0802/README.md b/samples/phase-08/0802/README.md
new file mode 100644
index 00000000..1aa9c08b
--- /dev/null
+++ b/samples/phase-08/0802/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0802 - MIDI Player
+
+Sample-local contract for `samples/phase-08/0802`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-08/0802/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `MidiPlayerScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-08/0802` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-08` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-08/0802/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-08/0802/index.html
+samples/phase-08/0802
+0802
+```
+
+Phase folder:
+
+```text
+samples/phase-08
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-08/0803/README.md b/samples/phase-08/0803/README.md
new file mode 100644
index 00000000..f91a7a6d
--- /dev/null
+++ b/samples/phase-08/0803/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0803 - Synthesizer
+
+Sample-local contract for `samples/phase-08/0803`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-08/0803/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `SynthesizerScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-08/0803` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-08` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-08/0803/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-08/0803/index.html
+samples/phase-08/0803
+0803
+```
+
+Phase folder:
+
+```text
+samples/phase-08
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-08/0804/README.md b/samples/phase-08/0804/README.md
new file mode 100644
index 00000000..3b3e76f7
--- /dev/null
+++ b/samples/phase-08/0804/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0804 - Frequency Player
+
+Sample-local contract for `samples/phase-08/0804`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-08/0804/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `FrequencyPlayerScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-08/0804` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-08` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-08/0804/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-08/0804/index.html
+samples/phase-08/0804
+0804
+```
+
+Phase folder:
+
+```text
+samples/phase-08
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-08/0805/README.md b/samples/phase-08/0805/README.md
new file mode 100644
index 00000000..7915d7d6
--- /dev/null
+++ b/samples/phase-08/0805/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0805 - Explosion / Particle FX
+
+Sample-local contract for `samples/phase-08/0805`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-08/0805/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `ParticleFxScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-08/0805` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-08` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-08/0805/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-08/0805/index.html
+samples/phase-08/0805
+0805
+```
+
+Phase folder:
+
+```text
+samples/phase-08
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-08/0806/README.md b/samples/phase-08/0806/README.md
new file mode 100644
index 00000000..95bc6354
--- /dev/null
+++ b/samples/phase-08/0806/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0806 - UI Framework
+
+Sample-local contract for `samples/phase-08/0806`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-08/0806/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `UIFrameworkScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-08/0806` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-08` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-08/0806/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-08/0806/index.html
+samples/phase-08/0806
+0806
+```
+
+Phase folder:
+
+```text
+samples/phase-08
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-08/0807/README.md b/samples/phase-08/0807/README.md
new file mode 100644
index 00000000..abd5c5e2
--- /dev/null
+++ b/samples/phase-08/0807/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0807 - Input Context System
+
+Sample-local contract for `samples/phase-08/0807`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-08/0807/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `InputContextSystemScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-08/0807` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-08` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-08/0807/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-08/0807/index.html
+samples/phase-08/0807
+0807
+```
+
+Phase folder:
+
+```text
+samples/phase-08
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-08/0808/README.md b/samples/phase-08/0808/README.md
new file mode 100644
index 00000000..e08c0de2
--- /dev/null
+++ b/samples/phase-08/0808/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0808 - Scene Transitions
+
+Sample-local contract for `samples/phase-08/0808`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-08/0808/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `TransitionProofScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-08/0808` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-08` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-08/0808/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-08/0808/index.html
+samples/phase-08/0808
+0808
+```
+
+Phase folder:
+
+```text
+samples/phase-08
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-08/0809/README.md b/samples/phase-08/0809/README.md
new file mode 100644
index 00000000..20d51b45
--- /dev/null
+++ b/samples/phase-08/0809/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0809 - Save Slots / Profiles
+
+Sample-local contract for `samples/phase-08/0809`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-08/0809/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `SaveSlotsProfilesScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-08/0809` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-08` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-08/0809/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-08/0809/index.html
+samples/phase-08/0809
+0809
+```
+
+Phase folder:
+
+```text
+samples/phase-08
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-08/0810/README.md b/samples/phase-08/0810/README.md
new file mode 100644
index 00000000..4152e45c
--- /dev/null
+++ b/samples/phase-08/0810/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0810 - Logging + Error System
+
+Sample-local contract for `samples/phase-08/0810`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-08/0810/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `LoggingErrorSystemScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-08/0810` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-08` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-08/0810/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-08/0810/index.html
+samples/phase-08/0810
+0810
+```
+
+Phase folder:
+
+```text
+samples/phase-08
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-09/0901/README.md b/samples/phase-09/0901/README.md
new file mode 100644
index 00000000..bd2b3b62
--- /dev/null
+++ b/samples/phase-09/0901/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0901 - Vector Rendering System
+
+Sample-local contract for `samples/phase-09/0901`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-09/0901/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `VectorRenderingSystemScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-09/0901` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-09` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-09/0901/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-09/0901/index.html
+samples/phase-09/0901
+0901
+```
+
+Phase folder:
+
+```text
+samples/phase-09
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-09/0902/README.md b/samples/phase-09/0902/README.md
new file mode 100644
index 00000000..9298a022
--- /dev/null
+++ b/samples/phase-09/0902/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0902 - Polygon Collision
+
+Sample-local contract for `samples/phase-09/0902`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-09/0902/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `PolygonCollisionScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-09/0902` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-09` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-09/0902/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-09/0902/index.html
+samples/phase-09/0902
+0902
+```
+
+Phase folder:
+
+```text
+samples/phase-09
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-09/0903/README.md b/samples/phase-09/0903/README.md
new file mode 100644
index 00000000..e82dd009
--- /dev/null
+++ b/samples/phase-09/0903/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0903 - Point-In-Polygon / Inside-Shape Collision
+
+Sample-local contract for `samples/phase-09/0903`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-09/0903/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `PointInPolygonScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-09/0903` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-09` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-09/0903/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-09/0903/index.html
+samples/phase-09/0903
+0903
+```
+
+Phase folder:
+
+```text
+samples/phase-09
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-09/0904/README.md b/samples/phase-09/0904/README.md
new file mode 100644
index 00000000..84a215de
--- /dev/null
+++ b/samples/phase-09/0904/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0904 - Raster Mask Collision
+
+Sample-local contract for `samples/phase-09/0904`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-09/0904/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `RasterMaskCollisionScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-09/0904` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-09` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-09/0904/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-09/0904/index.html
+samples/phase-09/0904
+0904
+```
+
+Phase folder:
+
+```text
+samples/phase-09
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-09/0905/README.md b/samples/phase-09/0905/README.md
new file mode 100644
index 00000000..ab781232
--- /dev/null
+++ b/samples/phase-09/0905/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0905 - Pixel-Perfect Collision
+
+Sample-local contract for `samples/phase-09/0905`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-09/0905/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `PixelPerfectCollisionScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-09/0905` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-09` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-09/0905/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-09/0905/index.html
+samples/phase-09/0905
+0905
+```
+
+Phase folder:
+
+```text
+samples/phase-09
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-09/0906/README.md b/samples/phase-09/0906/README.md
new file mode 100644
index 00000000..d94708cb
--- /dev/null
+++ b/samples/phase-09/0906/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 0906 - Hybrid Collision (Bounds -> Shape -> Pixel)
+
+Sample-local contract for `samples/phase-09/0906`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-09/0906/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `HybridCollisionScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-09/0906` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-09` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-09/0906/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-09/0906/index.html
+samples/phase-09/0906
+0906
+```
+
+Phase folder:
+
+```text
+samples/phase-09
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-10/1001/README.md b/samples/phase-10/1001/README.md
new file mode 100644
index 00000000..6c885ced
--- /dev/null
+++ b/samples/phase-10/1001/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 1001 - MP3 Player
+
+Sample-local contract for `samples/phase-10/1001`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-10/1001/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `MP3PlayerScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-10/1001` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-10` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-10/1001/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-10/1001/index.html
+samples/phase-10/1001
+1001
+```
+
+Phase folder:
+
+```text
+samples/phase-10
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-10/1002/README.md b/samples/phase-10/1002/README.md
new file mode 100644
index 00000000..f5b7094e
--- /dev/null
+++ b/samples/phase-10/1002/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 1002 - Cookie Write / Read System
+
+Sample-local contract for `samples/phase-10/1002`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-10/1002/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `CookieWriteReadScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-10/1002` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-10` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-10/1002/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-10/1002/index.html
+samples/phase-10/1002
+1002
+```
+
+Phase folder:
+
+```text
+samples/phase-10
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-10/1003/README.md b/samples/phase-10/1003/README.md
new file mode 100644
index 00000000..18fd3f5f
--- /dev/null
+++ b/samples/phase-10/1003/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 1003 - Audio Playlist / Track Management
+
+Sample-local contract for `samples/phase-10/1003`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-10/1003/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `AudioPlaylistTrackManagementScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-10/1003` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-10` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-10/1003/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-10/1003/index.html
+samples/phase-10/1003
+1003
+```
+
+Phase folder:
+
+```text
+samples/phase-10
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-11/1101/README.md b/samples/phase-11/1101/README.md
new file mode 100644
index 00000000..d2679f14
--- /dev/null
+++ b/samples/phase-11/1101/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 1101 - Settings System
+
+Sample-local contract for `samples/phase-11/1101`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-11/1101/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `SettingsSystemScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-11/1101` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-11` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-11/1101/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-11/1101/index.html
+samples/phase-11/1101
+1101
+```
+
+Phase folder:
+
+```text
+samples/phase-11
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-11/1102/README.md b/samples/phase-11/1102/README.md
new file mode 100644
index 00000000..cbc6d0ca
--- /dev/null
+++ b/samples/phase-11/1102/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 1102 - Accessibility Options
+
+Sample-local contract for `samples/phase-11/1102`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-11/1102/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `AccessibilityOptionsScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-11/1102` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-11` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-11/1102/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-11/1102/index.html
+samples/phase-11/1102
+1102
+```
+
+Phase folder:
+
+```text
+samples/phase-11
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-11/1103/README.md b/samples/phase-11/1103/README.md
new file mode 100644
index 00000000..d36cd814
--- /dev/null
+++ b/samples/phase-11/1103/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 1103 - Deployment Profiles
+
+Sample-local contract for `samples/phase-11/1103`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-11/1103/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `DeploymentProfilesScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-11/1103` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-11` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-11/1103/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-11/1103/index.html
+samples/phase-11/1103
+1103
+```
+
+Phase folder:
+
+```text
+samples/phase-11
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-11/1104/README.md b/samples/phase-11/1104/README.md
new file mode 100644
index 00000000..8d3fb6d0
--- /dev/null
+++ b/samples/phase-11/1104/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 1104 - Distribution Packaging
+
+Sample-local contract for `samples/phase-11/1104`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-11/1104/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `DistributionPackagingScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-11/1104` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-11` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-11/1104/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-11/1104/index.html
+samples/phase-11/1104
+1104
+```
+
+Phase folder:
+
+```text
+samples/phase-11
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-11/1105/README.md b/samples/phase-11/1105/README.md
new file mode 100644
index 00000000..8072db94
--- /dev/null
+++ b/samples/phase-11/1105/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 1105 - Crash Recovery / Fallback Flow
+
+Sample-local contract for `samples/phase-11/1105`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-11/1105/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `CrashRecoveryScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-11/1105` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-11` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-11/1105/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-11/1105/index.html
+samples/phase-11/1105
+1105
+```
+
+Phase folder:
+
+```text
+samples/phase-11
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-11/1106/README.md b/samples/phase-11/1106/README.md
new file mode 100644
index 00000000..cd516c31
--- /dev/null
+++ b/samples/phase-11/1106/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# sample 1106 - Release Validation Checklist Flow
+
+Sample-local contract for `samples/phase-11/1106`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-11/1106/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `ReleaseValidationChecklistScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-11/1106` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-11` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-11/1106/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-11/1106/index.html
+samples/phase-11/1106
+1106
+```
+
+Phase folder:
+
+```text
+samples/phase-11
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-12/1201/README.md b/samples/phase-12/1201/README.md
index 41e8bc6b..af763f05 100644
--- a/samples/phase-12/1201/README.md
+++ b/samples/phase-12/1201/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1201 - Tilemap Viewer
+
+Sample-local contract for `samples/phase-12/1201`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-12/1201/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `TilemapViewerScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-12/1201` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-12` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-12/1201/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-12/1201/index.html
+samples/phase-12/1201
+1201
+```
+
+Phase folder:
+
+```text
+samples/phase-12
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample1201 - Tilemap Viewer

 ## Purpose
diff --git a/samples/phase-12/1202/README.md b/samples/phase-12/1202/README.md
index 20cab688..258735f6 100644
--- a/samples/phase-12/1202/README.md
+++ b/samples/phase-12/1202/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1202 - Tilemap Hero Movement
+
+Sample-local contract for `samples/phase-12/1202`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-12/1202/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `TilemapHeroMovementScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-12/1202` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-12` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-12/1202/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-12/1202/index.html
+samples/phase-12/1202
+1202
+```
+
+Phase folder:
+
+```text
+samples/phase-12
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample1202 - Tilemap Hero Movement

 ## Purpose
diff --git a/samples/phase-12/1203/README.md b/samples/phase-12/1203/README.md
index 8fa770b9..1f21a2b5 100644
--- a/samples/phase-12/1203/README.md
+++ b/samples/phase-12/1203/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1203 - Tilemap Hero Jump Collision
+
+Sample-local contract for `samples/phase-12/1203`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-12/1203/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `TilemapHeroJumpCollisionScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-12/1203` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-12` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-12/1203/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-12/1203/index.html
+samples/phase-12/1203
+1203
+```
+
+Phase folder:
+
+```text
+samples/phase-12
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample1203 - Tilemap Hero Jump Collision

 ## Purpose
diff --git a/samples/phase-12/1204/README.md b/samples/phase-12/1204/README.md
index 9e44546f..b56a679a 100644
--- a/samples/phase-12/1204/README.md
+++ b/samples/phase-12/1204/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1204 - Tilemap Parallax Hero
+
+Sample-local contract for `samples/phase-12/1204`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-12/1204/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `TilemapParallaxHeroScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-12/1204` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-12` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-12/1204/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-12/1204/index.html
+samples/phase-12/1204
+1204
+```
+
+Phase folder:
+
+```text
+samples/phase-12
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample1204 - Tilemap Parallax Hero

 ## Purpose
diff --git a/samples/phase-12/1205/README.md b/samples/phase-12/1205/README.md
index 5e39963b..066bb995 100644
--- a/samples/phase-12/1205/README.md
+++ b/samples/phase-12/1205/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1205 - Multi-System Demo
+
+Sample-local contract for `samples/phase-12/1205`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-12/1205/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `MultiSystemDemoScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-12/1205` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-12` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-12/1205/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-12/1205/index.html
+samples/phase-12/1205
+1205
+```
+
+Phase folder:
+
+```text
+samples/phase-12
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 1205 - Multi-System Demo

 ## Purpose
diff --git a/samples/phase-12/1206/README.md b/samples/phase-12/1206/README.md
index f570750a..8c78a17a 100644
--- a/samples/phase-12/1206/README.md
+++ b/samples/phase-12/1206/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1206 - Trigger Zone
+
+Sample-local contract for `samples/phase-12/1206`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-12/1206/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `TriggerZoneDemoScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-12/1206` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-12` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-12/1206/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-12/1206/index.html
+samples/phase-12/1206
+1206
+```
+
+Phase folder:
+
+```text
+samples/phase-12
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 1206 - Trigger Zone

 ## Purpose
diff --git a/samples/phase-12/1207/README.md b/samples/phase-12/1207/README.md
index 532f7936..51173456 100644
--- a/samples/phase-12/1207/README.md
+++ b/samples/phase-12/1207/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1207 - Switch Checkpoint Marker
+
+Sample-local contract for `samples/phase-12/1207`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-12/1207/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `SwitchCheckpointDemoScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-12/1207` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-12` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-12/1207/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-12/1207/index.html
+samples/phase-12/1207
+1207
+```
+
+Phase folder:
+
+```text
+samples/phase-12
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 1207 - Switch Checkpoint Marker

 ## Purpose
diff --git a/samples/phase-12/1208/README.md b/samples/phase-12/1208/README.md
index f20c3eb9..8ee2f585 100644
--- a/samples/phase-12/1208/README.md
+++ b/samples/phase-12/1208/README.md
@@ -1,3 +1,65 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1208 - Tool Formatted Tiles Parallax
+
+Sample-local contract for `samples/phase-12/1208`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-12/1208/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `ToolFormattedTilesParallaxScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-12/1208` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-12` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-12/1208/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-12/1208/index.html
+samples/phase-12/1208
+1208
+```
+
+Phase folder:
+
+```text
+samples/phase-12
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
+
+## Existing Notes Preserved
+
 # Sample 1208 - Tool Formatted Tiles Parallax

 ## Purpose
diff --git a/samples/phase-12/1209/README.md b/samples/phase-12/1209/README.md
new file mode 100644
index 00000000..40344095
--- /dev/null
+++ b/samples/phase-12/1209/README.md
@@ -0,0 +1,58 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1209 - Tilemap Basic Layout Preset
+
+Sample-local contract for `samples/phase-12/1209`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-12/1209/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `./presetReferences.js`
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `presetReferences.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-12/1209` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-12` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-12/1209/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-12/1209/index.html
+samples/phase-12/1209
+1209
+```
+
+Phase folder:
+
+```text
+samples/phase-12
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-12/1210/README.md b/samples/phase-12/1210/README.md
new file mode 100644
index 00000000..f0e18118
--- /dev/null
+++ b/samples/phase-12/1210/README.md
@@ -0,0 +1,58 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1210 - Tilemap Objective Layout Preset
+
+Sample-local contract for `samples/phase-12/1210`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-12/1210/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `./presetReferences.js`
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `presetReferences.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-12/1210` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-12` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-12/1210/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-12/1210/index.html
+samples/phase-12/1210
+1210
+```
+
+Phase folder:
+
+```text
+samples/phase-12
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-12/1211/README.md b/samples/phase-12/1211/README.md
new file mode 100644
index 00000000..b94dd460
--- /dev/null
+++ b/samples/phase-12/1211/README.md
@@ -0,0 +1,58 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1211 - Tilemap Mario Learning Layout Preset
+
+Sample-local contract for `samples/phase-12/1211`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-12/1211/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `./presetReferences.js`
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `presetReferences.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-12/1211` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-12` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-12/1211/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-12/1211/index.html
+samples/phase-12/1211
+1211
+```
+
+Phase folder:
+
+```text
+samples/phase-12
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-12/1212/README.md b/samples/phase-12/1212/README.md
new file mode 100644
index 00000000..64b1e00f
--- /dev/null
+++ b/samples/phase-12/1212/README.md
@@ -0,0 +1,56 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1212 - Vector Map Overworld Route Preset
+
+Sample-local contract for `samples/phase-12/1212`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-12/1212/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-12/1212` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-12` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-12/1212/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-12/1212/index.html
+samples/phase-12/1212
+1212
+```
+
+Phase folder:
+
+```text
+samples/phase-12
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-12/1213/README.md b/samples/phase-12/1213/README.md
new file mode 100644
index 00000000..916cde65
--- /dev/null
+++ b/samples/phase-12/1213/README.md
@@ -0,0 +1,56 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1213 - Vector Map Dungeon Layout Preset
+
+Sample-local contract for `samples/phase-12/1213`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-12/1213/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-12/1213` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-12` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-12/1213/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-12/1213/index.html
+samples/phase-12/1213
+1213
+```
+
+Phase folder:
+
+```text
+samples/phase-12
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-12/1214/README.md b/samples/phase-12/1214/README.md
new file mode 100644
index 00000000..d2e6ce1d
--- /dev/null
+++ b/samples/phase-12/1214/README.md
@@ -0,0 +1,56 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1214 - Vector Map Arena Zone Preset
+
+Sample-local contract for `samples/phase-12/1214`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-12/1214/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-12/1214` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-12` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-12/1214/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-12/1214/index.html
+samples/phase-12/1214
+1214
+```
+
+Phase folder:
+
+```text
+samples/phase-12
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-12/1215/README.md b/samples/phase-12/1215/README.md
new file mode 100644
index 00000000..af90a838
--- /dev/null
+++ b/samples/phase-12/1215/README.md
@@ -0,0 +1,56 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1215 - Vector Asset Sky Gradient Preset
+
+Sample-local contract for `samples/phase-12/1215`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-12/1215/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-12/1215` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-12` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-12/1215/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-12/1215/index.html
+samples/phase-12/1215
+1215
+```
+
+Phase folder:
+
+```text
+samples/phase-12
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-12/1216/README.md b/samples/phase-12/1216/README.md
new file mode 100644
index 00000000..45034885
--- /dev/null
+++ b/samples/phase-12/1216/README.md
@@ -0,0 +1,56 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1216 - Vector Asset Mountain Range Preset
+
+Sample-local contract for `samples/phase-12/1216`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-12/1216/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-12/1216` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-12` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-12/1216/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-12/1216/index.html
+samples/phase-12/1216
+1216
+```
+
+Phase folder:
+
+```text
+samples/phase-12
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-12/1217/README.md b/samples/phase-12/1217/README.md
new file mode 100644
index 00000000..046b41c4
--- /dev/null
+++ b/samples/phase-12/1217/README.md
@@ -0,0 +1,56 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1217 - Vector Asset Mario Backdrop Preset
+
+Sample-local contract for `samples/phase-12/1217`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-12/1217/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-12/1217` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-12` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-12/1217/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-12/1217/index.html
+samples/phase-12/1217
+1217
+```
+
+Phase folder:
+
+```text
+samples/phase-12
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-12/1218/README.md b/samples/phase-12/1218/README.md
new file mode 100644
index 00000000..f5d5b24f
--- /dev/null
+++ b/samples/phase-12/1218/README.md
@@ -0,0 +1,56 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1218 - Parallax Layered World Preset
+
+Sample-local contract for `samples/phase-12/1218`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-12/1218/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-12/1218` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-12` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-12/1218/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-12/1218/index.html
+samples/phase-12/1218
+1218
+```
+
+Phase folder:
+
+```text
+samples/phase-12
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-12/1219/README.md b/samples/phase-12/1219/README.md
new file mode 100644
index 00000000..940b2d35
--- /dev/null
+++ b/samples/phase-12/1219/README.md
@@ -0,0 +1,56 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1219 - Parallax Tilemap Embedded Preset
+
+Sample-local contract for `samples/phase-12/1219`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-12/1219/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-12/1219` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-12` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-12/1219/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-12/1219/index.html
+samples/phase-12/1219
+1219
+```
+
+Phase folder:
+
+```text
+samples/phase-12
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-12/1220/README.md b/samples/phase-12/1220/README.md
new file mode 100644
index 00000000..62c330f6
--- /dev/null
+++ b/samples/phase-12/1220/README.md
@@ -0,0 +1,56 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1220 - Parallax Mario Learning Preset
+
+Sample-local contract for `samples/phase-12/1220`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-12/1220/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-12/1220` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-12` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-12/1220/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-12/1220/index.html
+samples/phase-12/1220
+1220
+```
+
+Phase folder:
+
+```text
+samples/phase-12
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-13/1301/README.md b/samples/phase-13/1301/README.md
new file mode 100644
index 00000000..223ef669
--- /dev/null
+++ b/samples/phase-13/1301/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1301 - Networking Layer
+
+Sample-local contract for `samples/phase-13/1301`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-13/1301/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `NetworkingLayerScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-13/1301` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-13` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-13/1301/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-13/1301/index.html
+samples/phase-13/1301
+1301
+```
+
+Phase folder:
+
+```text
+samples/phase-13
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-13/1302/README.md b/samples/phase-13/1302/README.md
new file mode 100644
index 00000000..44d2e020
--- /dev/null
+++ b/samples/phase-13/1302/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1302 - State Sync / Replication
+
+Sample-local contract for `samples/phase-13/1302`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-13/1302/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `StateSyncReplicationScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-13/1302` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-13` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-13/1302/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-13/1302/index.html
+samples/phase-13/1302
+1302
+```
+
+Phase folder:
+
+```text
+samples/phase-13
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-13/1303/README.md b/samples/phase-13/1303/README.md
new file mode 100644
index 00000000..a2960e7e
--- /dev/null
+++ b/samples/phase-13/1303/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1303 - Asteroids World Systems
+
+Sample-local contract for `samples/phase-13/1303`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-13/1303/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `AsteroidsWorldSystemsScene.js`
+- `index.html`
+- `index.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-13/1303` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-13` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-13/1303/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-13/1303/index.html
+samples/phase-13/1303
+1303
+```
+
+Phase folder:
+
+```text
+samples/phase-13
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-13/1304/README.md b/samples/phase-13/1304/README.md
new file mode 100644
index 00000000..d454c7d5
--- /dev/null
+++ b/samples/phase-13/1304/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1304 - Client Prediction / Reconciliation
+
+Sample-local contract for `samples/phase-13/1304`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-13/1304/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `ClientPredictionReconciliationScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-13/1304` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-13` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-13/1304/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-13/1304/index.html
+samples/phase-13/1304
+1304
+```
+
+Phase folder:
+
+```text
+samples/phase-13
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-13/1305/README.md b/samples/phase-13/1305/README.md
new file mode 100644
index 00000000..020a794b
--- /dev/null
+++ b/samples/phase-13/1305/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1305 - Serialization System
+
+Sample-local contract for `samples/phase-13/1305`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-13/1305/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `SerializationSystemScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-13/1305` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-13` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-13/1305/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-13/1305/index.html
+samples/phase-13/1305
+1305
+```
+
+Phase folder:
+
+```text
+samples/phase-13
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-13/1306/README.md b/samples/phase-13/1306/README.md
new file mode 100644
index 00000000..20eeae46
--- /dev/null
+++ b/samples/phase-13/1306/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1306 - Network Debug Overlay
+
+Sample-local contract for `samples/phase-13/1306`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-13/1306/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `NetworkDebugOverlayScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-13/1306` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-13` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-13/1306/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-13/1306/index.html
+samples/phase-13/1306
+1306
+```
+
+Phase folder:
+
+```text
+samples/phase-13
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-13/1307/README.md b/samples/phase-13/1307/README.md
new file mode 100644
index 00000000..47c00b4d
--- /dev/null
+++ b/samples/phase-13/1307/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1307 - Remote Entity Interpolation
+
+Sample-local contract for `samples/phase-13/1307`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-13/1307/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `RemoteEntityInterpolationScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-13/1307` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-13` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-13/1307/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-13/1307/index.html
+samples/phase-13/1307
+1307
+```
+
+Phase folder:
+
+```text
+samples/phase-13
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-13/1308/README.md b/samples/phase-13/1308/README.md
new file mode 100644
index 00000000..44869a49
--- /dev/null
+++ b/samples/phase-13/1308/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1308 - Lobby / Session System
+
+Sample-local contract for `samples/phase-13/1308`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-13/1308/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `LobbySessionSystemScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-13/1308` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-13` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-13/1308/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-13/1308/index.html
+samples/phase-13/1308
+1308
+```
+
+Phase folder:
+
+```text
+samples/phase-13
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-13/1309/README.md b/samples/phase-13/1309/README.md
new file mode 100644
index 00000000..858edb7d
--- /dev/null
+++ b/samples/phase-13/1309/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1309 - Space Invaders World Systems
+
+Sample-local contract for `samples/phase-13/1309`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-13/1309/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `SpaceInvadersWorldSystemsScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-13/1309` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-13` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-13/1309/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-13/1309/index.html
+samples/phase-13/1309
+1309
+```
+
+Phase folder:
+
+```text
+samples/phase-13
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-13/1310/README.md b/samples/phase-13/1310/README.md
new file mode 100644
index 00000000..d1dd513e
--- /dev/null
+++ b/samples/phase-13/1310/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1310 - Host / Server Bootstrap
+
+Sample-local contract for `samples/phase-13/1310`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-13/1310/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `HostServerBootstrapScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-13/1310` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-13` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-13/1310/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-13/1310/index.html
+samples/phase-13/1310
+1310
+```
+
+Phase folder:
+
+```text
+samples/phase-13
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-13/1311/README.md b/samples/phase-13/1311/README.md
new file mode 100644
index 00000000..d6b5563f
--- /dev/null
+++ b/samples/phase-13/1311/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1311 - Interest Management
+
+Sample-local contract for `samples/phase-13/1311`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-13/1311/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `InterestManagementScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-13/1311` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-13` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-13/1311/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-13/1311/index.html
+samples/phase-13/1311
+1311
+```
+
+Phase folder:
+
+```text
+samples/phase-13
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-13/1312/README.md b/samples/phase-13/1312/README.md
new file mode 100644
index 00000000..9d486ca9
--- /dev/null
+++ b/samples/phase-13/1312/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1312 - Lag / Packet Loss Simulation
+
+Sample-local contract for `samples/phase-13/1312`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-13/1312/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `LagPacketLossSimulationScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-13/1312` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-13` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-13/1312/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-13/1312/index.html
+samples/phase-13/1312
+1312
+```
+
+Phase folder:
+
+```text
+samples/phase-13
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-13/1313/README.md b/samples/phase-13/1313/README.md
new file mode 100644
index 00000000..87819d02
--- /dev/null
+++ b/samples/phase-13/1313/README.md
@@ -0,0 +1,60 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1313 - Pacman Lite World Systems
+
+Sample-local contract for `samples/phase-13/1313`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-13/1313/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `index.js`
+- `main.js`
+- `PacmanLiteWorldSystemsScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-13/1313` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-13` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-13/1313/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-13/1313/index.html
+samples/phase-13/1313
+1313
+```
+
+Phase folder:
+
+```text
+samples/phase-13
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-13/1314/README.md b/samples/phase-13/1314/README.md
new file mode 100644
index 00000000..2756ad6b
--- /dev/null
+++ b/samples/phase-13/1314/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1314 - Chat / Presence Layer
+
+Sample-local contract for `samples/phase-13/1314`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-13/1314/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `ChatPresenceLayerScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-13/1314` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-13` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-13/1314/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-13/1314/index.html
+samples/phase-13/1314
+1314
+```
+
+Phase folder:
+
+```text
+samples/phase-13
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-13/1315/README.md b/samples/phase-13/1315/README.md
new file mode 100644
index 00000000..86435b38
--- /dev/null
+++ b/samples/phase-13/1315/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1315 - Rollback / Replay Diagnostics
+
+Sample-local contract for `samples/phase-13/1315`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-13/1315/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `RollbackReplayDiagnosticsScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-13/1315` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-13` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-13/1315/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-13/1315/index.html
+samples/phase-13/1315
+1315
+```
+
+Phase folder:
+
+```text
+samples/phase-13
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-13/1316/README.md b/samples/phase-13/1316/README.md
new file mode 100644
index 00000000..d9451841
--- /dev/null
+++ b/samples/phase-13/1316/README.md
@@ -0,0 +1,62 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1316 - Local Loopback / Fake Network
+
+Sample-local contract for `samples/phase-13/1316`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-13/1316/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `debug/networkSampleADebug.js`
+- `game/FakeLoopbackNetworkModel.js`
+- `game/NetworkSampleAScene.js`
+- `index.html`
+- `main.js`
+- `server/networkSampleADashboardServer.mjs`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-13/1316` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-13` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-13/1316/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-13/1316/index.html
+samples/phase-13/1316
+1316
+```
+
+Phase folder:
+
+```text
+samples/phase-13
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-13/1317/README.md b/samples/phase-13/1317/README.md
new file mode 100644
index 00000000..e6bb1167
--- /dev/null
+++ b/samples/phase-13/1317/README.md
@@ -0,0 +1,61 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1317 - Host / Client Diagnostics
+
+Sample-local contract for `samples/phase-13/1317`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-13/1317/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `debug/networkSampleBDebug.js`
+- `game/FakeHostClientNetworkModel.js`
+- `game/NetworkSampleBScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-13/1317` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-13` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-13/1317/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-13/1317/index.html
+samples/phase-13/1317
+1317
+```
+
+Phase folder:
+
+```text
+samples/phase-13
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-13/1318/README.md b/samples/phase-13/1318/README.md
new file mode 100644
index 00000000..b0fbed4d
--- /dev/null
+++ b/samples/phase-13/1318/README.md
@@ -0,0 +1,63 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1318 - Divergence / Trace Validation
+
+Sample-local contract for `samples/phase-13/1318`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-13/1318/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `debug/networkSampleCDebug.js`
+- `game/FakeDivergenceTraceNetworkModel.js`
+- `game/NetworkSampleCScene.js`
+- `game/ReconciliationLayerAdapter.js`
+- `game/StateTimelineBuffer.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-13/1318` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-13` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-13/1318/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-13/1318/index.html
+samples/phase-13/1318
+1318
+```
+
+Phase folder:
+
+```text
+samples/phase-13
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-13/1319/README.md b/samples/phase-13/1319/README.md
new file mode 100644
index 00000000..70ecb91c
--- /dev/null
+++ b/samples/phase-13/1319/README.md
@@ -0,0 +1,62 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1319 - Real Network + Live Dashboard
+
+Sample-local contract for `samples/phase-13/1319`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-13/1319/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `game/RealNetworkLaunchScene.js`
+- `index.html`
+- `main.js`
+- `networkSample1319.css`
+- `server/realNetworkDashboard.mjs`
+- `server/realNetworkServer.mjs`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-13/1319` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-13` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-13/1319/assets/images`.
+- Asset folder status: missing.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-13/1319/index.html
+samples/phase-13/1319
+1319
+```
+
+Phase folder:
+
+```text
+samples/phase-13
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-13/1320/README.md b/samples/phase-13/1320/README.md
new file mode 100644
index 00000000..9628e15f
--- /dev/null
+++ b/samples/phase-13/1320/README.md
@@ -0,0 +1,72 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Pacman Lite
+
+Sample-local contract for `samples/phase-13/1320`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-13/1320/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `flow/attract.js`
+- `flow/highscore.js`
+- `flow/intro.js`
+- `game/PacmanLiteConfig.js`
+- `game/PacmanLiteDebugOverlay.js`
+- `game/PacmanLiteGhostController.js`
+- `game/PacmanLiteGrid.js`
+- `game/PacmanLiteInputController.js`
+- `game/PacmanLiteNavigator.js`
+- `game/PacmanLitePlayerController.js`
+- `game/PacmanLiteScene.js`
+- `game/PacmanLiteWorld.js`
+- `index.html`
+- `main.js`
+- `rules/flowContent.js`
+- `rules/flowRules.js`
+- `rules/gameFlowRules.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-13/1320` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-13` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-13/1320/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-13/1320/index.html
+samples/phase-13/1320
+1320
+```
+
+Phase folder:
+
+```text
+samples/phase-13
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-14/1401/README.md b/samples/phase-14/1401/README.md
new file mode 100644
index 00000000..d745f3cc
--- /dev/null
+++ b/samples/phase-14/1401/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1401 - Level Editor
+
+Sample-local contract for `samples/phase-14/1401`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-14/1401/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `LevelEditorScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-14/1401` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-14` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-14/1401/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-14/1401/index.html
+samples/phase-14/1401
+1401
+```
+
+Phase folder:
+
+```text
+samples/phase-14
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-14/1402/README.md b/samples/phase-14/1402/README.md
new file mode 100644
index 00000000..bb7c6ff7
--- /dev/null
+++ b/samples/phase-14/1402/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1402 - Tile / Map Editor
+
+Sample-local contract for `samples/phase-14/1402`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-14/1402/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `TileMapEditorScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-14/1402` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-14` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-14/1402/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-14/1402/index.html
+samples/phase-14/1402
+1402
+```
+
+Phase folder:
+
+```text
+samples/phase-14
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-14/1403/README.md b/samples/phase-14/1403/README.md
new file mode 100644
index 00000000..b47ad2da
--- /dev/null
+++ b/samples/phase-14/1403/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1403 - Entity Placement Editor
+
+Sample-local contract for `samples/phase-14/1403`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-14/1403/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `EntityPlacementEditorScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-14/1403` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-14` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-14/1403/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-14/1403/index.html
+samples/phase-14/1403
+1403
+```
+
+Phase folder:
+
+```text
+samples/phase-14
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-14/1404/README.md b/samples/phase-14/1404/README.md
new file mode 100644
index 00000000..21296da5
--- /dev/null
+++ b/samples/phase-14/1404/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1404 - Timeline / Cutscene Editor
+
+Sample-local contract for `samples/phase-14/1404`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-14/1404/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `TimelineCutsceneEditorScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-14/1404` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-14` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-14/1404/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-14/1404/index.html
+samples/phase-14/1404
+1404
+```
+
+Phase folder:
+
+```text
+samples/phase-14
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-14/1405/README.md b/samples/phase-14/1405/README.md
new file mode 100644
index 00000000..0124427c
--- /dev/null
+++ b/samples/phase-14/1405/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1405 - Automated Test Runner
+
+Sample-local contract for `samples/phase-14/1405`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-14/1405/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `AutomatedTestRunnerScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-14/1405` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-14` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-14/1405/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-14/1405/index.html
+samples/phase-14/1405
+1405
+```
+
+Phase folder:
+
+```text
+samples/phase-14
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-14/1406/README.md b/samples/phase-14/1406/README.md
new file mode 100644
index 00000000..9c22e2f5
--- /dev/null
+++ b/samples/phase-14/1406/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1406 - Regression Playback Harness
+
+Sample-local contract for `samples/phase-14/1406`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-14/1406/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `RegressionPlaybackHarnessScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-14/1406` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-14` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-14/1406/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-14/1406/index.html
+samples/phase-14/1406
+1406
+```
+
+Phase folder:
+
+```text
+samples/phase-14
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-14/1407/README.md b/samples/phase-14/1407/README.md
new file mode 100644
index 00000000..a62c9fdd
--- /dev/null
+++ b/samples/phase-14/1407/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1407 - Performance Benchmark Runner
+
+Sample-local contract for `samples/phase-14/1407`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-14/1407/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `PerformanceBenchmarkRunnerScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-14/1407` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-14` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-14/1407/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-14/1407/index.html
+samples/phase-14/1407
+1407
+```
+
+Phase folder:
+
+```text
+samples/phase-14
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-14/1408/README.md b/samples/phase-14/1408/README.md
new file mode 100644
index 00000000..83d40605
--- /dev/null
+++ b/samples/phase-14/1408/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1408 - CI Validation Flow
+
+Sample-local contract for `samples/phase-14/1408`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-14/1408/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `CIValidationFlowScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-14/1408` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-14` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-14/1408/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-14/1408/index.html
+samples/phase-14/1408
+1408
+```
+
+Phase folder:
+
+```text
+samples/phase-14
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-14/1409/README.md b/samples/phase-14/1409/README.md
new file mode 100644
index 00000000..ad5d949e
--- /dev/null
+++ b/samples/phase-14/1409/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1409 - Packet Validation / Anti-Cheat Basics
+
+Sample-local contract for `samples/phase-14/1409`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-14/1409/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `PacketValidationAntiCheatScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-14/1409` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-14` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-14/1409/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-14/1409/index.html
+samples/phase-14/1409
+1409
+```
+
+Phase folder:
+
+```text
+samples/phase-14
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-14/1410/README.md b/samples/phase-14/1410/README.md
new file mode 100644
index 00000000..ee5ec1e5
--- /dev/null
+++ b/samples/phase-14/1410/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1410 - Save / Data Integrity Checks
+
+Sample-local contract for `samples/phase-14/1410`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-14/1410/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `SaveDataIntegrityChecksScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-14/1410` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-14` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-14/1410/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-14/1410/index.html
+samples/phase-14/1410
+1410
+```
+
+Phase folder:
+
+```text
+samples/phase-14
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-14/1411/README.md b/samples/phase-14/1411/README.md
new file mode 100644
index 00000000..b41206d0
--- /dev/null
+++ b/samples/phase-14/1411/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1411 - Permissions / Capability Gating
+
+Sample-local contract for `samples/phase-14/1411`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-14/1411/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `PermissionsCapabilityGatingScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-14/1411` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-14` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-14/1411/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-14/1411/index.html
+samples/phase-14/1411
+1411
+```
+
+Phase folder:
+
+```text
+samples/phase-14
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-14/1412/README.md b/samples/phase-14/1412/README.md
new file mode 100644
index 00000000..d5184264
--- /dev/null
+++ b/samples/phase-14/1412/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1412 - Trust / Session Validation
+
+Sample-local contract for `samples/phase-14/1412`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-14/1412/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `TrustSessionValidationScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-14/1412` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-14` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-14/1412/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-14/1412/index.html
+samples/phase-14/1412
+1412
+```
+
+Phase folder:
+
+```text
+samples/phase-14
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-14/1413/README.md b/samples/phase-14/1413/README.md
new file mode 100644
index 00000000..0574250f
--- /dev/null
+++ b/samples/phase-14/1413/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1413 - Asset Import Pipeline
+
+Sample-local contract for `samples/phase-14/1413`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-14/1413/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `AssetImportPipelineScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-14/1413` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-14` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-14/1413/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-14/1413/index.html
+samples/phase-14/1413
+1413
+```
+
+Phase folder:
+
+```text
+samples/phase-14
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-14/1414/README.md b/samples/phase-14/1414/README.md
new file mode 100644
index 00000000..2479d2de
--- /dev/null
+++ b/samples/phase-14/1414/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1414 - Texture / Sprite Preprocess Pipeline
+
+Sample-local contract for `samples/phase-14/1414`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-14/1414/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `TextureSpritePreprocessPipelineScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-14/1414` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-14` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-14/1414/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-14/1414/index.html
+samples/phase-14/1414
+1414
+```
+
+Phase folder:
+
+```text
+samples/phase-14
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-14/1415/README.md b/samples/phase-14/1415/README.md
new file mode 100644
index 00000000..884d4800
--- /dev/null
+++ b/samples/phase-14/1415/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1415 - Audio Preprocess Pipeline
+
+Sample-local contract for `samples/phase-14/1415`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-14/1415/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `AudioPreprocessPipelineScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-14/1415` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-14` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-14/1415/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-14/1415/index.html
+samples/phase-14/1415
+1415
+```
+
+Phase folder:
+
+```text
+samples/phase-14
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-14/1416/README.md b/samples/phase-14/1416/README.md
new file mode 100644
index 00000000..9065437e
--- /dev/null
+++ b/samples/phase-14/1416/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1416 - Content Versioning / Migration
+
+Sample-local contract for `samples/phase-14/1416`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-14/1416/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `ContentVersioningMigrationScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-14/1416` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-14` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-14/1416/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-14/1416/index.html
+samples/phase-14/1416
+1416
+```
+
+Phase folder:
+
+```text
+samples/phase-14
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-14/1417/README.md b/samples/phase-14/1417/README.md
new file mode 100644
index 00000000..07595d36
--- /dev/null
+++ b/samples/phase-14/1417/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1417 - Build Asset Manifest System
+
+Sample-local contract for `samples/phase-14/1417`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-14/1417/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `BuildAssetManifestSystemScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-14/1417` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-14` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-14/1417/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-14/1417/index.html
+samples/phase-14/1417
+1417
+```
+
+Phase folder:
+
+```text
+samples/phase-14
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-14/1418/README.md b/samples/phase-14/1418/README.md
new file mode 100644
index 00000000..4ca0aad2
--- /dev/null
+++ b/samples/phase-14/1418/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1418 - Content Validation Pipeline
+
+Sample-local contract for `samples/phase-14/1418`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-14/1418/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `ContentValidationPipelineScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-14/1418` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-14` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-14/1418/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-14/1418/index.html
+samples/phase-14/1418
+1418
+```
+
+Phase folder:
+
+```text
+samples/phase-14
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-15/1501/README.md b/samples/phase-15/1501/README.md
new file mode 100644
index 00000000..cc1bd17c
--- /dev/null
+++ b/samples/phase-15/1501/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1501 - Developer Console
+
+Sample-local contract for `samples/phase-15/1501`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-15/1501/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `DeveloperConsoleScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-15/1501` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-15` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-15/1501/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-15/1501/index.html
+samples/phase-15/1501
+1501
+```
+
+Phase folder:
+
+```text
+samples/phase-15
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-15/1502/README.md b/samples/phase-15/1502/README.md
new file mode 100644
index 00000000..7ba348a1
--- /dev/null
+++ b/samples/phase-15/1502/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1502 - In-Engine Inspector
+
+Sample-local contract for `samples/phase-15/1502`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-15/1502/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `InEngineInspectorScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-15/1502` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-15` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-15/1502/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-15/1502/index.html
+samples/phase-15/1502
+1502
+```
+
+Phase folder:
+
+```text
+samples/phase-15
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-15/1503/README.md b/samples/phase-15/1503/README.md
new file mode 100644
index 00000000..3937f0a0
--- /dev/null
+++ b/samples/phase-15/1503/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1503 - Property Editor
+
+Sample-local contract for `samples/phase-15/1503`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-15/1503/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `PropertyEditorScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-15/1503` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-15` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-15/1503/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-15/1503/index.html
+samples/phase-15/1503
+1503
+```
+
+Phase folder:
+
+```text
+samples/phase-15
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-15/1504/README.md b/samples/phase-15/1504/README.md
new file mode 100644
index 00000000..fc37f960
--- /dev/null
+++ b/samples/phase-15/1504/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1504 - Live Tuning / Hot Reload
+
+Sample-local contract for `samples/phase-15/1504`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-15/1504/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `LiveTuningHotReloadScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-15/1504` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-15` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-15/1504/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-15/1504/index.html
+samples/phase-15/1504
+1504
+```
+
+Phase folder:
+
+```text
+samples/phase-15
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-15/1505/README.md b/samples/phase-15/1505/README.md
new file mode 100644
index 00000000..1e778547
--- /dev/null
+++ b/samples/phase-15/1505/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1505 - Asset Browser
+
+Sample-local contract for `samples/phase-15/1505`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-15/1505/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `AssetBrowserScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-15/1505` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-15` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-15/1505/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-15/1505/index.html
+samples/phase-15/1505
+1505
+```
+
+Phase folder:
+
+```text
+samples/phase-15
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-15/1506/README.md b/samples/phase-15/1506/README.md
new file mode 100644
index 00000000..00e7d055
--- /dev/null
+++ b/samples/phase-15/1506/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1506 - Scene Graph / Entity Hierarchy Viewer
+
+Sample-local contract for `samples/phase-15/1506`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-15/1506/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `SceneGraphEntityHierarchyViewerScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-15/1506` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-15` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-15/1506/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-15/1506/index.html
+samples/phase-15/1506
+1506
+```
+
+Phase folder:
+
+```text
+samples/phase-15
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-16/1601/README.md b/samples/phase-16/1601/README.md
new file mode 100644
index 00000000..e108419a
--- /dev/null
+++ b/samples/phase-16/1601/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1601 - 3D Cube Explorer
+
+Sample-local contract for `samples/phase-16/1601`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-16/1601/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `CubeExplorer3DScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-16/1601` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-16` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-16/1601/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-16/1601/index.html
+samples/phase-16/1601
+1601
+```
+
+Phase folder:
+
+```text
+samples/phase-16
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-16/1602/README.md b/samples/phase-16/1602/README.md
new file mode 100644
index 00000000..586f398b
--- /dev/null
+++ b/samples/phase-16/1602/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1602 - 3D Maze Runner
+
+Sample-local contract for `samples/phase-16/1602`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-16/1602/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `MazeRunner3DScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-16/1602` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-16` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-16/1602/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-16/1602/index.html
+samples/phase-16/1602
+1602
+```
+
+Phase folder:
+
+```text
+samples/phase-16
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-16/1603/README.md b/samples/phase-16/1603/README.md
new file mode 100644
index 00000000..be1b9781
--- /dev/null
+++ b/samples/phase-16/1603/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1603 - First Person Walkthrough
+
+Sample-local contract for `samples/phase-16/1603`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-16/1603/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `FirstPersonWalkthroughScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-16/1603` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-16` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-16/1603/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-16/1603/index.html
+samples/phase-16/1603
+1603
+```
+
+Phase folder:
+
+```text
+samples/phase-16
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-16/1604/README.md b/samples/phase-16/1604/README.md
new file mode 100644
index 00000000..ed83b1cf
--- /dev/null
+++ b/samples/phase-16/1604/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1604 - 3D Platformer Basics
+
+Sample-local contract for `samples/phase-16/1604`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-16/1604/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `Platformer3DBasicsScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-16/1604` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-16` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-16/1604/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-16/1604/index.html
+samples/phase-16/1604
+1604
+```
+
+Phase folder:
+
+```text
+samples/phase-16
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-16/1605/README.md b/samples/phase-16/1605/README.md
new file mode 100644
index 00000000..15974b5c
--- /dev/null
+++ b/samples/phase-16/1605/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1605 - 3D Driving Sandbox
+
+Sample-local contract for `samples/phase-16/1605`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-16/1605/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `DrivingSandbox3DScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-16/1605` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-16` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-16/1605/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-16/1605/index.html
+samples/phase-16/1605
+1605
+```
+
+Phase folder:
+
+```text
+samples/phase-16
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-16/1606/README.md b/samples/phase-16/1606/README.md
new file mode 100644
index 00000000..e36debb6
--- /dev/null
+++ b/samples/phase-16/1606/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1606 - 3D Physics Playground
+
+Sample-local contract for `samples/phase-16/1606`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-16/1606/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `PhysicsPlayground3DScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-16/1606` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-16` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-16/1606/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-16/1606/index.html
+samples/phase-16/1606
+1606
+```
+
+Phase folder:
+
+```text
+samples/phase-16
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-16/1607/README.md b/samples/phase-16/1607/README.md
new file mode 100644
index 00000000..599185a5
--- /dev/null
+++ b/samples/phase-16/1607/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1607 - 3D Space Shooter
+
+Sample-local contract for `samples/phase-16/1607`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-16/1607/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `SpaceShooter3DScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-16/1607` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-16` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-16/1607/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-16/1607/index.html
+samples/phase-16/1607
+1607
+```
+
+Phase folder:
+
+```text
+samples/phase-16
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-16/1608/README.md b/samples/phase-16/1608/README.md
new file mode 100644
index 00000000..9e9ad8b5
--- /dev/null
+++ b/samples/phase-16/1608/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1608 - 3D Dungeon Crawler
+
+Sample-local contract for `samples/phase-16/1608`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-16/1608/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `DungeonCrawler3DScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-16/1608` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-16` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-16/1608/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-16/1608/index.html
+samples/phase-16/1608
+1608
+```
+
+Phase folder:
+
+```text
+samples/phase-16
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-16/1609/README.md b/samples/phase-16/1609/README.md
new file mode 100644
index 00000000..1e65f66e
--- /dev/null
+++ b/samples/phase-16/1609/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1609 - 3D Lighting Demo
+
+Sample-local contract for `samples/phase-16/1609`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-16/1609/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `LightingDemo3DScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-16/1609` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-16` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-16/1609/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-16/1609/index.html
+samples/phase-16/1609
+1609
+```
+
+Phase folder:
+
+```text
+samples/phase-16
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-16/1610/README.md b/samples/phase-16/1610/README.md
new file mode 100644
index 00000000..5855146b
--- /dev/null
+++ b/samples/phase-16/1610/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1610 - Hybrid 2D/3D World
+
+Sample-local contract for `samples/phase-16/1610`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-16/1610/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `Hybrid2D3DWorldScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-16/1610` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-16` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-16/1610/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-16/1610/index.html
+samples/phase-16/1610
+1610
+```
+
+Phase folder:
+
+```text
+samples/phase-16
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-16/1611/README.md b/samples/phase-16/1611/README.md
new file mode 100644
index 00000000..6521755e
--- /dev/null
+++ b/samples/phase-16/1611/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1611 - Multiplayer Sync Demo
+
+Sample-local contract for `samples/phase-16/1611`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-16/1611/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `MultiplayerSyncDemo3DScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-16/1611` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-16` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-16/1611/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-16/1611/index.html
+samples/phase-16/1611
+1611
+```
+
+Phase folder:
+
+```text
+samples/phase-16
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-16/1612/README.md b/samples/phase-16/1612/README.md
new file mode 100644
index 00000000..081b1bfa
--- /dev/null
+++ b/samples/phase-16/1612/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1612 - 3D Stress Test (1,000 Objects)
+
+Sample-local contract for `samples/phase-16/1612`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-16/1612/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `StressTest3DScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-16/1612` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-16` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-16/1612/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-16/1612/index.html
+samples/phase-16/1612
+1612
+```
+
+Phase folder:
+
+```text
+samples/phase-16
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-16/1613/README.md b/samples/phase-16/1613/README.md
new file mode 100644
index 00000000..7f0b3296
--- /dev/null
+++ b/samples/phase-16/1613/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1613 - 3D Input Lab
+
+Sample-local contract for `samples/phase-16/1613`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-16/1613/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `InputLab3DScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-16/1613` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-16` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-16/1613/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-16/1613/index.html
+samples/phase-16/1613
+1613
+```
+
+Phase folder:
+
+```text
+samples/phase-16
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-16/1614/README.md b/samples/phase-16/1614/README.md
new file mode 100644
index 00000000..cc3ac7a9
--- /dev/null
+++ b/samples/phase-16/1614/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1614 - 3D Camera Modes Lab
+
+Sample-local contract for `samples/phase-16/1614`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-16/1614/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `CameraModesLab3DScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-16/1614` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-16` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-16/1614/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-16/1614/index.html
+samples/phase-16/1614
+1614
+```
+
+Phase folder:
+
+```text
+samples/phase-16
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-16/1615/README.md b/samples/phase-16/1615/README.md
new file mode 100644
index 00000000..2569271a
--- /dev/null
+++ b/samples/phase-16/1615/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1615 - 3D Entity Composition
+
+Sample-local contract for `samples/phase-16/1615`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-16/1615/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `EntityComposition3DScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-16/1615` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-16` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-16/1615/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-16/1615/index.html
+samples/phase-16/1615
+1615
+```
+
+Phase folder:
+
+```text
+samples/phase-16
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-16/1616/README.md b/samples/phase-16/1616/README.md
new file mode 100644
index 00000000..9044ef37
--- /dev/null
+++ b/samples/phase-16/1616/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1616 - 3D World Streaming
+
+Sample-local contract for `samples/phase-16/1616`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-16/1616/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `WorldStreaming3DScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-16/1616` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-16` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-16/1616/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-16/1616/index.html
+samples/phase-16/1616
+1616
+```
+
+Phase folder:
+
+```text
+samples/phase-16
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-16/1617/README.md b/samples/phase-16/1617/README.md
new file mode 100644
index 00000000..c4365de6
--- /dev/null
+++ b/samples/phase-16/1617/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1617 - 3D Large World Streaming
+
+Sample-local contract for `samples/phase-16/1617`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-16/1617/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `LargeWorldStreaming3DScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-16/1617` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-16` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-16/1617/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-16/1617/index.html
+samples/phase-16/1617
+1617
+```
+
+Phase folder:
+
+```text
+samples/phase-16
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-16/1618/README.md b/samples/phase-16/1618/README.md
new file mode 100644
index 00000000..92be519b
--- /dev/null
+++ b/samples/phase-16/1618/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1618 - 3D Lighting & Materials Lab
+
+Sample-local contract for `samples/phase-16/1618`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-16/1618/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `LightingMaterialsLab3DScene.js`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-16/1618` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-16` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-16/1618/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-16/1618/index.html
+samples/phase-16/1618
+1618
+```
+
+Phase folder:
+
+```text
+samples/phase-16
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-16/1619/README.md b/samples/phase-16/1619/README.md
new file mode 100644
index 00000000..59509e7a
--- /dev/null
+++ b/samples/phase-16/1619/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1619 - 3D Debug HUD Sample
+
+Sample-local contract for `samples/phase-16/1619`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-16/1619/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `DebugHud3DScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-16/1619` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-16` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-16/1619/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-16/1619/index.html
+samples/phase-16/1619
+1619
+```
+
+Phase folder:
+
+```text
+samples/phase-16
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-16/1620/README.md b/samples/phase-16/1620/README.md
new file mode 100644
index 00000000..1e47788c
--- /dev/null
+++ b/samples/phase-16/1620/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1620 - 3D Mini Game Reference
+
+Sample-local contract for `samples/phase-16/1620`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-16/1620/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `MiniGame3DReferenceScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-16/1620` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-16` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-16/1620/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-16/1620/index.html
+samples/phase-16/1620
+1620
+```
+
+Phase folder:
+
+```text
+samples/phase-16
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-16/1621/README.md b/samples/phase-16/1621/README.md
new file mode 100644
index 00000000..da8be14c
--- /dev/null
+++ b/samples/phase-16/1621/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1621 - 3D AI Navigation Demo
+
+Sample-local contract for `samples/phase-16/1621`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-16/1621/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `AINavigation3DScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-16/1621` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-16` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-16/1621/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-16/1621/index.html
+samples/phase-16/1621
+1621
+```
+
+Phase folder:
+
+```text
+samples/phase-16
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-16/1622/README.md b/samples/phase-16/1622/README.md
new file mode 100644
index 00000000..84e620d0
--- /dev/null
+++ b/samples/phase-16/1622/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1622 - Basic Raycast Walls
+
+Sample-local contract for `samples/phase-16/1622`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-16/1622/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `RaycastDemoScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-16/1622` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-16` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-16/1622/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-16/1622/index.html
+samples/phase-16/1622
+1622
+```
+
+Phase folder:
+
+```text
+samples/phase-16
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-17/1701/README.md b/samples/phase-17/1701/README.md
new file mode 100644
index 00000000..0456f34c
--- /dev/null
+++ b/samples/phase-17/1701/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1701 - DOOM Raycast + Sprites
+
+Sample-local contract for `samples/phase-17/1701`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-17/1701/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `RaycastDemoScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-17/1701` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-17` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-17/1701/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-17/1701/index.html
+samples/phase-17/1701
+1701
+```
+
+Phase folder:
+
+```text
+samples/phase-17
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-17/1702/README.md b/samples/phase-17/1702/README.md
new file mode 100644
index 00000000..1ce43cbe
--- /dev/null
+++ b/samples/phase-17/1702/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1702 - Wolf Grid Raycast
+
+Sample-local contract for `samples/phase-17/1702`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-17/1702/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `RaycastDemoScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-17/1702` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-17` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-17/1702/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-17/1702/index.html
+samples/phase-17/1702
+1702
+```
+
+Phase folder:
+
+```text
+samples/phase-17
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-17/1703/README.md b/samples/phase-17/1703/README.md
new file mode 100644
index 00000000..5ce57072
--- /dev/null
+++ b/samples/phase-17/1703/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1703 - Wolf Optimized Raycast
+
+Sample-local contract for `samples/phase-17/1703`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-17/1703/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `RaycastDemoScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-17/1703` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-17` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-17/1703/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-17/1703/index.html
+samples/phase-17/1703
+1703
+```
+
+Phase folder:
+
+```text
+samples/phase-17
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-17/1704/README.md b/samples/phase-17/1704/README.md
new file mode 100644
index 00000000..a7ec1569
--- /dev/null
+++ b/samples/phase-17/1704/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1704 - Unreal Texture + Lighting
+
+Sample-local contract for `samples/phase-17/1704`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-17/1704/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `TextureMaterialDemoScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-17/1704` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-17` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-17/1704/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-17/1704/index.html
+samples/phase-17/1704
+1704
+```
+
+Phase folder:
+
+```text
+samples/phase-17
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-17/1705/README.md b/samples/phase-17/1705/README.md
new file mode 100644
index 00000000..cc213e92
--- /dev/null
+++ b/samples/phase-17/1705/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1705 - Unreal Skinned Mesh Demo
+
+Sample-local contract for `samples/phase-17/1705`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-17/1705/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `ImageSkinnedCharacterDemoScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-17/1705` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-17` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-17/1705/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-17/1705/index.html
+samples/phase-17/1705
+1705
+```
+
+Phase folder:
+
+```text
+samples/phase-17
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-17/1706/README.md b/samples/phase-17/1706/README.md
new file mode 100644
index 00000000..5700488a
--- /dev/null
+++ b/samples/phase-17/1706/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1706 - Minecraft Voxel Terrain
+
+Sample-local contract for `samples/phase-17/1706`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-17/1706/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `VoxelWorldDemoScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-17/1706` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-17` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-17/1706/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-17/1706/index.html
+samples/phase-17/1706
+1706
+```
+
+Phase folder:
+
+```text
+samples/phase-17
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-17/1707/README.md b/samples/phase-17/1707/README.md
new file mode 100644
index 00000000..34c7a192
--- /dev/null
+++ b/samples/phase-17/1707/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1707 - Minecraft Chunk Streaming
+
+Sample-local contract for `samples/phase-17/1707`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-17/1707/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `VoxelWorldDemoScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-17/1707` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-17` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-17/1707/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-17/1707/index.html
+samples/phase-17/1707
+1707
+```
+
+Phase folder:
+
+```text
+samples/phase-17
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-17/1708/README.md b/samples/phase-17/1708/README.md
new file mode 100644
index 00000000..23e04dd7
--- /dev/null
+++ b/samples/phase-17/1708/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1708 - Real Gameplay Mini-Game
+
+Sample-local contract for `samples/phase-17/1708`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-17/1708/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `RealGameplayMiniGameScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-17/1708` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-17` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-17/1708/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-17/1708/index.html
+samples/phase-17/1708
+1708
+```
+
+Phase folder:
+
+```text
+samples/phase-17
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-17/1709/README.md b/samples/phase-17/1709/README.md
new file mode 100644
index 00000000..687d9b99
--- /dev/null
+++ b/samples/phase-17/1709/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1709 - Movement Models Lab
+
+Sample-local contract for `samples/phase-17/1709`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-17/1709/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `MovementModelsLabScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-17/1709` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-17` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-17/1709/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-17/1709/index.html
+samples/phase-17/1709
+1709
+```
+
+Phase folder:
+
+```text
+samples/phase-17
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-17/1710/README.md b/samples/phase-17/1710/README.md
new file mode 100644
index 00000000..395a8430
--- /dev/null
+++ b/samples/phase-17/1710/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1710 - Real Gameplay Mini-Game
+
+Sample-local contract for `samples/phase-17/1710`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-17/1710/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `RealGameplayMiniGameScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-17/1710` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-17` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-17/1710/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-17/1710/index.html
+samples/phase-17/1710
+1710
+```
+
+Phase folder:
+
+```text
+samples/phase-17
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-17/1711/README.md b/samples/phase-17/1711/README.md
new file mode 100644
index 00000000..2a4025a3
--- /dev/null
+++ b/samples/phase-17/1711/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1711 - Movement Models Lab
+
+Sample-local contract for `samples/phase-17/1711`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-17/1711/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `MovementModelsLabScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-17/1711` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-17` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-17/1711/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-17/1711/index.html
+samples/phase-17/1711
+1711
+```
+
+Phase folder:
+
+```text
+samples/phase-17
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-17/1712/README.md b/samples/phase-17/1712/README.md
new file mode 100644
index 00000000..046bcd57
--- /dev/null
+++ b/samples/phase-17/1712/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1712 - Gameplay Metrics & Telemetry
+
+Sample-local contract for `samples/phase-17/1712`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-17/1712/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `GameplayMetricsTelemetryScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-17/1712` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-17` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-17/1712/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-17/1712/index.html
+samples/phase-17/1712
+1712
+```
+
+Phase folder:
+
+```text
+samples/phase-17
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-17/1713/README.md b/samples/phase-17/1713/README.md
new file mode 100644
index 00000000..a461b7de
--- /dev/null
+++ b/samples/phase-17/1713/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1713 - Final Reference Game
+
+Sample-local contract for `samples/phase-17/1713`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-17/1713/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `FinalReferenceGameScene.js`
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-17/1713` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-17` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-17/1713/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-17/1713/index.html
+samples/phase-17/1713
+1713
+```
+
+Phase folder:
+
+```text
+samples/phase-17
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-18/1801/README.md b/samples/phase-18/1801/README.md
new file mode 100644
index 00000000..f16d514b
--- /dev/null
+++ b/samples/phase-18/1801/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1801 - Phase 18 Foundation
+
+Sample-local contract for `samples/phase-18/1801`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-18/1801/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `Phase18FoundationScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-18/1801` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-18` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-18/1801/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-18/1801/index.html
+samples/phase-18/1801
+1801
+```
+
+Phase folder:
+
+```text
+samples/phase-18
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-19/1901/README.md b/samples/phase-19/1901/README.md
new file mode 100644
index 00000000..69238e1f
--- /dev/null
+++ b/samples/phase-19/1901/README.md
@@ -0,0 +1,59 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1901 - Phase 19 Foundation
+
+Sample-local contract for `samples/phase-19/1901`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-19/1901/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+- `Phase19FoundationScene.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-19/1901` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-19` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-19/1901/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-19/1901/index.html
+samples/phase-19/1901
+1901
+```
+
+Phase folder:
+
+```text
+samples/phase-19
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/samples/phase-19/1902/README.md b/samples/phase-19/1902/README.md
new file mode 100644
index 00000000..36b51a48
--- /dev/null
+++ b/samples/phase-19/1902/README.md
@@ -0,0 +1,58 @@
+<!-- SAMPLE_LOCAL_CONTRACT_README -->
+# Sample 1902 - Workspace All Tools Integration
+
+Sample-local contract for `samples/phase-19/1902`.
+
+## Implementation Location
+
+- Entrypoint: `samples/phase-19/1902/index.html`
+- Sample implementation lives in this physical sample folder and its local subdirectories.
+- `index.html` is the only discovery and launch entrypoint for tooling. It may load local modules, shared sample helpers, or engine modules, but Preview Generator V2 should launch this sample through `index.html` rather than assuming another file.
+- If `index.html` redirects, forwards, or bypasses into other code, that behavior must remain explicit in `index.html` so folder enumeration still has one stable launch point.
+
+Current `index.html` script launches:
+- `/samples/shared/sampleDetailPageEnhancement.js`
+- `./main.js`
+- `../../../src/engine/theme/mount-shared-header.js`
+
+Local implementation files currently present outside `assets/images`:
+- `index.html`
+- `main.js`
+
+## Discovery Contract
+
+- This sample is valid because the physical folder `samples/phase-19/1902` exists and contains `index.html`.
+- Preview Generator V2 discovers samples by enumerating directories under `samples/phase-19` and keeping only four-digit child folders that contain `index.html`.
+- Do not infer this sample from numeric ranges, phase counts, metadata-only references, or planned IDs.
+- If this folder or its `index.html` is missing, tooling must log `SKIP`, not `FAIL`.
+- Use `FAIL` only after an existing discovered sample cannot be launched, rendered, captured, or written.
+
+## Assets
+
+- Preview and image assets belong under `samples/phase-19/1902/assets/images`.
+- Asset folder status: present.
+
+## Preview Generator V2 Inputs
+
+Single sample:
+
+```text
+samples/phase-19/1902/index.html
+samples/phase-19/1902
+1902
+```
+
+Phase folder:
+
+```text
+samples/phase-19
+```
+
+Games target examples:
+
+```text
+games/<gamename>/index.html
+<gamename>
+```
+
+Games are separate targets and are not discovered from the samples tree.
diff --git a/tools/preview-generator-v2/index.html b/tools/preview-generator-v2/index.html
index 12e918d2..ecdc7c5c 100644
--- a/tools/preview-generator-v2/index.html
+++ b/tools/preview-generator-v2/index.html
@@ -732,26 +732,30 @@ tools\&lt;toolname&gt;\index.html
       async function captureFullScreenCanvas(frameDoc) {
         const html2canvasFn = window.html2canvas;
         if (typeof html2canvasFn !== "function") {
-          return frameDoc.querySelector("canvas");
+          throw new Error("Full Screen capture requires html2canvas. Use Canvas Only or ensure html2canvas loads before using Full Screen (1600x900 HTML Page).");
         }

         const frameBody = frameDoc.body;
         if (!frameBody) {
-          return null;
+          throw new Error("Full Screen capture failed because the target document has no body.");
         }

-        return await html2canvasFn(frameBody, {
-          backgroundColor: "#000000",
-          useCORS: true,
-          allowTaint: true,
-          logging: false,
-          width: 1600,
-          height: 900,
-          windowWidth: 1600,
-          windowHeight: 900,
-          scrollX: 0,
-          scrollY: 0
-        });
+        try {
+          return await html2canvasFn(frameBody, {
+            backgroundColor: "#000000",
+            useCORS: true,
+            allowTaint: true,
+            logging: false,
+            width: 1600,
+            height: 900,
+            windowWidth: 1600,
+            windowHeight: 900,
+            scrollX: 0,
+            scrollY: 0
+          });
+        } catch (error) {
+          throw new Error(`Full Screen capture failed at 1600x900. html2canvas may not support CSS color functions such as color(...). Use Canvas Only or replace unsupported CSS before fullscreen capture. ${error.message}`);
+        }
       }

       async function attemptCapture() {
@@ -789,7 +793,12 @@ tools\&lt;toolname&gt;\index.html

           drawSourceCanvas(sourceCanvas);
           setStatus("ready");
-        } catch (_) {
+        } catch (error) {
+          if (runtimeCaptureMode === "fullscreen1600") {
+            fail(error.message);
+            return;
+          }
+
           if (Date.now() - startTime > timeoutMs) {
             fail("Capture timeout");
             return;
@@ -1280,15 +1289,18 @@ tools\&lt;toolname&gt;\index.html
     async function extractFullPageSvg1600(doc) {
       const win = frame.contentWindow;
       if (!win) {
-        return buildFallbackSvg(CAPTURE_TIMEOUT_MARKER);
+        throw new Error("Full Screen capture failed because the target frame window is unavailable.");
       }

       const html2canvasFn = window.html2canvas || win.html2canvas;
       if (typeof html2canvasFn !== "function") {
-        return extractBestToolFallbackSvg(doc);
+        throw new Error("Full Screen capture requires html2canvas. Use Canvas Only or ensure html2canvas loads before using Full Screen (1600x900 HTML Page).");
       }

       const body = doc.body;
+      if (!body) {
+        throw new Error("Full Screen capture failed because the target document has no body.");
+      }
       const fullWidth = 1600;
       const fullHeight = 900;

@@ -1311,8 +1323,7 @@ tools\&lt;toolname&gt;\index.html

         return buildCanvasWrappedSvg(canvas);
       } catch (error) {
-        log(`WARN tool full-page capture failed, using fallback (${error.message})`);
-        return extractBestToolFallbackSvg(doc);
+        throw new Error(`Full Screen capture failed at 1600x900. html2canvas may not support CSS color functions such as color(...). Use Canvas Only or replace unsupported CSS before fullscreen capture. ${error.message}`);
       }
     }

@@ -1327,6 +1338,9 @@ tools\&lt;toolname&gt;\index.html
     async function extractSvgFromFrame(entry) {
       const doc = frame.contentDocument;
       if (!doc) {
+        if (getSelectedCaptureMode() === "fullscreen1600") {
+          throw new Error("Full Screen capture failed because the target frame document is unavailable.");
+        }
         return buildFallbackSvg(CAPTURE_TIMEOUT_MARKER);
       }
