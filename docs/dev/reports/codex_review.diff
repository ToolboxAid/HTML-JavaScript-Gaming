# git diff --cached -- PR scoped files
diff --git a/docs/dev/codex_commands.md b/docs/dev/codex_commands.md
index 085a716a..eb4cb2be 100644
--- a/docs/dev/codex_commands.md
+++ b/docs/dev/codex_commands.md
@@ -1,29 +1,49 @@
-# Codex Commands - PR_26126_030-preview-generator-v2-status-header-and-fullscreen-css-fix
+# Codex Commands - PR_26126_031-preview-generator-v2-left-accordion-and-preview-size

 ```bash
-codex run "Create PR_26126_030-preview-generator-v2-status-header-and-fullscreen-css-fix. Fix Preview Generator V2 UI/capture only. Preserve existing generation behavior. Status must not render accordion affordances; remove the \"+\" from the Status header. Keep Clear on the same line as Status, but render it as a normal button, not accordion UI. Fix Full Screen capture failure caused by unsupported CSS color(...) values by sanitizing/normalizing unsupported CSS color functions only for the cloned capture DOM before html2canvas runs. Do not change live page styling. Do not fall back silently. Full Screen should succeed when the only blocker is unsupported CSS color(...). Preserve Canvas Only behavior. Do not modify samples. Do not add schema. Produce review artifacts."
+codex run "Create PR_26126_031-preview-generator-v2-left-accordion-and-preview-size. Fix Preview Generator V2 UI only. Preserve existing generation behavior. Add working accordion behavior to the \"Paths or IDs\" section and the \"Last Generated Image\" section in the left column (expand/collapse with proper icons and no dead controls). Increase the display height of the Last Generated Image area by 2.5x using stylesheet classes (no inline styles). Keep Clear/Status behavior as-is. Do not modify samples. Do not add schema. Produce review artifacts."
 ```

 ## Validation Commands

-```bash
-rg -n "statusAccordion|Status|accordion-v2__icon|preview-generator-v2__status-header-row|preview-generator-v2__status-content|replaceUnsupportedColorFunctions|sanitizeComputedColorStyles|sanitizeClonedToolDocument|onclone" tools/preview-generator-v2/index.html
-node -e "const fs=require('fs');const html=fs.readFileSync('tools/preview-generator-v2/index.html','utf8');const status=html.match(/<section id=\"statusAccordion\"[\\s\\S]*?<\\/section>/)?.[0]||'';if(!status)throw new Error('Missing status section');if(status.includes('accordion-v2__icon')||status.includes('accordion-v2__header'))throw new Error('Status still has accordion affordance markup');console.log('status section has no accordion affordance');"
-node -e "const fs=require('fs');const html=fs.readFileSync('tools/preview-generator-v2/index.html','utf8');const scripts=[...html.matchAll(/<script(?![^>]*\\bsrc=)[^>]*>([\\s\\S]*?)<\\/script>/gi)].map(m=>m[1]);scripts.forEach((script,i)=>{new Function(script);console.log('inline script '+(i+1)+' syntax ok');});"
-git diff --check
-git diff --name-only -- '*.json' tools/shared tools/schemas start_of_day
+```powershell
+@'
+const fs = require('fs');
+const html = fs.readFileSync('tools/preview-generator-v2/index.html', 'utf8');
+['id="pathsOrIdsTitle"', 'aria-controls="pathsOrIdsContent"', 'id="lastGeneratedImageTitle"', 'aria-controls="lastGeneratedImageContent"', 'accordion-v2__icon'].forEach((token) => {
+  if (!html.includes(token)) throw new Error(`Missing accordion token: ${token}`);
+});
+console.log('preview generator center accordions present');
+'@ | node -
+@'
+const fs = require('fs');
+const html = fs.readFileSync('tools/preview-generator-v2/index.html', 'utf8');
+if (!/\.preview-generator-v2__last-generated-empty\s*\{[\s\S]*?min-height:\s*300px;/.test(html)) throw new Error('Last generated empty area is not 2.5x height');
+if (!/\.preview-generator-v2__last-generated-frame\s*\{[\s\S]*?min-height:\s*300px;[\s\S]*?max-height:\s*550px;/.test(html)) throw new Error('Last generated frame sizing is not 2.5x');
+if (!/\.preview-generator-v2__last-generated-frame img\s*\{[\s\S]*?max-height:\s*550px;/.test(html)) throw new Error('Last generated image max-height is not 2.5x');
+console.log('last generated sizing ok');
+'@ | node -
+@'
+const fs = require('fs');
+const html = fs.readFileSync('tools/preview-generator-v2/index.html', 'utf8');
+const scripts = [...html.matchAll(/<script(?![^>]*\bsrc=)[^>]*>([\s\S]*?)<\/script>/gi)].map((match) => match[1]);
+scripts.forEach((script, index) => {
+  new Function(script);
+  console.log(`inline script ${index + 1} syntax ok`);
+});
+'@ | node -
+git diff --check -- tools/preview-generator-v2/index.html docs/dev/codex_commands.md docs/dev/commit_comment.txt docs/dev/reports/codex_review.diff docs/dev/reports/codex_changed_files.txt
 npm run test:workspace-v2
-npm test
 ```

 ## Playwright

-No Playwright test was added. This PR changes Preview Generator V2 status header markup and cloned-DOM fullscreen capture sanitization only. `npm run test:workspace-v2` is attempted as the standard command if available.
+No new Playwright test was added. This PR changes Preview Generator V2 accordion markup and CSS sizing only; the existing `accordionV2` behavior is consumed by the new sections. `npm run test:workspace-v2` is attempted as the standard command if available.

 ## Test Notes

-`npm run test:workspace-v2` is not defined in the current `package.json`. `npm test` was attempted and still fails in the existing shared extraction guard baseline, outside this PR scope.
+`npm run test:workspace-v2` is not defined in the current `package.json`.

 ## Manual Test

-Open Preview Generator V2 and confirm Status has no accordion plus/minus affordance, with Clear on the same line. Run Full Screen capture against a page whose only blocker is CSS `color(...)` and confirm it no longer fails for that parser error. Recheck Canvas Only capture still works.
+Open Preview Generator V2 and confirm `Paths or IDs` and `Last Generated Image` expand/collapse with the same accordion behavior and icons as the other sections. Confirm Status/Clear behavior is unchanged and the Last Generated Image display area is visibly taller before and after generating a preview.
diff --git a/docs/dev/commit_comment.txt b/docs/dev/commit_comment.txt
index e10b993f..f97eb7c3 100644
--- a/docs/dev/commit_comment.txt
+++ b/docs/dev/commit_comment.txt
@@ -1 +1 @@
-Remove status accordion affordance and sanitize fullscreen capture CSS colors - PR_26126_030-preview-generator-v2-status-header-and-fullscreen-css-fix
+Add Preview Generator V2 center accordions and enlarge last generated image display - PR_26126_031-preview-generator-v2-left-accordion-and-preview-size
diff --git a/tools/preview-generator-v2/index.html b/tools/preview-generator-v2/index.html
index cd10cc2f..2ea57a2e 100644
--- a/tools/preview-generator-v2/index.html
+++ b/tools/preview-generator-v2/index.html
@@ -41,6 +41,7 @@
     }

     .preview-generator-v2__left-accordion,
+    .preview-generator-v2__center-accordion,
     .preview-generator-v2__right-accordion {
       --accordion-v2-accent: var(--pm-accent);
       --accordion-v2-line: var(--pm-line);
@@ -145,18 +146,12 @@
     }

     .preview-generator-v2__last-generated-section {
-      flex: 0 0 auto;
+      flex: 1 1 0;
       min-width: 0;
-      display: grid;
-      gap: 8px;
-      border: 1px solid var(--pm-line, rgba(221, 214, 254, 0.26));
-      border-radius: 8px;
-      background: var(--pm-surface, rgba(18, 16, 28, 0.78));
-      padding: 10px;
     }

     .preview-generator-v2__last-generated-empty {
-      min-height: 120px;
+      min-height: 300px;
       display: grid;
       place-items: center;
       border: 1px dashed var(--pm-line, rgba(221, 214, 254, 0.26));
@@ -182,8 +177,8 @@
     }

     .preview-generator-v2__last-generated-frame {
-      min-height: 120px;
-      max-height: 220px;
+      min-height: 300px;
+      max-height: 550px;
       display: grid;
       place-items: center;
       overflow: hidden;
@@ -194,7 +189,7 @@

     .preview-generator-v2__last-generated-frame img {
       max-width: 100%;
-      max-height: 220px;
+      max-height: 550px;
       object-fit: contain;
       display: block;
     }
@@ -485,8 +480,13 @@
       </aside>

       <main class="palette-manager-v2__panel palette-manager-v2__panel--center preview-generator-v2__center-panel">
-        <h2 id="pathsOrIdsTitle" class="preview-generator-v2__section-heading">Paths or IDs</h2>
-        <textarea id="sampleList" class="preview-generator-v2__paths-input" aria-labelledby="pathsOrIdsTitle" placeholder="Games examples:
+        <section class="accordion-v2 preview-generator-v2__center-accordion is-open" data-accordion-v2-open="true">
+          <button id="pathsOrIdsTitle" class="accordion-v2__header" type="button" aria-expanded="true" aria-controls="pathsOrIdsContent">
+            <span>Paths or IDs</span>
+            <span class="accordion-v2__icon" aria-hidden="true">+</span>
+          </button>
+          <div id="pathsOrIdsContent" class="accordion-v2__content">
+            <textarea id="sampleList" class="preview-generator-v2__paths-input" aria-labelledby="pathsOrIdsTitle" placeholder="Games examples:
 games\&lt;gamename&gt;\index.html
 &lt;gamename&gt;

@@ -500,15 +500,22 @@ samples/phase-13/1303/index.html
 Tools examples:
 tools\&lt;toolname&gt;\index.html
 &lt;toolname&gt;"></textarea>
-        <section id="lastGeneratedImageSection" class="preview-generator-v2__last-generated-section" aria-labelledby="lastGeneratedImageTitle">
-          <h2 id="lastGeneratedImageTitle" class="preview-generator-v2__section-heading">Last Generated Image</h2>
-          <div id="lastGeneratedImageEmpty" class="preview-generator-v2__last-generated-empty">No preview generated yet.</div>
-          <figure id="lastGeneratedImagePreview" class="preview-generator-v2__last-generated-preview" hidden>
-            <div class="preview-generator-v2__last-generated-frame">
-              <img id="lastGeneratedImage" alt="Most recently generated preview" />
-            </div>
-            <figcaption id="lastGeneratedImageMeta">No preview generated yet.</figcaption>
-          </figure>
+          </div>
+        </section>
+        <section id="lastGeneratedImageSection" class="accordion-v2 preview-generator-v2__center-accordion preview-generator-v2__last-generated-section is-open" data-accordion-v2-open="true" aria-labelledby="lastGeneratedImageTitle">
+          <button id="lastGeneratedImageTitle" class="accordion-v2__header" type="button" aria-expanded="true" aria-controls="lastGeneratedImageContent">
+            <span>Last Generated Image</span>
+            <span class="accordion-v2__icon" aria-hidden="true">+</span>
+          </button>
+          <div id="lastGeneratedImageContent" class="accordion-v2__content">
+            <div id="lastGeneratedImageEmpty" class="preview-generator-v2__last-generated-empty">No preview generated yet.</div>
+            <figure id="lastGeneratedImagePreview" class="preview-generator-v2__last-generated-preview" hidden>
+              <div class="preview-generator-v2__last-generated-frame">
+                <img id="lastGeneratedImage" alt="Most recently generated preview" />
+              </div>
+              <figcaption id="lastGeneratedImageMeta">No preview generated yet.</figcaption>
+            </figure>
+          </div>
         </section>
       </main>


# git diff -- PR scoped files
diff --git a/docs/dev/codex_commands.md b/docs/dev/codex_commands.md
index eb4cb2be..27079503 100644
--- a/docs/dev/codex_commands.md
+++ b/docs/dev/codex_commands.md
@@ -1,7 +1,7 @@
-# Codex Commands - PR_26126_031-preview-generator-v2-left-accordion-and-preview-size
+# Codex Commands - PR_26126_033-preview-generator-v2-paths-stretch-layout

 ```bash
-codex run "Create PR_26126_031-preview-generator-v2-left-accordion-and-preview-size. Fix Preview Generator V2 UI only. Preserve existing generation behavior. Add working accordion behavior to the \"Paths or IDs\" section and the \"Last Generated Image\" section in the left column (expand/collapse with proper icons and no dead controls). Increase the display height of the Last Generated Image area by 2.5x using stylesheet classes (no inline styles). Keep Clear/Status behavior as-is. Do not modify samples. Do not add schema. Produce review artifacts."
+codex run "Create PR_26126_033-preview-generator-v2-paths-stretch-layout. Fix Preview Generator V2 layout only. Preserve existing generation behavior. In the left column, make the \"Paths or IDs\" accordion section stretch to fill all remaining vertical space down to the bottom of the panel above \"Last Generated Image\". Implement using flex layout: left panel column must be a flex container with column direction; set the Paths or IDs section to flex-grow:1 and its textarea to height:100% (or flex:1) so it expands fully. Do not use inline styles. Keep Last Generated Image fixed height below it. Do not modify samples. Do not add schema. Produce review artifacts."
 ```

 ## Validation Commands
@@ -10,18 +10,20 @@ codex run "Create PR_26126_031-preview-generator-v2-left-accordion-and-preview-s
 @'
 const fs = require('fs');
 const html = fs.readFileSync('tools/preview-generator-v2/index.html', 'utf8');
-['id="pathsOrIdsTitle"', 'aria-controls="pathsOrIdsContent"', 'id="lastGeneratedImageTitle"', 'aria-controls="lastGeneratedImageContent"', 'accordion-v2__icon'].forEach((token) => {
-  if (!html.includes(token)) throw new Error(`Missing accordion token: ${token}`);
-});
-console.log('preview generator center accordions present');
-'@ | node -
-@'
-const fs = require('fs');
-const html = fs.readFileSync('tools/preview-generator-v2/index.html', 'utf8');
-if (!/\.preview-generator-v2__last-generated-empty\s*\{[\s\S]*?min-height:\s*300px;/.test(html)) throw new Error('Last generated empty area is not 2.5x height');
-if (!/\.preview-generator-v2__last-generated-frame\s*\{[\s\S]*?min-height:\s*300px;[\s\S]*?max-height:\s*550px;/.test(html)) throw new Error('Last generated frame sizing is not 2.5x');
-if (!/\.preview-generator-v2__last-generated-frame img\s*\{[\s\S]*?max-height:\s*550px;/.test(html)) throw new Error('Last generated image max-height is not 2.5x');
-console.log('last generated sizing ok');
+const required = [
+  '.preview-generator-v2__center-panel .preview-generator-v2__paths-section',
+  'flex: 1 1 auto;',
+  '.preview-generator-v2__paths-section .accordion-v2__content',
+  'height: 100%;',
+  '.preview-generator-v2__center-panel .preview-generator-v2__last-generated-section',
+  'flex: 0 0 auto;',
+  'preview-generator-v2__paths-section is-open'
+];
+for (const token of required) {
+  if (!html.includes(token)) throw new Error(`Missing layout token: ${token}`);
+}
+if (/<[^>]+style=/.test(html)) throw new Error('Inline style attribute found');
+console.log('paths stretch layout tokens ok');
 '@ | node -
 @'
 const fs = require('fs');
@@ -38,7 +40,7 @@ npm run test:workspace-v2

 ## Playwright

-No new Playwright test was added. This PR changes Preview Generator V2 accordion markup and CSS sizing only; the existing `accordionV2` behavior is consumed by the new sections. `npm run test:workspace-v2` is attempted as the standard command if available.
+No new Playwright test was added. This PR changes Preview Generator V2 layout CSS/section classing only, with existing `accordionV2` behavior unchanged. `npm run test:workspace-v2` is attempted as the standard command if available.

 ## Test Notes

@@ -46,4 +48,4 @@ No new Playwright test was added. This PR changes Preview Generator V2 accordion

 ## Manual Test

-Open Preview Generator V2 and confirm `Paths or IDs` and `Last Generated Image` expand/collapse with the same accordion behavior and icons as the other sections. Confirm Status/Clear behavior is unchanged and the Last Generated Image display area is visibly taller before and after generating a preview.
+Open Preview Generator V2 and confirm the `Paths or IDs` accordion stretches through the available column height above `Last Generated Image`. Confirm the `Paths or IDs` textarea grows with the section, `Last Generated Image` stays fixed below it, and generation behavior is unchanged.
diff --git a/docs/dev/commit_comment.txt b/docs/dev/commit_comment.txt
index f97eb7c3..4e8d9e53 100644
--- a/docs/dev/commit_comment.txt
+++ b/docs/dev/commit_comment.txt
@@ -1 +1 @@
-Add Preview Generator V2 center accordions and enlarge last generated image display - PR_26126_031-preview-generator-v2-left-accordion-and-preview-size
+Stretch Preview Generator V2 Paths accordion above fixed Last Generated Image - PR_26126_033-preview-generator-v2-paths-stretch-layout
diff --git a/tools/preview-generator-v2/index.html b/tools/preview-generator-v2/index.html
index 2ea57a2e..4974c3f2 100644
--- a/tools/preview-generator-v2/index.html
+++ b/tools/preview-generator-v2/index.html
@@ -40,6 +40,16 @@
       overflow: hidden;
     }

+    .preview-generator-v2__center-panel .preview-generator-v2__paths-section {
+      flex: 1 1 auto;
+      min-height: 0;
+    }
+
+    .preview-generator-v2__paths-section .accordion-v2__content {
+      flex: 1 1 auto;
+      min-height: 0;
+    }
+
     .preview-generator-v2__left-accordion,
     .preview-generator-v2__center-accordion,
     .preview-generator-v2__right-accordion {
@@ -138,6 +148,7 @@
     .preview-generator-v2__paths-input {
       flex: 1 1 auto;
       min-height: 0;
+      height: 100%;
       width: 100%;
       resize: none;
       overflow: auto;
@@ -145,8 +156,8 @@
       font-family: ui-monospace, SFMono-Regular, Consolas, "Liberation Mono", monospace;
     }

-    .preview-generator-v2__last-generated-section {
-      flex: 1 1 0;
+    .preview-generator-v2__center-panel .preview-generator-v2__last-generated-section {
+      flex: 0 0 auto;
       min-width: 0;
     }

@@ -480,7 +491,7 @@
       </aside>

       <main class="palette-manager-v2__panel palette-manager-v2__panel--center preview-generator-v2__center-panel">
-        <section class="accordion-v2 preview-generator-v2__center-accordion is-open" data-accordion-v2-open="true">
+        <section class="accordion-v2 preview-generator-v2__center-accordion preview-generator-v2__paths-section is-open" data-accordion-v2-open="true">
           <button id="pathsOrIdsTitle" class="accordion-v2__header" type="button" aria-expanded="true" aria-controls="pathsOrIdsContent">
             <span>Paths or IDs</span>
             <span class="accordion-v2__icon" aria-hidden="true">+</span>
