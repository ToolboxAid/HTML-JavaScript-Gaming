# git diff -- . excluding generated review artifact files

diff --git a/docs/dev/codex_commands.md b/docs/dev/codex_commands.md
index eef9830c..0a97a770 100644
--- a/docs/dev/codex_commands.md
+++ b/docs/dev/codex_commands.md
@@ -1,77 +1,35 @@
-# Codex Commands - PR_26126_035-preview-generator-v2-file-split-and-control-classes
+# Codex Commands - PR_26126_036-preview-generator-v2-shared-dependency-and-style-audit
 
 ```bash
-codex run "Create PR_26126_035-preview-generator-v2-file-split-and-control-classes. Fix Preview Generator V2 structure only. Preserve current working behavior exactly. Remove all inline style blocks and inline script blocks from tools/preview-generator-v2/index.html. Move CSS into external stylesheet files. Move JavaScript into external module files. Use one class per JavaScript file. Give each UI control/section its own class. Keep a small app/bootstrap entry file only for wiring classes together. Preserve existing IDs and DOM behavior unless a rename is required for correctness. Do not rewrite generation logic. Do not add schema. Do not modify samples. Do not modify start_of_day folders. Produce review artifacts and class ownership notes."
+codex run "Create PR_26126_036-preview-generator-v2-shared-dependency-and-style-audit. Audit Preview Generator V2 structure and validation only. Verify tools/shared dependencies, report common CSS declaration candidates by comparing declaration bodies, verify Playwright launches Preview Generator V2, repair minimal Playwright coverage only if needed, do not modify samples, do not add schema, do not modify start_of_day folders, and produce required review artifacts."
 ```
 
 ## Validation Commands
 
 ```powershell
+rg -n "tools/shared|../shared|../../tools/shared|platformShell|shared/" tools/preview-generator-v2 tests/playwright/PreviewGeneratorV2Baseline.spec.mjs
+if ($LASTEXITCODE -eq 1) { Write-Output "no tools/shared references found"; $global:LASTEXITCODE = 0 }
+rg -n "<link|<script|import .* from" tools/preview-generator-v2 tests/playwright/PreviewGeneratorV2Baseline.spec.mjs
+
 $files = Get-ChildItem -Path tools/preview-generator-v2 -Recurse -Filter *.js | Select-Object -ExpandProperty FullName
 foreach ($file in $files) {
   node --check $file
   if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
 }
-@'
-const fs = require('fs');
-const html = fs.readFileSync('tools/preview-generator-v2/index.html', 'utf8');
-if (/<style\b/i.test(html) || /<\/style>/i.test(html)) throw new Error('Inline style block remains');
-const inlineScripts = [...html.matchAll(/<script(?![^>]*\bsrc=)[^>]*>/gi)];
-if (inlineScripts.length) throw new Error(`Inline script block remains: ${inlineScripts.length}`);
-if (!html.includes('./previewGeneratorV2.css')) throw new Error('External CSS link missing');
-if (!html.includes('./previewGeneratorV2.bootstrap.js')) throw new Error('Bootstrap module missing');
-console.log('index has no inline style/script blocks');
-'@ | node -
-@'
-const fs = require('fs');
-const path = require('path');
-const root = 'tools/preview-generator-v2';
-const files = [];
-function walk(dir) {
-  for (const entry of fs.readdirSync(dir, { withFileTypes: true })) {
-    const full = path.join(dir, entry.name);
-    if (entry.isDirectory()) walk(full);
-    if (entry.isFile() && entry.name.endsWith('.js')) files.push(full);
-  }
-}
-walk(root);
-for (const file of files) {
-  const text = fs.readFileSync(file, 'utf8');
-  const classes = [...text.matchAll(/\bclass\s+[A-Za-z0-9_]+/g)].map(match => match[0]);
-  if (classes.length > 1) throw new Error(`${file} has more than one class: ${classes.join(', ')}`);
-}
-console.log('one class per js file check ok');
-'@ | node -
-@'
-const fs = require('fs');
-const path = require('path');
-const root = 'tools/preview-generator-v2';
-const files = [];
-function walk(dir) {
-  for (const entry of fs.readdirSync(dir, { withFileTypes: true })) {
-    const full = path.join(dir, entry.name);
-    if (entry.isDirectory()) walk(full);
-    if (entry.isFile() && entry.name.endsWith('.js')) files.push(full);
-  }
-}
-walk(root);
-for (const file of files) {
-  const text = fs.readFileSync(file, 'utf8');
-  for (const match of text.matchAll(/from\s+["'](.+?)["']/g)) {
-    const target = path.resolve(path.dirname(file), match[1]);
-    if (!fs.existsSync(target)) throw new Error(`${file} imports missing ${match[1]}`);
-  }
-}
-console.log('local module imports resolve');
-'@ | node -
+node --check tests/playwright/PreviewGeneratorV2Baseline.spec.mjs
+npx playwright test tests/playwright/PreviewGeneratorV2Baseline.spec.mjs --project=playwright --reporter=list
 git status --short -- samples tools/schemas start_of_day
-git diff --check -- tools/preview-generator-v2 docs/dev/codex_commands.md docs/dev/commit_comment.txt docs/dev/reports/preview_generator_v2_class_responsibilities.md docs/dev/reports/codex_review.diff docs/dev/reports/codex_changed_files.txt
+git diff --check -- tools/preview-generator-v2 tests/playwright docs/dev/codex_commands.md docs/dev/commit_comment.txt docs/dev/reports/preview_generator_v2_shared_dependency_audit.txt docs/dev/reports/preview_generator_v2_common_style_candidates.txt docs/dev/reports/preview_generator_v2_playwright_result.txt
 npm run test:workspace-v2
 ```
 
 ## Playwright
 
-No new Playwright test was added. This PR is a file split and class ownership cleanup for Preview Generator V2 only; existing behavior is intended to remain unchanged. `npm run test:workspace-v2` is attempted as the standard command if available.
+Playwright validates that Preview Generator V2 launches, the main shell/menu/status controls render, Generate Preview and Stop start disabled, Games is the default target source, and a working accordion collapses/reopens without page errors.
+
+Expected pass behavior: the targeted Playwright test passes with one launched tool page.
+
+Expected fail behavior: the test fails if the tool cannot load, controls are missing, the default target source changes unexpectedly, accordion behavior is broken, or page errors are emitted.
 
 ## Test Notes
 
@@ -79,4 +37,6 @@ No new Playwright test was added. This PR is a file split and class ownership cl
 
 ## Manual Test
 
-Open Preview Generator V2 and confirm the page loads with the same layout and controls. Pick a repo, verify Generate Preview gating, run a small known path, use Stop/Clear, and confirm Last Generated Image and Output Summary update as before.
+Open `tools/preview-generator-v2/index.html`, confirm the tool shell loads, toggle Repo Destination, confirm Generate Preview is visible but disabled before required fields are complete, and verify Status/Clear remain visible.
+
+Full samples smoke test was skipped because this PR does not modify samples or shared sample loading.
diff --git a/docs/dev/commit_comment.txt b/docs/dev/commit_comment.txt
index d7c819ed..cff6b294 100644
--- a/docs/dev/commit_comment.txt
+++ b/docs/dev/commit_comment.txt
@@ -1 +1 @@
-Split Preview Generator V2 files and add per-section control classes - PR_26126_035-preview-generator-v2-file-split-and-control-classes
+Audit Preview Generator V2 dependencies and add launch baseline - PR_26126_036-preview-generator-v2-shared-dependency-and-style-audit

# git diff --no-index -- /dev/null docs/dev/reports/preview_generator_v2_common_style_candidates.txt

diff --git a/docs/dev/reports/preview_generator_v2_common_style_candidates.txt b/docs/dev/reports/preview_generator_v2_common_style_candidates.txt
new file mode 100644
index 00000000..dd06b337
--- /dev/null
+++ b/docs/dev/reports/preview_generator_v2_common_style_candidates.txt
@@ -0,0 +1,96 @@
+# Preview Generator V2 Common Style Candidates - PR_26126_036
+
+Scope:
+- src/engine/theme/main.css
+- src/engine/ui/hubCommon.css
+- src/engine/theme/accordionV2/accordionV2.css
+- tools/palette-manager-v2/paletteManagerV2.css
+- tools/preview-generator-v2/previewGeneratorV2.css
+
+Method:
+- Parsed CSS declaration blocks inside braces.
+- Normalized declaration contents by sorting declarations and normalizing whitespace.
+- Compared declaration bodies, not selector names.
+- Reported exact duplicate bodies and near-duplicate bodies involving Preview Generator V2 or its immediate layout base.
+
+No consolidation was applied in this PR. These are candidates only because consolidation would affect shared layout/style behavior across tools.
+
+Exact duplicate candidates:
+
+1. Header frame host sizing
+- Declaration body: display: block; margin: 10px auto; width: calc(100% - 32px)
+- tools/palette-manager-v2/paletteManagerV2.css:29 selector [data-palette-manager-v2-header], [data-palette-manager-v2-status]
+- tools/preview-generator-v2/previewGeneratorV2.css:15 selector [data-preview-generator-v2-header]
+- Candidate: common tool-local header frame host rule.
+
+2. Accordion color variable binding
+- Declaration body: --accordion-v2-accent: var(--pm-accent); --accordion-v2-line: var(--pm-line); --accordion-v2-surface: var(--pm-surface)
+- tools/palette-manager-v2/paletteManagerV2.css:248 selector .palette-manager-v2__panel--right .accordion-v2
+- tools/palette-manager-v2/paletteManagerV2.css:295 selector .palette-manager-v2__panel--left .accordion-v2, .palette-manager-v2__panel--center .accordion-v2
+- tools/preview-generator-v2/previewGeneratorV2.css:39 selector .preview-generator-v2__left-accordion, .preview-generator-v2__center-accordion, .preview-generator-v2__right-accordion
+- Candidate: theme-level accordionV2 palette binding.
+
+3. Fullscreen tool root sizing
+- Declaration body: display: flex; flex-direction: column; flex: 1 1 auto; margin: 0; max-width: none; min-height: 0; min-width: 0; overflow: hidden; padding: 8px 12px; width: 100%
+- tools/palette-manager-v2/paletteManagerV2.css:781 selector html:fullscreen body.tools-platform-surface[data-tool-id="palette-manager-v2"] > .palette-manager-v2, body.tools-platform-surface.tools-platform-fullscreen-active[data-tool-id="palette-manager-v2"] > .palette-manager-v2
+- tools/preview-generator-v2/previewGeneratorV2.css:271 selector html:fullscreen body.tools-platform-surface[data-tool-id="preview-generator-v2"] > .palette-manager-v2, body.tools-platform-surface.tools-platform-fullscreen-active[data-tool-id="preview-generator-v2"] > .palette-manager-v2
+- Candidate: common fullscreen tool root rule after Palette Manager/Preview Generator behavior is proven stable.
+
+4. Fullscreen three-column layout sizing
+- Declaration body: align-items: stretch; flex: 1 1 auto; grid-template-columns: 340px minmax(0, 1fr) 360px; height: 100%; min-height: 0; width: 100%
+- tools/palette-manager-v2/paletteManagerV2.css:795 selector html:fullscreen body.tools-platform-surface[data-tool-id="palette-manager-v2"] .palette-manager-v2__layout, body.tools-platform-surface.tools-platform-fullscreen-active[data-tool-id="palette-manager-v2"] .palette-manager-v2__layout
+- tools/preview-generator-v2/previewGeneratorV2.css:285 selector html:fullscreen body.tools-platform-surface[data-tool-id="preview-generator-v2"] .palette-manager-v2__layout, body.tools-platform-surface.tools-platform-fullscreen-active[data-tool-id="preview-generator-v2"] .palette-manager-v2__layout
+- Candidate: common fullscreen three-column shell rule.
+
+5. Fullscreen left/right panel sizing
+- Declaration body: align-self: stretch; height: 100%; max-height: none; position: static
+- tools/palette-manager-v2/paletteManagerV2.css:810 selector fullscreen palette left/right panels
+- tools/preview-generator-v2/previewGeneratorV2.css:300 selector fullscreen preview-generator left/right panels
+- Candidate: common fullscreen side-panel rule.
+
+6. Fullscreen side-panel anchoring
+- Declaration body: justify-self: start
+- tools/palette-manager-v2/paletteManagerV2.css:820 selector fullscreen palette left panel
+- tools/preview-generator-v2/previewGeneratorV2.css:310 selector fullscreen preview-generator left panel
+- Declaration body: justify-self: end
+- tools/palette-manager-v2/paletteManagerV2.css:825 selector fullscreen palette right panel
+- tools/preview-generator-v2/previewGeneratorV2.css:315 selector fullscreen preview-generator right panel
+- Candidate: common fullscreen side-panel alignment rules.
+
+7. Fullscreen center panel sizing
+- Declaration body: align-self: stretch; height: 100%; max-height: none; min-height: 0
+- tools/palette-manager-v2/paletteManagerV2.css:830 selector fullscreen palette center panel
+- tools/preview-generator-v2/previewGeneratorV2.css:320 selector fullscreen preview-generator center panel
+- Candidate: common fullscreen center-panel rule.
+
+8. Hidden element display
+- Declaration body: display: none
+- src/engine/theme/accordionV2/accordionV2.css:67 selector .accordion-v2__content[hidden]
+- tools/preview-generator-v2/previewGeneratorV2.css:161 selector .preview-generator-v2__last-generated-empty[hidden]
+- tools/preview-generator-v2/previewGeneratorV2.css:172 selector .preview-generator-v2__last-generated-preview[hidden]
+- tools/preview-generator-v2/previewGeneratorV2.css:220 selector .preview-generator-v2 .accordion-v2__content[hidden]
+- Candidate: review whether the local accordion hidden override is still needed; the figure/empty-state hidden rules may remain local because they target non-accordion elements.
+
+9. Preview-only flex growth duplication
+- Declaration body: flex: 1 1 auto; min-height: 0
+- tools/preview-generator-v2/previewGeneratorV2.css:29 selector .preview-generator-v2__center-panel .preview-generator-v2__paths-section
+- tools/preview-generator-v2/previewGeneratorV2.css:34 selector .preview-generator-v2__paths-section .accordion-v2__content
+- Candidate: local utility or grouped selector if a later style cleanup PR is approved.
+
+Near-duplicate candidates:
+
+1. Tool body shell
+- Similarity: 0.75 declaration overlap.
+- tools/palette-manager-v2/paletteManagerV2.css:1 body.tools-platform-tool-page[data-tool-id="palette-manager-v2"]
+- tools/preview-generator-v2/previewGeneratorV2.css:1 body.tools-platform-tool-page[data-tool-id="preview-generator-v2"]
+- Shared parts: flex column viewport shell, min-height, overflow behavior.
+- Preview-specific parts: color and background.
+- Candidate: common tool-page shell base with tool-specific theme decoration.
+
+2. Preview flex column surfaces
+- Similarity: 0.83 declaration overlap.
+- tools/preview-generator-v2/previewGeneratorV2.css:21 .preview-generator-v2__center-panel
+- tools/preview-generator-v2/previewGeneratorV2.css:211 .preview-generator-v2__status-content
+- Shared parts: flex column, flex grow, min-height, overflow hidden.
+- Difference: status content adds padding.
+- Candidate: local layout utility only if a later cleanup PR allows class-level consolidation.

# git diff --no-index -- /dev/null docs/dev/reports/preview_generator_v2_playwright_result.txt

diff --git a/docs/dev/reports/preview_generator_v2_playwright_result.txt b/docs/dev/reports/preview_generator_v2_playwright_result.txt
new file mode 100644
index 00000000..126fc303
--- /dev/null
+++ b/docs/dev/reports/preview_generator_v2_playwright_result.txt
@@ -0,0 +1,38 @@
+# Preview Generator V2 Playwright Result - PR_26126_036
+
+Coverage before this PR:
+- No existing Playwright spec under the configured Playwright roots launched tools/preview-generator-v2/index.html.
+- tests/playwright existed but contained no runnable spec files.
+- tests/ui existed but contained no runnable spec files.
+
+Coverage added:
+- tests/playwright/PreviewGeneratorV2Baseline.spec.mjs
+
+What the test validates:
+- Preview Generator V2 launches from /tools/preview-generator-v2/index.html through the repo HTTP server.
+- The Preview Generator V2 app shell is visible.
+- The Preview Generator V2 heading is visible.
+- menuSample nav exists.
+- Generate Preview and Stop buttons are visible and initially disabled.
+- Games is the default target source.
+- Paths or IDs input is visible.
+- Status block is visible.
+- Repo Destination accordion collapses and reopens with aria-expanded state changes.
+- No page errors are emitted during the launch/toggle path.
+
+Command run:
+- npx playwright test tests/playwright/PreviewGeneratorV2Baseline.spec.mjs --project=playwright --reporter=list
+
+Result:
+- PASS: 1 test passed.
+
+Output:
+- ok 1 [playwright] tests/playwright/PreviewGeneratorV2Baseline.spec.mjs Preview Generator V2 baseline launches the tool shell and toggles a working accordion
+- 1 passed
+
+Workspace V2 default command:
+- Command attempted: npm run test:workspace-v2
+- Result: FAIL, package.json does not define a test:workspace-v2 script.
+
+Full samples smoke test:
+- Skipped. This PR is a Preview Generator V2 audit/test-hook PR and does not modify samples or shared sample loading.

# git diff --no-index -- /dev/null docs/dev/reports/preview_generator_v2_shared_dependency_audit.txt

diff --git a/docs/dev/reports/preview_generator_v2_shared_dependency_audit.txt b/docs/dev/reports/preview_generator_v2_shared_dependency_audit.txt
new file mode 100644
index 00000000..22e8afec
--- /dev/null
+++ b/docs/dev/reports/preview_generator_v2_shared_dependency_audit.txt
@@ -0,0 +1,40 @@
+# Preview Generator V2 Shared Dependency Audit - PR_26126_036
+
+Scope:
+- tools/preview-generator-v2
+- tests/playwright/PreviewGeneratorV2Baseline.spec.mjs
+
+Method:
+- Searched for tools/shared references, shared path imports, platformShell references, CSS links, script references, and module imports.
+- Reviewed Preview Generator V2 shell behavior after PR_26126_035 file split.
+
+tools/shared result:
+- PASS: No tools/shared import was found.
+- PASS: No tools/shared script reference was found.
+- PASS: No tools/shared CSS reference was found.
+- PASS: No platformShell.css or platformShell.js reference was found.
+- PASS: No runtime dependency on tools/shared behavior was found.
+
+Search result:
+- Command: rg -n "tools/shared|../shared|../../tools/shared|platformShell|shared/" tools/preview-generator-v2 tests/playwright/PreviewGeneratorV2Baseline.spec.mjs
+- Result: no matches.
+
+Non-tools/shared dependencies observed:
+- tools/preview-generator-v2/index.html:8 links ../../src/engine/theme/main.css.
+- tools/preview-generator-v2/index.html:9 links ../../src/engine/ui/hubCommon.css.
+- tools/preview-generator-v2/index.html:10 links ../../src/engine/theme/accordionV2/accordionV2.css.
+- tools/preview-generator-v2/index.html:11 links ../palette-manager-v2/paletteManagerV2.css.
+- tools/preview-generator-v2/index.html:12 links ./previewGeneratorV2.css.
+- tools/preview-generator-v2/index.html:13 loads html2canvas from cdn.jsdelivr.net.
+- tools/preview-generator-v2/index.html:230 loads ./previewGeneratorV2.bootstrap.js.
+- tools/preview-generator-v2/index.html:231 loads ../../src/engine/theme/accordionV2/accordionV2.js.
+- tools/preview-generator-v2/index.html:232 loads ../../src/engine/theme/mount-shared-header.js.
+
+Runtime note:
+- The element id shared-theme-header is mounted by src/engine/theme/mount-shared-header.js, not by tools/shared.
+- PreviewGeneratorV2ShellControl owns the local Hide Header and Details behavior for this tool.
+- The tool still uses Palette Manager V2 CSS as an explicit visual base dependency; that is outside tools/shared and was not changed in this PR.
+
+Implementation changes from this audit:
+- No tool runtime behavior was changed.
+- A minimal Playwright launch baseline was added because no existing Playwright spec under the configured Playwright roots actually launched Preview Generator V2.

# git diff --no-index -- /dev/null tests/playwright/PreviewGeneratorV2Baseline.spec.mjs

diff --git a/tests/playwright/PreviewGeneratorV2Baseline.spec.mjs b/tests/playwright/PreviewGeneratorV2Baseline.spec.mjs
new file mode 100644
index 00000000..e6974f75
--- /dev/null
+++ b/tests/playwright/PreviewGeneratorV2Baseline.spec.mjs
@@ -0,0 +1,47 @@
+import { expect, test } from "@playwright/test";
+import { startRepoServer } from "../helpers/playwrightRepoServer.mjs";
+
+test.describe("Preview Generator V2 baseline", () => {
+  test("launches the tool shell and toggles a working accordion", async ({ page }) => {
+    const server = await startRepoServer();
+    const pageErrors = [];
+
+    page.on("pageerror", (error) => {
+      pageErrors.push(error.message);
+    });
+
+    try {
+      await page.goto(`${server.baseUrl}/tools/preview-generator-v2/index.html`, { waitUntil: "domcontentloaded" });
+
+      await expect(page.locator(".preview-generator-v2.app-shell")).toBeVisible();
+      await expect(page.locator("h1", { hasText: "Preview Generator V2" })).toBeVisible();
+      await expect(page.locator('nav[aria-label="menuSample"]')).toBeVisible();
+
+      await expect(page.locator("#executeBtn")).toBeVisible();
+      await expect(page.locator("#executeBtn")).toBeDisabled();
+      await expect(page.locator("#stopBtn")).toBeVisible();
+      await expect(page.locator("#stopBtn")).toBeDisabled();
+
+      await expect(page.locator("#targetTypeGames")).toBeChecked();
+      await expect(page.locator("#sampleList")).toBeVisible();
+      await expect(page.locator("#statusAccordion")).toBeVisible();
+
+      const repoHeader = page.locator('.accordion-v2__header[aria-controls="repoDestinationContent"]');
+      const repoContent = page.locator("#repoDestinationContent");
+      await expect(repoHeader).toBeVisible();
+      await expect(repoContent).toBeVisible();
+
+      await repoHeader.click();
+      await expect(repoContent).toBeHidden();
+      await expect(repoHeader).toHaveAttribute("aria-expanded", "false");
+
+      await repoHeader.click();
+      await expect(repoContent).toBeVisible();
+      await expect(repoHeader).toHaveAttribute("aria-expanded", "true");
+
+      expect(pageErrors).toEqual([]);
+    } finally {
+      await server.close();
+    }
+  });
+});
