# CODEX START OF DAY (UPDATED)

## Output Contract (MANDATORY)

All BUILD_PR executions MUST:
- Produce a delta ZIP at:
    /tmp/BUILD_PR_<NAME>_delta.zip
- Return ONLY after ZIP is created
- Not return partial or text-only responses
