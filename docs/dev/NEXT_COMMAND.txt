Create BUILD_PR_LEVEL_11_1_AUTHORITATIVE_STATE_HANDOFF_CANDIDATE as a docs-first, repo-structured delta. Use Level 10.7 docs as source of truth. Define one narrowly scoped authoritative handoff candidate, its ownership limits, contract tests, rollback gates, and one real consumer validation path without changing engine core APIs.
