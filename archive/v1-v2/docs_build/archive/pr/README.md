# Legacy PR History Archive

This directory stores historical PR markdown records moved out of the active `docs_build/pr/` folder.

## Policy
- Preserve filenames exactly.
- Use archive moves instead of deletion.
- Keep active working PR docs in `docs_build/pr/`.
- Do not place start-of-day files here.
