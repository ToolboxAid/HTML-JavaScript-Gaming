{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "src/shared/schemas/tools/audio-sfx-playground-v2.schema.json",
  "title": "Audio / SFX Playground V2 Schema",
  "description": "Exported toolState wrapper for Audio / SFX Playground V2. Saved sounds are the single source of truth; activeSoundId identifies the selected sound.",
  "type": "object",
  "required": ["$schema", "schema", "version", "toolId", "payload"],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "const": "src/shared/schemas/tools/audio-sfx-playground-v2.schema.json"
    },
    "schema": {
      "type": "string",
      "const": "html-js-gaming.tool-state"
    },
    "version": {
      "type": "integer",
      "minimum": 1
    },
    "toolId": {
      "type": "string",
      "const": "audio-sfx-playground-v2"
    },
    "payload": {
      "$ref": "#/$defs/audioSfxPayload"
    }
  },
  "$defs": {
    "audioSfxPayload": {
      "type": "object",
      "required": ["schema", "version", "toolId", "activeSoundId", "sounds"],
      "additionalProperties": false,
      "properties": {
        "schema": {
          "type": "string",
          "const": "html-js-gaming.audio-sfx-playground-v2"
        },
        "version": {
          "type": "integer",
          "minimum": 1
        },
        "toolId": {
          "type": "string",
          "const": "audio-sfx-playground-v2"
        },
        "activeSoundId": {
          "type": "string",
          "minLength": 1
        },
        "sounds": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/soundEntry"
          }
        }
      }
    },
    "soundEntry": {
      "type": "object",
      "required": ["id", "sound"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1
        },
        "sound": {
          "$ref": "#/$defs/sound"
        }
      }
    },
    "sound": {
      "type": "object",
      "required": ["attackMs", "durationMs", "frequencyHz", "name", "noise", "noiseAmount", "noiseDecayMs", "noiseFilterHz", "playbackMode", "pitchSweepCents", "releaseMs", "volume", "waveform"],
      "additionalProperties": false,
      "properties": {
        "attackMs": {
          "type": "integer",
          "minimum": 0,
          "maximum": 250
        },
        "durationMs": {
          "type": "integer",
          "minimum": 60,
          "maximum": 2000,
          "multipleOf": 5
        },
        "frequencyHz": {
          "type": "integer",
          "minimum": 20,
          "maximum": 20000
        },
        "name": {
          "type": "string",
          "minLength": 1
        },
        "noise": {
          "type": "boolean"
        },
        "noiseAmount": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "noiseDecayMs": {
          "type": "integer",
          "minimum": 20,
          "maximum": 600,
          "multipleOf": 5
        },
        "noiseFilterHz": {
          "type": "integer",
          "minimum": 400,
          "maximum": 9000,
          "multipleOf": 50
        },
        "playbackMode": {
          "type": "string",
          "enum": ["oneShot", "loop"]
        },
        "pitchSweepCents": {
          "type": "integer",
          "minimum": -1200,
          "maximum": 1200,
          "multipleOf": 5
        },
        "releaseMs": {
          "type": "integer",
          "minimum": 20,
          "maximum": 700,
          "multipleOf": 5
        },
        "volume": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "waveform": {
          "type": "string",
          "enum": ["sine", "square", "triangle", "sawtooth", "noise"]
        }
      }
    }
  }
}
