# Sample37 - Sprite Render Layer

## Purpose
Introduces a sprite-ready render path with safe fallback rendering.

## What it shows
- sprite-ready render path
- fallback color rendering
- label support
- render abstraction
- foundation for real sprite work

## Behavior
Entities can render using sprite-ready frame proxies or fallback colors through one shared render layer.
