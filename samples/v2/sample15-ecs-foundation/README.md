# Sample15 - ECS Foundation

## Purpose
Introduces a simple world object that stores entities and components in a reusable structure.

## What it shows
- entity ids
- component maps
- component lookup
- rendering from queried components
- minimal ECS layout

## Behavior
The sample creates a player entity and a wall entity, then renders both through shared component access.
