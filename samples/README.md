# Samples

Samples are the executable proof for Engine V2.

Rules:

- each sample demonstrates one main concern
- keep assets minimal
- avoid game-specific coupling
- prefer plain readable code over framework patterns

Current V2 samples:

- `v2/sample01-basic-loop/` - engine boot, scene lifecycle, fixed update, render interpolation
