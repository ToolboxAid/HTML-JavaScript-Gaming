Status legend:
- [ ] Todo
- [.] In progress
- [!] Blocker
- [-] Don't do
- [x] Complete
- [X] Complete

Fullscreen Gaming local plan

Goal:
Refactor this sample to follow the `Game Engine` template structure while keeping it focused on fullscreen behavior and diagnostics.

Local checklist:
[x] Keep the sample purpose narrow: fullscreen behavior first, template structure second
[x] Decide whether diagnostics stay DOM-based or move onto canvas, and document that choice
[x] Mirror the `Game Engine` template structure without adding fake gameplay concepts
[x] Keep fullscreen orchestration separated from diagnostics/render wiring
[x] Keep status/error/debug output minimal and user-safe
[x] Verify repeated enter/exit flow does not leak state/listeners
[x] Verify headers and sample docs stay in place after the refactor

Focused review checklist:
[x] Architecture: turn `game.js` into a lifecycle shell with state routing only
[x] Architecture: keep render/update work in the smallest number of modules the sample needs
[x] Architecture: isolate DOM metrics ownership from core loop logic
[x] Move-to-engine: identify reusable fullscreen or diagnostics helpers for engine extraction
[-] Move fullscreen sample helpers into `engine/` now
[x] Naming: keep state labels, metric labels, and control text consistent
[x] Safety: verify repeated interaction stays leak-free
[x] Center the sample layout cleanly on the page

Detail checklist:
[x] Move all sample configuration into `global.js` using template-style config objects
[x] Add explicit sample states such as `attract` and `playGame`
[x] Create a `stateHandlers` map in `game.js` like the `Game Engine` template
[x] Keep attract/render/diagnostics behavior in `game.js` after confirming `gameStates.js` is unnecessary
[x] Add `gameMetrics.js` or `gameDom.js` if DOM querying/updating needs its own module
[x] Remove inline styles from `index.html`
[x] Update `styles.css` to own layout and diagnostics presentation
[x] Verify `destroy()` still leaves no dangling references
[x] Update README with the refactored file structure and controls
[x] Add/validate tests only if the refactor introduces logic worth isolating

[x] Find dead code
Final:
[x] Run browser smoke pass for fullscreen enter/exit, diagnostics updates, and cleanup

Smoke pass steps:
1. Load `samples/visual/Fullscreen Gaming/index.html`.
2. Verify the attract/default state renders correctly before entering fullscreen.
3. Enter fullscreen using the provided control and verify canvas/layout scales correctly.
4. Exit fullscreen (`Esc` or control) and verify layout/state returns cleanly.
5. Repeat enter/exit multiple times and verify no stuck UI state or missed transitions.
6. Reload/navigate away and confirm no console errors related to fullscreen lifecycle cleanup.

Decision notes:
- Follow the `Game Engine` template structure, but keep this sample simpler than the template where gameplay-specific concepts do not help.
- Prefer DOM diagnostics unless there is a strong reason to move metrics onto the canvas.
- Any shared fullscreen UI glue should only move to engine if reused by multiple samples/games.
- The reusable starter/template stays under `samples/engine/`, not in the shared `engine/` runtime.
- Sample-level unit tests are optional; prefer browser smoke coverage unless new reusable logic is extracted.
- Repeated fullscreen enter/exit was verified by code inspection: `engine/core/fullscreen.js` registers listeners once and reuses them across toggles.
- Cleanup verification now includes the `GameBase.destroy()` path importing and calling `Fullscreen.destroy()` correctly.
