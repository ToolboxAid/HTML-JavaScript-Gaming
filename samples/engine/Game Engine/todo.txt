Status legend:
- [ ] Todo
- [.] In progress
- [!] Blocker
- [-] Don't do
- [x] Complete
- [X] Complete

Game Engine local plan

Goal:
Keep this sample as the canonical engine template for lifecycle and game flow.

Completed:
[x] Keep sample structure and docs aligned with the canonical engine template
[x] Keep lifecycle orchestration separate from UI/render wiring
[x] Verify naming, cleanup, restart flow, and page layout
[x] Review reusable helper candidates without moving template-only code into engine
[x] Update README and related starter docs

Final:
[x] Run browser smoke pass for lifecycle/update/render and cleanup

Smoke pass steps:
1. Load `samples/engine/Game Engine/index.html`.
2. Verify initialization path completes and sample enters steady game loop.
3. Verify expected update/render behavior and any sample controls.
4. Restart/reload and confirm lifecycle state resets cleanly.
5. Navigate away and confirm no console errors related to listener/resource cleanup.

Decision notes:
- This sample should stay minimal and serve as a reference implementation for engine lifecycle patterns.
- Reusable helpers should only move into engine when they improve multiple samples/games.
- Final pass should confirm destroy ownership and avoid hidden coupling in template code.
- Browser tests run from dedicated `tests/testRunner.html` to avoid mutating singleton runtime state during normal sample usage.
